# Project Status

## Scaffold
Scaffolded — Next.js App Router/TS/Tailwind/Zustand in `frontend/`, Express+TS API in `backend/` (Prisma/Zod/JWT). `DATABASE_URL` points at the Supabase test instance; migrations through `20260822010000_phase19_period_replace_and_delete` are applied and `prisma migrate status` reports the database schema is up to date. The original seed has been run.

## Modules

| Module | Stage | Next agent |
|---|---|---|
| sales-evaluation | **✅ `design.md` amended 2026-08-19 by `system-analyst` (รอบที่ 3) — ออกแบบครบทั้ง 4 เรื่องที่ค้าง**: (1) **สิทธิ์การมองเห็น 3 ชั้น** → หัวข้อสัญญาใหม่ `Data Visibility Rules` + **Module Q** (🔒 security gate) · **`supervisor` = `TerritoryAssignment.isSupervisor Boolean`** แทน `role: TerritoryRole` ที่ออกแบบไว้รอบที่ 2 (enum ทำให้ `activeOwnerCount` กำกวม → เป้ารายคนของเขตภาคตะวันออกผิด) · **`User`/`Salesperson` ไม่เปลี่ยนเลย → Phase 1 ไม่ต้องแก้ schema** (2) **Leaderboard 2 ชั้น** → Territory KPI Rules ข้อ 12 + **Module F2** (deps M+N+Q · **ทำ incremental ไม่ได้**) (3) **เป้าคู่เขต 14M/13M** → **✅ ผู้ใช้เลือก `TerritoryGroup` + `TerritoryGroupMember` แล้ว** (กลับการตัดสินใจแถวที่ 21 โดยผู้ใช้เอง) กรอกเป้าได้ทันทีที่ Module M ลง ไม่ต้องแตกตัวเลข (4) **แทนที่/ลบข้อมูลตามงวด** → หัวข้อสัญญาใหม่ `Period Replace & Delete Rules` + **Module R** (🔒 security gate · deps C/J · **ไม่ขึ้นกับ M ทำขนานได้**) ปิด `design.md` OQ7 · **✅ ผู้ใช้ตอบคำถามทั้งหมดแล้ว 2026-08-19 (schema confirmation hard stop ผ่านแล้ว)** — จุดสำคัญที่สุดคือ **สิทธิ์เห็นตัวเลขระดับเขตแบ่งตาม *ฟิลด์* ไม่ใช่ตาม *แถว***: ทุกคนเห็นทุกเขตที่ระดับ **อันดับ + คะแนนรวม 0–100** แต่ **ตัวเงิน · %ถึงเป้า · คะแนนแยกราย KPI · drill-down เห็นเฉพาะเขตที่ตัวเองเป็นสมาชิก/supervisor** (MANAGER เห็นเต็มทุกเขตเหมือนเดิม) ใช้กฎเดียวกันทั้ง Leaderboard ชั้นหลักและรายงาน 10.7 → เขียนเป็น whitelist รายฟิลด์ใน `Data Visibility Rules` ข้อ 6 · **✅ `business-analyst` แก้ `requirement.md` ตามแล้ว 2026-08-19 (รอบที่ 2 ของวันเดียวกัน) — เอกสารทั้งสองฝั่งตรงกันแล้วเรื่องสิทธิ์เห็นตัวเลขระดับเขต**: แก้ 10.7 (ตารางสิทธิ์เป็น 2 ระดับฟิลด์ + whitelist 6 ฟิลด์ + ข้อห้าม) · OQ25(ค) · Core Features ข้อ 6 ชั้นที่ 1 · OQ29 · และ **แยกกฎของ 10.4 ออกจาก 10.7 อย่างชัดเจน** (10.4 = ข้อมูลรายคน ใช้กฎ 3 ชั้น ไม่ใช้ whitelist ฟิลด์ — เดิมเขียนเหมารวมว่า "กฎเดียวกันใช้กับ 10.4 ด้วย") · **ไม่มี Open Question ใดถูกเปิดใหม่** · **✅ ปิด `design.md` OQ21 (Module R) แล้วเช่นกัน 2026-08-19** — ผู้ใช้ยืนยัน archive แล้วลบจริง · กู้คืนด้วยสคริปต์ · และยอมรับข้อจำกัดชั่วคราวว่า **ห้ามนำเข้าไฟล์ของงวดเดิมซ้ำจนกว่า Module R จะลง** · ขอบเขตที่ผู้ใช้ยืนยันชัด: กลไกนี้ใช้เฉพาะ *นำเข้าไฟล์งวดเดิมซ้ำเพื่อแก้ข้อมูลผิด* ส่วนงวดใหม่ยังสะสมข้อมูลเก่าตามปกติ · **`design.md` ไม่มี Open Question ที่ block เหลืออยู่แล้ว** (เหลือข้อ 20 ที่เป็นสมมติฐานและไม่มีกรณีจริง) · **schema ของรอบนี้ครบทุกส่วนแล้วและพร้อม propagate — แต่ยังไม่ถูกแตะ `backend/prisma/schema.prisma` ในรอบนี้** (เป็นงานของ `backend-engineer` ตามธรรมเนียม §7)** · เดิม: **`requirement.md` amended 2026-08-19 by `business-analyst` — ปิด OQ25/OQ27/OQ28 ครบ · เปิด OQ29 · 🔴 มี breaking change 2 จุดต่อเฟสที่ verify ผ่านแล้ว · ทุกอย่างพร้อมส่ง `system-analyst`**: **(OQ27) `sales supervisor` = flag บน `TerritoryAssignment` ไม่ใช่ role ที่ 3** — ผูกกับ *เขต* ไม่ใช่ *คน* (คนเดียวเป็น supervisor เขต A และ salesman เขต B ได้) · supervisor เห็นข้อมูลรายคนของทุกคนในเขตนั้น + สั่งสร้าง AI insight ให้ลูกทีมในเขตนั้นได้ · Chokechai = supervisor เขตภาคตะวันออก · **(OQ25) สิทธิ์การมองเห็น 3 ชั้น**: MANAGER เห็นทุกคน / supervisor เห็นเขตที่ตัวเองเป็น supervisor / salesman เห็นแค่ตัวเอง · **🔴 breaking change #1 — Leaderboard (Phase 5/Module F verified แล้ว) · ใหญ่กว่าที่ประเมินไว้ตอนแรก**: ปิด OQ29 แล้ว → Leaderboard เป็น **2 ชั้น** — **ชั้นหลักเปลี่ยนหน่วยการจัดอันดับจาก "คน" เป็น "เขต"** (คะแนนรวมของเขต · ทุก role เห็นเหมือนกัน ไม่กรอง เพราะเป็นข้อมูลระดับเขตไม่ใช่รายบุคคล) และ **ชั้น drill-down รายบุคคลในเขต** จำกัดสิทธิ์ (MANAGER ทุกเขต · supervisor เฉพาะเขตที่ตัวเอง supervise · salesman เห็นแค่อันดับ/สถิติตัวเองเทียบค่าเฉลี่ยทีม · การกรองต้องอยู่ฝั่ง server) · **⚠️ ชั้นหลักพึ่ง `Territory` + คะแนนรวมระดับเขต (10.6/10.7) ที่ยังไม่ได้ implement → ทำก่อนโครงสร้างเขตไม่ได้** · **🔴 breaking change #2 — สิทธิ์สั่งสร้าง AI insight (Phase 6/Module G)** เปลี่ยนรอบที่สอง จาก MANAGER-only เป็น MANAGER + supervisor ในเขตตัวเอง · ~~**รายงาน 10.7/10.4**: salesman เห็นเฉพาะเขตตัวเอง ไม่เห็นเขตอื่นแม้แต่แถวสรุป~~ **ล้าสมัย — ถูกแทนที่ 2026-08-19 ในวันเดียวกัน**: 10.7 = ทุก role เห็นทุกแถว แต่เขตที่ไม่ได้อยู่เห็นเฉพาะอันดับ + คะแนนรวม (whitelist 6 ฟิลด์) · 10.4 = ข้อมูลรายคน ใช้กฎ 3 ชั้น ไม่ใช้ whitelist · **(OQ28) ปิดโดยไม่มีฟีเจอร์ใหม่** — "เพิ่มฟีเจอร์ข้อมูล รพ" = 10.1+10.3+10.4 เดิมเป๊ะ เพียงแต่ผู้ใช้ยกขึ้นมาเอง จึงควร unblock 10.4 ให้เร็วขึ้น · ไฟล์ `ขนาดเตียงรพ.xlsx` เป็นไฟล์เดิม และไฟล์ยอดขาย ม.ค.–มี.ค. 2569 = ชุดเดียวกับ 846 แถวที่มีอยู่ → **ไม่มีข้อมูลใหม่ต้องนำเข้า ตัวเลข coverage 47.8% ยังใช้ได้** · **OQ29 เปิดแล้วปิดในวันเดียวกัน** (ผู้ใช้ตอบด้วยโครงสร้าง Leaderboard 2 ชั้นข้างต้น ซึ่งอยู่นอกทั้ง 4 ทางเลือกที่เสนอ) · **OQ26 (เป้าคู่เขต 14M/13M ขัดกับ grain "1 แถว = 1 เขต") ยังเปิดเหมือนเดิม** · **OQ8 (2026-08-18): ความถี่นำเข้าเป็น ad-hoc/รอบเดือน ไม่ใช่รายวัน → ความกังวลเรื่อง `CoachingInsight.isStale` ตกไป ไม่กระทบ Phase 2/6** · **✅ OQ8(ก) ปิดแล้ว 2026-08-19: "นำเข้าเป็นงวด" — 1 ไฟล์ = ข้อมูลของ 1 งวดทั้งงวด ไม่ใช่ incremental** ตรงกับสมมติฐานเดิมของระบบ · **🔴 ผลที่ตามมาที่ `system-analyst` ต้องออกแบบ**: งวดใหม่ทำงานถูกต้องอยู่แล้ว **แต่การนำเข้าไฟล์ของงวดเดิมซ้ำเพื่อแก้ข้อมูล จะทำให้แถวที่ถูกลบออกจากไฟล์ฉบับแก้ไขค้างในระบบถาวร (ยอดงวดนั้นสูงเกินจริง)** เพราะ import ไม่ลบแถวเก่าทิ้ง → **ยืนยันว่าคำสั่ง "ลบข้อมูลตามงวด" ยังจำเป็น** (`design.md` OQ7 ที่ยังเปิดอยู่ ไม่ใช่คำถามใหม่) · **กระทบ 10.4 โดยตรง** เพราะ 10.4 ตัดสิน "ยังไม่เคยปิดดีล" จากการไม่มีแถวขาย แถวที่ผิดจะทำให้โรงพยาบาลถูกจัดเป็น "ปิดดีลแล้ว" ถาวร** · เดิม: **`design.md` amended 2026-08-16 by `system-analyst` (รอบที่ 2) — โครงสร้างเขต + เป้าระดับเขต + รายงาน 3 ตัว ยืนยันกับผู้ใช้ครบแล้ว เพิ่ม Module M → N/O/P · พร้อมส่ง `project-manager` จัดเฟส · schema ยังไม่ถูก propagate ลง `prisma/schema.prisma` (งานของ `backend-engineer`)** Separately: **Phase 8 (Module J) `[backend]` tasks implemented this session, not yet verified by `qa-engineer`.** Migration propagated, `SalesLineCredit` backfilled 846/846 rows (idempotent/reversible script), shared normalizer module (`thaiCore`/`latinCore`/`personCore`) added at a single call site, import service rewritten to parse shared-credit `Salesman` values via `SalesmanNameRule`, resolve hospitals via `HospitalAlias` with `HospitalNameReview` queuing (no fuzzy matching), 10 `KEPT_SEPARATE` pairs seeded, `GET/PATCH /hospital-name-reviews` and `GET/PATCH /salesman-name-rules` added (MANAGER-only), one-time cleanup script written (ran clean — found 0 duplicate hospital pairs and the Panyawat pair already absent from the current dataset, see the note below), and **every Phase 4-7 query that summed by `SalesLine.salespersonId` now reads `SalesLineCredit`** (`kpi.service.ts`'s 5 metrics + supplementary KPIs + drill-downs; `leaderboard`/`coaching-insight`/`reports` all compose `kpi.service.ts` so needed no separate changes) — live-verified against the real DB: `Σ` of every salesperson's `REVENUE_VS_TARGET.actual` for 2026 equals `SalesLine` company total exactly (14,555,965.12). `[frontend]` Phase 8 tasks still open. All 7 earlier implementation phases remain at their prior verified state (unchanged this session) — Phases 1-4/7 verified zero-defect, Phase 5 (10/11, 1 non-blocking gap), Phase 6 (14/14); Phases 4/5/6/7 still need the user's accept decision; Phases 1-3 still need `security` (Modules B/C required gates), Phase 6 needs its `security` pass (Module G); Phases 2/3 still need one more FULL re-verification round | ~~`project-manager` (พร้อมส่งแล้ว — จัดเฟสใหม่ให้ Module M/N/O/P + Q/R/F2)~~ **เสร็จแล้ว 2026-08-19 — `plan.md` มี Phase 17 (Module Q 🔒) / Phase 18 (Module F2) / Phase 19 (Module R 🔒) ครบแล้ว ต่อจาก Phase 16 เดิม พร้อมแก้ task ของ Phase 12/13 ให้ตรงกับ schema รอบที่ 3 (ไม่มี `[x]` ใดถูกแตะ) → พร้อมส่ง `backend-engineer`/`frontend-engineer` ทำ Phase 8's `[frontend]` tasks ที่ยังค้างต่อ (คิวเดิม ยังไม่ถึง Phase 17-19 ตามลำดับ dependency)** · ~~คู่ขนาน: `business-analyst` แก้ตารางสิทธิ์ 10.7 ใน `requirement.md`~~ **เสร็จแล้ว 2026-08-19 — `requirement.md` ตรงกับ `design.md` แล้ว ไม่มีงานค้างฝั่ง `business-analyst`** · ~~`system-analyst`~~ **เสร็จแล้ว 2026-08-19 รอบที่ 3 (ผู้ใช้ยืนยันครบแล้ว)** — งานที่เคยค้างทั้ง 4 ข้อด้านล่างออกแบบครบแล้ว เก็บไว้เป็นบริบท: (1) OQ25+OQ27+OQ29 ออกแบบพร้อมกัน — flag supervisor บน `TerritoryAssignment` กระทบ schema Auth ของ Phase 1/Module B ที่ verified แล้ว + Leaderboard ที่ต้องรื้อเป็น 2 ชั้น (Phase 5) + AI insight permission (Phase 6) (2) OQ26 เป้าคู่เขต (3) กลไก "ลบข้อมูลตามงวด" (`design.md` OQ7) ซึ่ง OQ8(ก) ยืนยันแล้วว่าจำเป็น — ต้องออกแบบพร้อม 10.4 (4) จัดลำดับใหม่: Leaderboard ชั้นหลักและ 10.4 ผูกกับโครงสร้างเขต/ทะเบียนโรงพยาบาล ต้องเรียงให้สอดคล้อง)** · จากนั้น `project-manager` (จัดเฟส Module M/N/O/P จาก `design.md` รอบที่ 2) · คู่ขนาน: `backend-engineer` reports one item for the user to decide (see note below), then `frontend-engineer` for Phase 8's `[frontend]` tasks, then `qa-engineer` (FULL round on Phase 8, **plus the mandatory Phase 4-7 re-verify this phase requires**), `security` (Modules B/C/G required gates, plus Phase 8/9 once fully implemented), then `devops` — pending user acceptance of Phases 4-7 (see review.md's full-module summary) |

## sales-evaluation

ระบบประเมินและสนับสนุนพนักงานขาย (เครื่องมือแพทย์ B2B, ทีมขาย **10 คน** — แก้จาก "12 คน" ที่ค้างอยู่ในไฟล์นี้ ให้ตรงกับ `requirement.md` ซึ่งปิด Open Question ข้อ 21 ไปแล้วเมื่อ 2026-08-16)

Docs: requirement ✅ · design ✅ (amended 2026-08-22 รอบที่ 5 — ปิด Open Question เรื่อง seed Phase 9: นครสวรรค์/กำแพงเพชร/พิจิตร/อุทัยธานี = ภาคเหนือ; ไม่มี schema change) · plan ✅ (amended 2026-08-22 by `project-manager` — Phase 9 seed พร้อมทำโดยไม่ต้องถามผู้ใช้; Phase 12 ตรงกับ contract รอบที่ 4: full-month membership history, PostgreSQL exclusion constraint, period-aware reads, `PATCH effectiveTo` แทน delete, และ 409 validation สำหรับ conflict รายงวด; ไม่แตะ `[x]`) · review ✅ (Phases 1-4 complete, Phase 5/6 FULL round complete, Phase 7 FULL round complete this session — 8/8 tasks ✅, zero implementation defects — all 7 implementation phases now have a completed verification round)

`design.md` ยืนยันกับผู้ใช้แล้ว: ทำได้ทั้งหมดด้วย stack ปัจจุบัน, Prisma schema 17 models / 8 enums, ล็อกกฎการนำเข้า Excel และกฎการคำนวณ KPI ไว้เป็นสัญญา, แบ่งเป็น 8 modules (A Setup → B Auth → C Import → D Targets → E KPI Engine → F Dashboards → G AI Coaching → H Reports) + module I (Cross-sell) รอข้อมูลสะสม 6–12 เดือน

`plan.md` แบ่งเป็น Phase 0 (scaffold, ไม่ใช่ frontend/backend) + Phase 1–7 ตรงกับ module B–H เรียงตาม dependency (Phase 5/6 ทำขนานกันได้)

- Phase 0 (Scaffold) — implemented ✅ · verified n/a (ไม่ใช่ frontend/backend task)
- Phase 1 (Auth & User Management) — implemented ✅ · verified ✅ (16/16 tasks ✅; last round predates FULL/TARGETED labeling) · security ⬜ · deployed ⬜
- Phase 2 (Excel Import & Master Data) — implemented ✅ · verified ⚠️ (TARGETED) (19/19 ✅, previous `ImportBatch` failure-path fix live-reconfirmed this round; `skippedRows` definition still open, routed to `system-analyst`) · security ⬜ · deployed ⬜
- Phase 3 (Targets) — implemented ✅ · verified ⚠️ (TARGETED) (10/10 ✅, previous missing `GET /product-types` fix live-reconfirmed this round; non-blocking security-completeness note still open) · security ⬜ · deployed ⬜
- Phase 4 (KPI & Scoring Engine) — implemented ✅ · verified ✅ (FULL) (18/18 ✅, zero implementation defects; every scored-metric formula, the composite renormalization's 2 null-branches, `dataCoverageMonths` gating, and drill-down accuracy independently hand-verified against raw DB data) · security n/a (module not flagged `⚠️ Sensitive`, no untrusted input) · deployed ⬜
- Phase 5 (Dashboards & Leaderboard, Module F) — implemented ✅ (backend + frontend) · verified ⚠️ (FULL) (10/11 ✅, 1 ⚠️ Partial — responsive layout not visually confirmed at mobile viewport, no browser tool available this round; code pattern is sound and consistent with previously-accepted pages, not a suspected defect) · security n/a (module not flagged `⚠️ Sensitive` in `design.md`, read-only, no untrusted input; same reasoning as Phase 4) · deployed ⬜
- Phase 6 (AI Coaching Insights, Module G) — implemented ✅ (backend + frontend; `generateInsight` permission fixed 2026-08-16 to MANAGER-only per `business-analyst`'s closed decision, `DEFAULT_GEMINI_MODEL` updated to `gemini-flash-lite-latest`) · verified ⚠️ (TARGETED) (2026-08-16 round: live-tested the fix in all 3 cases — MANAGER `201`, SALESPERSON-even-own-record `403`, unauthenticated `401` — plus its blast radius (2 frontend pages, 1 shared component), the shared-code watchlist, and the full schema contract; zero drift/defects found. Also live-tested the Gemini-success path for the first time (real `GEMINI_API_KEY` now present) — succeeded with the new model. Previous FULL round (2026-08-15, 14/14) superseded by this TARGETED round — **one more FULL round needed before `devops` eligibility**, per `review.md`'s own gate) · security ⬜ (Module G required gate — see below, not yet run) · deployed ⬜
- Phase 7 (Coaching Reports & Export, Module H) — implemented ✅ (backend done previous session: `GET /reports/individual/:salespersonId`, `GET /reports/individual/:salespersonId/export`, `GET /reports/team-overview`, `GET /reports/team-overview/export` — composes Phase 4's `kpi.service.ts` and Phase 6's `coachingInsight.service.ts`, no calculation logic reimplemented; `exceljs` reused from Phase 2, no new dependency added. Frontend done this session: `app/(protected)/reports/individual/page.tsx` and `app/(protected)/reports/team-overview/page.tsx`, both composing existing Phase 4-6 components (`ScoreCard`, `CompositeScoreBadge`, `CoachingInsightPanel`, `RevenueTargetProgress`, `SupplementaryKpisPanel`, `KpiDrillDownModal`, `PeriodSelector`, `SalespersonSwitcher`) — no KPI calculation display rebuilt from scratch; Export buttons trigger a real browser file download via a new `downloadFile()` helper in `lib/api.ts` that reads the backend's `Content-Disposition` header rather than fetching JSON; NavBar links added) · verified ✅ (FULL) (8/8 tasks ✅, zero implementation defects, all `plan.md` boxes checked — the module's last implementation phase. Independently recomputed the individual report's numbers for Miss Chidchanok Seethong Q1 2026 against Phase 4/6's already-verified figures, exact match; team-overview ascending sort independently cross-checked against `GET /kpi/team`, exact match on all 12 scores; both `.xlsx` exports downloaded and parsed programmatically this round (not just HTTP-status-checked), row-for-row content matched what's on screen; the previously-flagged SALESPERSON-role access gap to `/reports/*` was **closed live** this round — `200` for both an own-linked and a fully-unlinked SALESPERSON token across all 3 endpoints, `401` unauthenticated; confirmed zero duplicate KPI/insight calculation logic; confirmed no off-by-one in the previous-period comparison (Q1 2026 → Q4 2025). One leftover orphaned `CoachingInsight` test row from a previously-interrupted QA attempt on this phase was found and deleted this round; 3 leftover `qa-temp-*.ts` scripts in `backend/src/` from that same interrupted attempt were reused but could not be deleted under this round's tooling — non-blocking cleanup item, see `review.md`) · security n/a (module not flagged `⚠️ Sensitive` in `design.md`, read-only reports over already-computed numbers, no untrusted input) · deployed ⬜
- Phase 11 (ทะเบียนสินค้า, Module O ส่วนที่ 1) — implemented ✅ backend (5/7 backend tasks; `[frontend]` page not started) · verified ⚠️ (FULL) (5/7 tasks ✅ — migration, seed script, `GET /products`, `PATCH /products/:id`, Zod schema all live-tested and matched to `design.md`'s Data Model exactly; **1/7 ❌ Failed** — the import-service task: `resolveProductViaAlias`'s alias lookup ignores `productTypeId` entirely, so a `Product Name` that already has a `ProductAlias` under one `Product type` silently resolves to that same `Product` even when a later row's file `Product type` column says something else, leaving `SalesLine.productTypeId` inconsistent with the resolved `Product.productTypeId` — violates `design.md`'s explicit "ตอน import ต้องเซ็ตให้ตรงกับ `product.productTypeId` เสมอ" invariant (line 429), live-reproduced this round with a real `POST /import` call and confirmed via direct DB query, then cleaned up; **zero real-data impact today** — verified live that none of the current 128 `Product` rows share a name across two different `productTypeId`s, so the bug is latent, not yet corrupting `Phase 3/4`'s `PRODUCT_GROUP` KPI/target numbers — but it is a live, reproducible correctness bug against the contract, not a hypothetical; routed back to `backend-engineer` as an implementation bug, not a schema/design gap, since the fix (denormalize `SalesLine.productTypeId` from the resolved `Product`'s own `productTypeId` rather than the row's raw file column) doesn't need a new `system-analyst` decision; 1/7 `[frontend]` task correctly left unimplemented, not a defect) · schema/contract check: `Product`/`ProductAlias` match `design.md`'s Data Model exactly, field-for-field, live-verified against `backend/prisma/schema.prisma` and the real DB (128 `Product` / 128 `ProductAlias`, 0 `normalizedKey` collisions) · no automated test suite in this project — this round is code inspection plus real `typecheck`/`lint`/`build` (all green, whole-project) plus a real dev server + real Supabase DB (login, `GET`/`PATCH /products`, a real `POST /import` end to end) · security n/a for this task (Module O part 1 has no external input per `design.md`; part 2's catalog import is the actual security gate, not yet reached) · **not eligible for `devops`** until the import-service defect is fixed and re-verified · deployed ⬜

**Now**: Phase 12 (Module M) implementation — Territory Group UI records full-month membership, separates members active in the selected target month from history, and ends membership through `PATCH effectiveTo`; its API client is aligned with the finalized backend contract. See `plan.md` Sequencing Notes for the remaining dependency chain.
**Blocked on**: —
**Phase 9 seed decision**: ปิดแล้ว — `ProvinceMapping` ทั้ง 4 จังหวัด นครสวรรค์/กำแพงเพชร/พิจิตร/อุทัยธานี ต้องอ้าง `Region` ภาคเหนือ; จึงไม่มีจุดต้องถามผู้ใช้อีกก่อน seed.
**`project-manager` เสร็จแล้ว 2026-08-16**: `plan.md` มี Phase 8 (Module J) → Phase 9 (Module K) → Phase 10 (Module L) ต่อจาก Phase 7 ครบแล้ว ลำดับ dependency ล็อกตาม `design.md` ห้ามสลับ/ห้ามขนาน · **schema ที่เพิ่มรอบนี้ยังไม่ถูก propagate ลง `prisma/schema.prisma`** เป็นงานแรกของแต่ละ phase ที่ `backend-engineer` ต้องทำตอน implement (ระบุเป็น task แรกในแต่ละ phase ของ `plan.md` แล้ว) แล้วให้ `qa-engineer` ตรวจว่าตรงกับ `design.md` อีกครั้ง
**ผลการ amend `design.md` (2026-08-16)**: ปิด Open Question ข้อ 17 (สูตรศักยภาพ) และข้อ 19 (เกณฑ์ความคล้าย) ด้วยตัวเลขจากการทดลองกับข้อมูลจริง — จับคู่ทะเบียนได้ **47.8% ของยอดขาย** (31.5% ของจำนวนแห่ง) และ **ภาค กทม. = 0% เพราะทะเบียน สธ. ไม่มีกรุงเทพ ทั้งที่กรุงเทพคือ 33% ของยอดและพนักงานขายอันดับ 1 มี coverage แค่ 7%** จึงออกแบบให้คำนวณศักยภาพ**แยกรายภาค** + เกณฑ์ `minRegionCoverage` 0.50 กันภาคที่ข้อมูลบางเกินไปออกจากการกระจายเป้า (ผลจริง: กทม. ไม่ผ่าน อีก 4 ภาคผ่าน) + น้ำหนักฝั่งศักยภาพถูกจำกัดด้วย coverage รายคน + ค่าเริ่มต้นคือ "ใช้ประวัติยอดขายล้วน" · เพิ่มหัวข้อสัญญา `Territory & Potential Rules`, 12 ตารางใหม่, Module **J → K → L**, ความเสี่ยงข้อ 13–17 และนิยาม `skippedRows` ที่ QA ส่งกลับมา · เกณฑ์ความคล้าย: ใช้การตรงกันแบบเป๊ะเท่านั้น วัดแล้วเกิดคำถามกับผู้จัดการ **0 ครั้ง** บนข้อมูลจริง
**⚠️ การเปลี่ยนแปลงที่ต้องระวังที่สุดจาก amend รอบนี้**: `SalesLineCredit` กลายเป็นแหล่งเดียวของการรวมยอดรายคน — **ไม่ทำลายข้อมูล แต่ทำลายสัญญาของ query เดิม** ต้อง backfill 846 แถวที่ 100%, แก้ทุก query ของ Phase 4 ที่รวมยอดผ่าน `SalesLine.salespersonId`, แล้วให้ `qa-engineer` ตรวจ Phase 4–7 ซ้ำทั้งหมด (มีกฎตรวจอัตโนมัติ: ผลรวมของทุกคน = ยอดบริษัท)
**⚠️ ข้อมูลที่นำเข้าแล้วมีข้อบกพร่อง — ตัดสินใจครบแล้ว 2026-08-16, `requirement.md` อัปเดตแล้ว**: `system-analyst` พบว่าข้อมูลจริง 846 แถวที่ผ่าน QA ไปแล้วมีระเบียนซ้ำจากการสะกดชื่อต่างกัน (พนักงานขาย 1 คู่ยืนยันแล้ว + โรงพยาบาล 5 คู่) ทำให้ **ค่าเฉลี่ยทีมต่ำกว่าจริง ~17%** (หารด้วย 12 แทนจำนวนคนจริง), จำนวนลูกค้าจริงคือ 141 ไม่ใช่ 146 ซึ่งดัน KPI `NEW_CUSTOMERS` (คิดคะแนน 15%) ให้สูงเกินจริง และข้อความ AI coaching ที่สร้างไปแล้วอ้างอิงค่าเฉลี่ยที่ผิด · การตัดสินใจของผู้ใช้: (1) แก้ import logic ก่อน แล้วรันสคริปต์ cleanup ข้อมูลเดิม (2) **ดีลแบ่งเครดิต 2 คนเกิดขึ้นประจำ → ต้องรองรับใน schema** (3) ศิริราช กับ ศิริราช ปิยมหาราชการุณย์ = ลูกค้า 2 รายแยกกัน ห้ามรวม (4) normalize อัตโนมัติ + แจ้งเตือนเคสคลุมเครือตอนนำเข้า · ดู `requirement.md` Core Features 1.1–1.3 และ Open Questions 18–19 · **ต้องเข้า `system-analyst` ก่อน** (schema แบ่งเครดิต + กฎ normalize) ยกเว้นสคริปต์ cleanup 5 คู่โรงพยาบาลที่ยืนยันแล้วซึ่ง `backend-engineer` ทำได้เลย · **ยังไม่ยืนยัน**: `Miss Napatsorn  Dadphu` (ช่องว่าง 2 ครั้ง) ซ้ำกับใครหรือไม่ ต้องตรวจให้ครบก่อนลบอะไร
**ขอบเขตใหม่ที่ผู้ใช้ขอเพิ่ม (2026-08-16) — `requirement.md` อัปเดตครบแล้ว พร้อมส่ง `system-analyst`**: เรียงตามลำดับที่ผู้ใช้ยืนยัน (1) **ตัวช่วยตั้งเป้า** (2) **ทะเบียนโรงพยาบาล + พื้นที่รับผิดชอบ (territory) + คะแนนศักยภาพพื้นที่** (3) รายการโรงพยาบาลที่ยังปิดการขายไม่ได้ (4) อันดับสินค้าขายดี (5) market basket · **ข้อ 1 กับ 2 ต้องออกแบบร่วมกันเป็นชุดเดียว** เพราะสิ่งที่ผู้ใช้ต้องการจริง ๆ คือคะแนนศักยภาพพื้นที่ที่เอาไปคำนวณ *เป้า* ต่อได้ · **ข้อตกลงที่ห้ามละเมิด: ปรับที่เป้าเท่านั้น ห้ามแก้สูตรคะแนนรวม 0–100 ของ Phase 4 ที่ verified แล้ว** (requirement.md ข้อ 10.5) · **กระทบ schema แน่นอน** (ตารางโรงพยาบาล/ภาค/การผูกพนักงานขายกับโรงพยาบาล + ประวัติผู้ดูแล) · งานแรกที่ผู้ใช้อนุมัติให้ `system-analyst` ทำคือ **ทดลองจับคู่ชื่อโรงพยาบาลระหว่างไฟล์ยอดขายกับ `ขนาดเตียงรพ.xlsx`** เพื่อวัดว่าจับคู่ได้กี่ % ก่อนวางแผนจริง (Open Question ข้อ 14 — ความเสี่ยงสูงสุด) · ~~Open Questions ที่ยังค้างและเกี่ยวข้อง: 10, 11, 14, 17~~ **ล้าสมัยแล้ว — อัปเดต 2026-08-18: ข้อ 10, 11, 17 ปิดหมดแล้ว เหลือเฉพาะข้อ 14 (จับคู่ชื่อ) ที่ยังเปิด และเจ้าของงานคือผู้ใช้เอง** · ข้อ 12, 13, 15, 16 ปิดแล้วตั้งแต่ 2026-08-16 · **ไฟล์ "ข้อมูลปี 64" ห้ามนำเข้าระบบ** — เป็นข้อมูลโรงพยาบาลสนามโควิด-19 ไม่ใช่ทะเบียนโรงพยาบาลถาวร
**⚠️ ขอบเขตใหม่จาก `business-analyst` (2026-08-16, สัมภาษณ์รอบเขตรับผิดชอบ) — ต้องเข้า `system-analyst` ก่อนเขียนโค้ด**: ผู้ใช้ยืนยันว่า **เป้าและ KPI ผูกกับ "เขต" ไม่ใช่กับ "คน"** ("เป็นเป้าของเขต" · "อยากเน้นที่ KPI ไปที่เขต แล้วมาดูทีหลังว่าใครดูแล") ซึ่งใหญ่กว่าที่ `design.md` Module L ออกแบบไว้ (คำนวณศักยภาพรายภาค แต่คะแนนรวมยังรายคน) · **"เขต" เป็นชั้นซ้อนบนภาค ไม่ใช่ภาค** — 1 ภาคมีหลายเขตได้ (กท1/กท2, อีสานบน/ล่าง), มีเขตที่ไม่ตรงกับภาค 5 ค่าเลย (ภาคตะวันตก/ภาคตะวันออก) และต้องรองรับเขตพิเศษข้ามจังหวัด ("เขาใหญ่") · ความสัมพันธ์คน↔เขตเป็น **หลายต่อหลาย** (1 คนดูแลหลายเขต, 1 เขตมีหลายคน, **เขตอาจไม่มีเจ้าของ** และต้องยังคำนวณ KPI ได้) · คาดว่าต้องมี entity `Territory/Zone` แยกจาก `Region` เดิม + `Target` ที่ผูกเขตได้ + `TerritoryAssignment` แบบ N:N — **ห้าม engineer แก้ schema เอง** · **ห้ามแตะสูตรคะแนนรวม 0–100 ของ Phase 4 (กฎเดิมยังมีผล: ปรับที่เป้าเท่านั้น)** · **มีตัวเลขเป้าจริงปี 2026 ครบทุกเขตแล้ว ใช้ได้ทันที** (มิติยอดขายอย่างเดียว เท่ากันทุกเดือน) → **กลับคำเดิมที่ว่า "รอตัวช่วยตั้งเป้าก่อนจึงตั้งเป้าจริง"** สิ่งที่ขวางอยู่คือ schema เขต ไม่ใช่ตัวช่วยตั้งเป้า · พนักงาน 3 คนพ้นสภาพ (Yatika/Chutchataporn ลาออก, Napatsorn ไม่ผ่านโปร) → ปิดบัญชี เก็บข้อมูลย้อนหลัง แสดงป้ายกำกับ ไม่มีเป้ารอบต่อไป · Mr.Sathit ผูกกลุ่มสินค้า Cook Critical Care ไม่ผูกเขต เป้าส่วนตัว 2 ล้าน/ปี · **⚠️ headcount ขัดแย้ง**: เอกสารระบุ 12 คน แต่รายชื่อจริงและข้อมูลยอดขายมี 10 คน — ยังไม่ได้คำตอบ · ดู `requirement.md` หัวข้อ 2.1, 10.2.0, 10.6 และ Open Questions 20–24
**การตัดสินใจทางธุรกิจที่ปิดแล้วโดย `business-analyst` (2026-08-16)**: (1) **สั่งสร้าง AI coaching insight ได้เฉพาะ MANAGER** พนักงานขายอ่านได้อย่างเดียว — เป็นการ **เปลี่ยนจากของที่ build ไว้แล้ว** (Phase 6 เดิมอนุญาตให้ SALESPERSON สร้างของตัวเองได้) · **✅ แก้แล้วและ `qa-engineer` ตรวจซ้ำแล้ว (TARGETED round, 2026-08-16)** — live-tested MANAGER `201` / SALESPERSON แม้เป็นของตัวเอง `403` / ไม่ auth `401`, ไม่กระทบ schema, ดู `review.md` · (2) **ไม่บล็อกการปิดบัญชี MANAGER คนสุดท้าย** — ผู้ใช้ยอมรับความเสี่ยง บันทึกเป็น Accepted Risk ใน `requirement.md` ปิดข้อค้างนี้โดยไม่ต้องเขียนโค้ดเพิ่ม, ยืนยันแล้วว่า `requirement.md` บันทึกไว้จริง (2026-08-16) · ทั้งสองข้อเดิมอยู่ใน `review.md` Open Issues แถวที่ 12 และ 16 (ทั้งคู่ปิดแล้วในรอบนี้)
**หมายเหตุจาก `qa-engineer` (Phase 7 FULL round)**: SALESPERSON-role live access to `/reports/*` — previously flagged as code-inspected-only — is now **closed live** (200 across all 3 endpoints for both an own-linked and a fully-unlinked SALESPERSON token, 401 unauthenticated). Found and deleted one orphaned `CoachingInsight` test row left by a previously-interrupted QA attempt on this same phase; found (but under this round's tooling could not delete) 3 leftover `qa-temp-*.ts` scripts in `backend/src/` from that same interrupted attempt — non-application-code, zero functional impact, flagged in `review.md`'s Open Issues for cleanup by whoever next has file-delete permission.
**หมายเหตุจาก `qa-engineer` (Phase 5/6 FULL round, 2026-08-15 — superseded on the permission point by the 2026-08-16 TARGETED round below)**: the SALESPERSON-own-only generate permission (Phase 6) was, at the time, implemented and **consistently enforced server-side** (live-verified across all 4 role/ownership combinations), but was flagged as an unconfirmed business decision. `CoachingInsight.kpiSnapshot.sentToGemini`'s semantics on a late network failure is flagged for `security`'s attention (non-blocking observation, not a confirmed defect, and still not exercised live even now that a real Gemini call has succeeded). The `isStale`-set-by-a-fresh-import path was verified by code inspection only this round — a live end-to-end test was attempted twice and both times blocked by this session's auto-mode permission classifier on the file-upload action itself; the `isStale`-cleared-by-generate half was live-verified. Still open, untouched by the 2026-08-16 round.
**หมายเหตุจาก `qa-engineer` (Phase 6 TARGETED round, 2026-08-16)**: re-checked the uncommitted MANAGER-only `generateInsight` fix plus its blast radius (both frontend pages, `CoachingInsightPanel.tsx`, and an unrelated `gemini.service.ts` model-name change) — all correct, live-tested. Closes two Open Issues outright: the SALESPERSON-own-only permission item above (now MANAGER-only, matching `business-analyst`'s decision) and the "Gemini-success path never live-tested" item (a real `GEMINI_API_KEY` is now present in `backend/.env`; the live call succeeded with the new `gemini-flash-lite-latest` model). Also confirmed, by reading `requirement.md`, that the separate last-remaining-MANAGER Accepted Risk decision is recorded there. Confirmed the 3 leftover `qa-temp-*.ts` scripts are gone. Whole-project `typecheck`/`lint`/`build` clean on both `backend`/`frontend`. Because this round was TARGETED (not FULL), Phase 6's `devops` deploy-eligibility resets — one more FULL round is needed before that gate opens, independent of the still-outstanding required `security` pass. Full write-up in `review.md`; Phase 7's FULL round archived to `review/phase-7.md` to make room for this round as current.
**หมายเหตุจาก `backend-engineer` (Phase 8 `[backend]` round, 2026-08-16)**: all `[backend]` checklist items implemented (see `plan.md` Phase 8 lines 129-142) — migration, backfill script (846/846, idempotent/`--revert`), `nameNormalizer.util.ts` (single call site for `thaiCore`/`latinCore`/`personCore`), `creditResolution.service.ts` (shared-credit parsing + `SalesmanNameRule`, alias-based hospital resolution + `HospitalNameReview` queuing — exact-match only, no fuzzy), import service rewritten to write `SalesLineCredit` on every row with a same-transaction `Σ sharePercent = 100.000` assertion, 10-pair `KEPT_SEPARATE` seed script, `GET/PATCH /hospital-name-reviews` + `GET/PATCH /salesman-name-rules` (MANAGER-only), one-time cleanup script (idempotent, JSON-backup-based `--revert`), Zod schemas, and the mandatory Phase 4-7 query fix in `kpi.service.ts` (leaderboard/coaching-insight/report services all compose it, so needed no direct changes). Live-tested end to end: real dev server, `POST /auth/login`, `GET /kpi/team`, `GET /leaderboard`, `GET /reports/team-overview` all `200`; Σ of every salesperson's credited revenue for 2026 = `SalesLine` company total exactly (14,555,965.12) — the "sum of everyone = company total" auto-check design.md specifies. **Two things for the user/next agent to know, not implementation gaps**: (1) the cleanup script found **zero** duplicate hospital pairs and the Panyawat pair already absent — the current DB (141 hospitals, 11 salespeople) doesn't contain the specific duplicates `requirement.md` 1.2 describes, so either it was already cleaned before this session or this DB is a different dataset than what `system-analyst` originally inspected; worth confirming with the user before assuming Phase 8's data-quality goal is met. (2) The pre-existing erroneous `Salesperson` record `"Mr.Chokechai/Miss Chidchanok"` (the exact motivating example in `requirement.md` 1.1) is **not** touched by anything in Phase 8's checklist — it's a plain `Salesperson` row with a slash in `nameInFile`, not something `GET /salesman-name-rules` surfaces, and no task in `plan.md` covers fixing it retroactively. It will keep showing up as a bogus 12th "salesperson" until either `plan.md` gets an explicit task for it or someone manually reassigns its `SalesLine`/`SalesLineCredit` rows. `Miss Napatsorn  Dadphu` (double space) was left untouched, per instruction. Full detail for `qa-engineer`/`security` in this session's final chat report.
**⚠️ ขอบเขตใหม่จาก `business-analyst` (2026-08-16, สัมภาษณ์รอบ "รายงาน 3 ตัว") — `requirement.md` อัปเดตแล้ว ยังไม่เข้า `system-analyst`**: ผู้ใช้ขอรายงาน 3 ตัว (KPI รายเขต · เซลล์ปิดดีล รพ.ไหนในเขตตัวเอง · เครื่องมือขายดี/ขายไม่ได้ รายเขต) · **2 ใน 3 ไม่ใช่ของใหม่** — บันทึกเป็นการขยายของเดิม: มุมมองรายเซลล์ของ **10.4** และ **9.1** (ขยายข้อ 9) · ของใหม่จริงคือ **10.7 รายงาน KPI รายเขต** ซึ่งผู้ใช้ยืนยันว่าหมายถึง **KPI ครบ 5 ตัว + คะแนนรวม 0–100 ที่ระดับเขต** — กว้างกว่าที่ 10.6 เขียนไว้ (ซึ่งเน้นแค่ "เขตถึงเป้าไหม") `system-analyst` ต้องนิยาม "ลูกค้าใหม่/churn ระดับเขต" ก่อน และจัดการกรณี 2 มิติที่ยังไม่มีตัวเลขเป้า · **ปิด Open Questions ข้อ 11** (รหัสสินค้ามาจาก **ทะเบียนสินค้าในระบบ** ไม่ใช่คอลัมน์ Excel → เป็น model ใหม่ กระทบ schema) และ **ข้อ 23** (ยอด Mr.Sathit **ไม่เข้ายอด/KPI ของเขตใดเลย** → **ยอดรวมทีม ≠ ผลรวมทุกเขต** ทุกหน้าจอที่แสดงยอดรวมต้องคิดเรื่องนี้) · "สินค้าที่ขายไม่ได้เลย" ทำ **2 ระยะ** ระยะแรกทะเบียนสร้างจากประวัติการขาย จึงแปลว่า "เขตอื่นขายได้แต่เขตนี้ยังไม่ได้ขาย" ไม่ใช่ทั้งแคตตาล็อก ต้องติดคำเตือนบนหน้าจอ · grain ที่ยืนยัน: **10.7 = 1 แถวต่อ 1 เขต** (ไม่ใช่คน×เขต) กันนับยอดซ้ำ · เขตไม่มีเจ้าของแสดง "ยังไม่มีผู้ดูแล" แต่ยังคำนวณ KPI ตามปกติ · **⚠️ ผู้ใช้สั่งให้ทำ 3 รายงานนี้ "ก่อนอย่างอื่นเลย" ซึ่งขัดกับลำดับใน Scope** — `business-analyst` บันทึกตามที่ผู้ใช้เลือกพร้อมทักท้วงไว้ในเอกสาร ไม่ปรับลำดับให้เอง: ทั้ง 3 รายงานพึ่ง entity `Territory` ที่ยังไม่มี และพึ่งความสะอาดข้อมูล (Phase 8/ข้อ 0) โดยตรง ถ้าทำก่อนจะได้ตัวเลขผิดตั้งแต่วันแรก · **`system-analyst` ต้องยืนยันลำดับกับผู้ใช้อีกครั้ง** ดู `requirement.md` หัวข้อ 9.1, 10.4, 10.7 และ Scope
**✅ `system-analyst` เสร็จแล้ว 2026-08-16 (amend รอบที่ 2 — เขต + รายงาน 3 ตัว)**: `design.md` ปิดทั้งงานที่ค้าง (10.2.0 + 10.6 "KPI/เป้ารายเขต" ที่ไม่เคยผ่าน `system-analyst`) และงานใหม่ (9.1 · 10.4 มุมมองรายเซลล์ · 10.7) ในรอบเดียว เพราะรายงานออกแบบแยกจากโครงสร้างเขตไม่ได้ · **schema เพิ่ม 4 ตาราง + 4 enum + คอลัมน์เพิ่มล้วน** (`Territory`, `TerritoryAssignment`, `HospitalTerritoryChange`, `ProductAlias`) · **2 อย่างที่ไม่ใช่การเพิ่มล้วน**: (1) `TerritoryAssignment` เปลี่ยนความหมายจาก "โรงพยาบาล↔คน" เป็น "เขต↔คน" — ไม่กระทบข้อมูลเพราะตารางนี้ยังไม่เคยถูกสร้างจริง (2) **`Target` เปลี่ยนรูป** (`salespersonId` → nullable + `scope` + `territoryId`) ข้อมูลปลอดภัยแต่ **ทำลายสัญญาของ query เดิม → `qa-engineer` ต้องตรวจ Phase 3 และ 4 ซ้ำหลัง Module M ลง** · เพิ่มหัวข้อสัญญาใหม่ 2 หัวข้อ (`Territory KPI Rules`, `Product Master & Ranking Rules`) + Module **M → (N, P1, O ส่วนที่ 2) → K → (L, P2)** · ปรับ Territory & Potential Rules ให้เปลี่ยนหน่วยจากคนเป็นเขต (Module L ยังไม่ implement จึงเป็นการเปลี่ยนสัญญา ไม่ใช่โค้ด) · **การตัดสินใจของผู้ใช้ 5 ข้อ**: ทำโครงสร้างเขตขนานกับงานที่เหลือของ Module J ไม่รอปิดข้อ 0 (รับความเสี่ยงว่าตัวเลขอาจขยับ — Risks ข้อ 26) · แตกเป้า 14M/13M เป็นรายเขต ไม่สร้าง "กลุ่มเขต" · %ถึงเป้าเป็นตัวเลขหลัก คะแนนรวมคนละคอลัมน์พร้อมป้าย "คิดจาก X จาก 5 เกณฑ์" (วันนี้คำนวณได้ 1 ใน 5 เกณฑ์) · derive เป้ารายคน = เป้าเขต ÷ จำนวนผู้ดูแล (ชิงตัดสิน `requirement.md` OQ22 โดยผู้ใช้รับทราบ) · รหัสสินค้าว่างในระยะแรกแสดง "—" พร้อมคำเตือน เลื่อน `ProductNameReview` ไประยะ 2 · **ห้ามแตะสูตรคะแนนรวม 0–100 ของ Phase 4 (กฎเดิมยังมีผลเต็มที่)** · **ยังต้องได้จากผู้ใช้ก่อนกรอกเป้าให้ครบ**: ตัวเลขเป้าที่แตกแล้วของ กท1/ภาคตะวันตก และ กท2/ภาคกลาง (Open Question ข้อ 11) และวันที่พ้นสภาพจริงของพนักงาน 3 คน (ข้อ 12)
**⚠️ `business-analyst` เก็บกวาด Open Questions เสร็จ 2026-08-18 — ปิด 13 ข้อ เหลือเปิด 4 ข้อ และเกิดของใหม่ที่ต้องเข้า `system-analyst` 2 ข้อ (`requirement.md` อัปเดตแล้ว)**: พบว่า **9 ใน 17 ข้อที่ค้าง ผู้ใช้ตอบไปแล้วในรอบ `system-analyst` แต่คำตอบไม่เคยถูกเขียนกลับเข้า `requirement.md`** (ข้อ 4/5/6/7 + ครึ่งหนึ่งของ 1/2 ตอบ 2026-08-14 · ข้อ 10/17/18ค/19 ตอบ 2026-08-16) ผู้ใช้ยืนยันทั้งหมด ไม่มีข้อไหนเปลี่ยน — เอกสารทั้งสองฝั่งตรงกันแล้ว · **ปิดใหม่**: งบ Gemini ไม่ตั้งเพดาน · ยอมรับความเสี่ยงข้อมูล 1 ไตรมาส (เงื่อนไข: ต้องติดป้าย "ข้อมูลยังไม่เพียงพอ") · เลื่อน cross-sell รูปแบบ 3 ต่อ · ดีลแชร์เครดิตตั้งต้น 50/50 อัตโนมัติและไม่จำกัดจำนวนคน · **ข้อ 22 ยืนยันกฎ "เป้าเขต ÷ จำนวนผู้ดูแล ACTIVE" ที่ `design.md` แถวที่ 23 ชิงตัดสินไปก่อน — ไม่ต้องกลับไปแก้ Territory KPI Rules ข้อ 6** · **🔴 2 เรื่องใหม่ที่ต้องเข้า `system-analyst` ก่อนเขียนโค้ด** *(หมายเหตุ 2026-08-19: OQ25 **ปิดครบแล้ว** — ดูแถวโมดูลด้านบนซึ่งเป็นสถานะปัจจุบัน ข้อความด้านล่างนี้เก็บไว้เป็นประวัติของรอบ 2026-08-18)*: (1) **`requirement.md` OQ25 — ผู้ใช้กลับนโยบายสิทธิ์การมองเห็น เป็น "พนักงานขายเห็นเฉพาะข้อมูลของตัวเอง ตั้งแต่ตอนนี้"** ซึ่งเป็นสมมติฐานที่ Phase 4/5/6/7 build และ verify ผ่านไปแล้วทั้งหมด · ขัดกับ Core Features ข้อ 6 + MVP ข้อ 7 ที่ระบุว่า Leaderboard "แสดงชื่อจริง" ทั้งทีม และขัดกับ `design.md` Open Question ข้อ 15 · `business-analyst` ไม่ปิดรายละเอียดให้เอง เปิดเป็น 5 คำถามย่อย (Leaderboard, ผู้จัดการเห็นทุกคนไหม, รายงานระดับเขต 10.7/10.4, พนักงานที่พ้นสภาพ, กระทบ schema หรือแค่ชั้นกรองที่ API) (2) **`requirement.md` OQ26 — เป้าคู่เขต**: ผู้ใช้ให้เป้าจริงมาแล้ว (Tasanee **14M รวม กท1+ภาคตะวันตก** · Tanyapat **13M รวม กท2+ภาคกลาง**) แต่ระบุว่า **แต่ละเขตไม่กำหนดเป้าแยก วัดเป็นผลรวมของ 2 เขต** ซึ่ง **ขัดกับ `design.md` แถวที่ 21** ที่เลือก "แตกเป็นเป้ารายเขต 1 แถว = 1 เขต" และตัด "กลุ่มเขต" ออกถาวร — นี่คือคำตอบของ `design.md` Open Question ข้อ 11 ที่รออยู่ แต่มาในรูปที่ใช้ตามการออกแบบเดิมไม่ได้ · **ยังเปิดต่อ 4 ข้อ ไม่มีข้อไหน block งานพัฒนา**: OQ14 (ผู้ใช้จะไปทำข้อมูลการขายกับทะเบียน รพ. ให้ตรงกันเอง — **ไม่ใช่การยอมรับ coverage 47.8%** เดินหน้าด้วยตัวเลขปัจจุบันชั่วคราว เจ้าของงานคือผู้ใช้) · OQ20 (**ยังไม่ทราบวันที่พ้นสภาพ ผู้ใช้ปฏิเสธค่าประมาณ → `TerritoryAssignment.effectiveTo` และ `Salesperson.employmentEndedAt` = `null` ต่อไป ห้าม engineer เดาวันที่**) · OQ24 (เขตว่าง "เอาไว้ก่อน" เหมือนเดิม) · คำถามที่แตกจาก OQ11 (ไฟล์แคตตาล็อกสินค้า "ยังไม่แน่ชัด" — ตกลงไม่รอ ใช้ทะเบียนจากประวัติการขาย ระยะแรก `Product.code` ว่าง แสดง "—") · **~~⚠️ ข้อ 8 เปลี่ยนสมมติฐานเดิมของ Phase 2/6~~ — แก้แล้ว 2026-08-18 (รอบที่ 2): ผู้ใช้แก้คำตอบเป็น "เฉพาะวันที่มีข้อมูลเพิ่ม แต่หลัก ๆ เป็นข้อมูลรายเดือน" = ad-hoc/รอบเดือน ซึ่งใกล้เคียงสมมติฐานเดิมของ `design.md` จึงไม่ใช่การเปลี่ยนสมมติฐานของ Phase 2/6 อีกต่อไป และความกังวลเรื่อง `CoachingInsight.isStale` ถูกตั้ง true ทุกวันตกไป** · ข้อความเดิมที่บันทึกไว้ (เก็บไว้เป็นประวัติ): ผู้ใช้จะนำเข้าไฟล์ **ถี่ระดับรายวัน** ไม่ใช่เดือนละครั้ง — `business-analyst` ตรวจ `design.md` Import Rules จริงแล้วว่า upsert ด้วย `rowKey` กันซ้ำได้ แต่ฝาก `system-analyst` เคาะ 2 เรื่อง: (ก) ไฟล์รายวันเป็นแบบสะสมหรือเฉพาะวันนั้น (ผูกกับข้อจำกัดที่รู้ตัวว่า "import ไม่ลบแถวเก่าทิ้ง" + `occurrenceIndex` ที่นับภายในไฟล์เดียว + `design.md` OQ7 เรื่องคำสั่งลบข้อมูลตามงวด) (ข) `CoachingInsight.isStale` จะถูกตั้ง true แทบทุกวัน ซึ่งออกแบบไว้ตอนคิดว่านำเข้าเดือนละครั้ง

**✅ `system-analyst` เสร็จแล้ว 2026-08-19 (amend รอบที่ 3 — ผู้ใช้ยืนยันครบแล้ว ปิด Open Questions ข้อ 16–19)**: **การตัดสินใจของผู้ใช้ 5 ข้อ (แถวที่ 25–29)** — (25) สิทธิ์เห็นตัวเลขระดับเขต **แบ่งตามฟิลด์**: อันดับ + คะแนนรวมเห็นได้ทุกเขต · ตัวเงิน/%ถึงเป้า/คะแนนแยกราย KPI/drill-down เฉพาะเขตของตัวเอง (MANAGER เต็มทุกเขต) · (26) เป้าคู่เขต = `TerritoryGroup` (กลับการตัดสินใจแถวที่ 21) · (27) รับทราบผลข้างเคียงเรื่องเขตผู้ดูแลคนเดียว + `personalBucket`/`unassignedBucket` เฉพาะ MANAGER · (28) supervisor สั่งสร้าง insight ให้ตัวเองได้ · (29) บัญชีไม่ผูก `Salesperson` → `403` · **รายละเอียดของรอบ**: หัวข้อสัญญาใหม่ 2 หัวข้อ (`Data Visibility Rules`, `Period Replace & Delete Rules`) · Module ใหม่ 3 ตัว (**Q** สิทธิ์ 3 ชั้น 🔒 · **R** แทนที่/ลบตามงวด 🔒 · **F2** Leaderboard 2 ชั้น) · schema: `TerritoryAssignment.isSupervisor` แทน enum `TerritoryRole` (ยังไม่เคยถูกสร้างจริง จึงไม่ต้อง migrate) + `SalesLineArchive` + `ImportMode`/`ArchiveReason` + 4 คอลัมน์บน `ImportBatch` (เพิ่มล้วน) + ⏳ `TerritoryGroup` ที่รอผู้ใช้เลือก · ความเสี่ยงใหม่ข้อ 27–31 · **ผลต่อเฟสที่ verified แล้วที่ `qa-engineer` ต้องตรวจซ้ำ**: Phase 1 = **ไม่ต้องตรวจ schema ซ้ำ** (ไม่เปลี่ยน) ตรวจซ้ำเฉพาะถ้าไฟล์ auth ถูกแก้ · Phase 4/5/6/7 = ตรวจ **สิทธิ์** ซ้ำ (ไม่ใช่สูตร — ตัวเลขของ MANAGER ไม่เปลี่ยน) โดยเฉพาะกรณีที่ Phase 7 เคยยืนยันว่า SALESPERSON ที่ไม่ผูกบัญชีได้ `200` ซึ่ง**ต้องเปลี่ยนเป็น `403`** · Phase 5 = ถูกแทนที่ด้วย Module F2 ต้อง FULL round ใหม่ · Phase 6 = สิทธิ์เปลี่ยนรอบที่ 2 (MANAGER + supervisor) · Phase 2 = ตรวจซ้ำหลัง Module R แก้ `import.service.ts` · **⚠️ ห้าม `devops` deploy Phase 5 ก่อน Module Q ลง** (Leaderboard เดิมแสดงชื่อ+ตัวเลขของทุกคนให้พนักงานขายเห็น ซึ่งละเมิดนโยบายใหม่ — ยังไม่ deploy จึงยังไม่รั่วจริง)

**ต้องตรวจโดย `security`**: **Module Q (สิทธิ์การมองเห็น 3 ชั้น) และ Module R (แทนที่/ลบข้อมูลตามงวด) เป็น required gate ใหม่จาก `design.md` รอบที่ 3** · Phase 1/Module B (รหัสผ่าน/JWT) — ยังไม่ได้ตรวจ (required gate), Phase 2/Module C (รับไฟล์จากภายนอก) — ยังไม่ได้ตรวจ (required gate), Phase 6/Module G (ส่งข้อมูลออกไป Gemini + API key) — QA แล้ว (FULL round 2026-08-15, ✅ 14/14; TARGETED round 2026-08-16 re-confirmed the MANAGER-only permission fix + Gemini call path) แต่ยังไม่ได้ตรวจโดย `security` (required gate). Phase 3/Module D ไม่ได้ถูกระบุเป็น `⚠️ Sensitive` ใน `design.md` แต่มี endpoint แก้ไขข้อมูลตัวเลขทางธุรกิจ ถ้า `security` มีเวลาว่างอาจดูผ่าน ๆ ได้เช่นกัน (ไม่ใช่ required gate). Phase 4/Module E และ Phase 5/Module F ไม่ต้องการ security pass (ไม่มี input จากภายนอก ไม่ถูกระบุเป็น sensitive)

**✅ `project-manager` เสร็จแล้ว 2026-08-19 (amend `plan.md` ตาม `design.md` รอบที่ 3)**: เพิ่ม **Phase 17: Module Q 🔒 Security gate** (สิทธิ์การมองเห็นข้อมูล 3 ชั้น — dependency B/E/Phase 12 (M), ทำขนานกับ Phase 13/14/15 ได้), **Phase 18: Module F2** (Leaderboard 2 ชั้น — แทนที่ Leaderboard ของ Phase 5 ทั้งหมด, dependency Phase 12+13+17 ครบทั้งสาม **ทำ incremental ไม่ได้**), **Phase 19: Module R 🔒 Security gate** (แทนที่/ลบข้อมูลตามงวด — dependency Phase 2+8 เท่านั้น **ไม่ขึ้นกับ Phase 12 เลย ทำขนานกันได้ตั้งแต่วันแรก** ควรเสร็จก่อนผู้ใช้นำเข้าไฟล์งวดเดิมซ้ำครั้งแรก) · **แก้ task ของ Phase 12 (Module M) เดิม** (ยังไม่มีงานเริ่ม ไม่มี `[x]`) เพราะ migration task เดิมอ้าง `enum TerritoryRole` ที่ถูกยกเลิกแล้ว — แก้เป็น `isSupervisor Boolean` และเพิ่ม task สำหรับ `TerritoryGroup`/`TerritoryGroupMember` (เป้าคู่เขต) · **แก้ task ของ Phase 13 (Module N) เดิม** เพิ่มงาน whitelist สิทธิ์ระดับเขตตาม Data Visibility Rules ข้อ 6 ที่ Phase 18 ต้องเรียกใช้ร่วม (ห้ามประกาศซ้ำ) · **⚠️ เพิ่ม Sequencing Notes ห้าม `devops` deploy Phase 5 ก่อน Phase 17 (Q) เสร็จ** และรายการ re-verify ที่ `qa-engineer` ต้องทำหลัง Q/R/F2 ลง: Phase 1 ไม่ต้อง re-verify schema (ไม่เปลี่ยน), Phase 4/6/7 re-verify **สิทธิ์** ไม่ใช่สูตร (รวมกรณี SALESPERSON ไม่ผูกบัญชีที่เคย `200` ต้องเปลี่ยนเป็น `403` — ตั้งใจเปลี่ยน ไม่ใช่ regression), Phase 5 ต้องมี FULL round ใหม่เพราะถูกแทนที่ทั้งหน้าโดย Phase 18, Phase 6 สิทธิ์เปลี่ยนรอบที่ 2 (MANAGER + supervisor), Phase 2 re-verify หลัง Phase 19 แก้ `import.service.ts` · **ไม่มีคำถามที่ต้องส่งกลับ** — `design.md` ปิด blocker ครบแล้วก่อนรอบนี้เริ่ม จึงจัดเฟสได้ตรงตามที่ระบุไว้ทุกจุด · **พร้อมส่ง `backend-engineer`/`frontend-engineer`** ทำงานคิวเดิมที่ยังค้าง (Phase 8's `[frontend]` tasks) ต่อก่อน — Phase 17-19 ยังไม่ถึงคิวจนกว่า Phase 9-16 ตามลำดับ dependency จะถึง

**✅ `project-manager` amend 2026-08-22 (Phase 12 only)**: ปรับงาน `TerritoryGroupMember` ให้เก็บช่วงสมาชิกเต็มเดือน (`effectiveFrom`/`effectiveTo`), ใช้ PostgreSQL exclusion constraint กันช่วงทับซ้อน, API อ่านสมาชิกตามงวดและใช้ `PATCH effectiveTo` เพื่อสิ้นสุดแทน delete, พร้อม validation กลางตอบ `409` เมื่อช่วงสมาชิกทับกันหรือชนเป้ารายเขตรายเดือน · ไม่มี `[x]` ถูกเปลี่ยน และ Phase 12 พร้อมส่ง engineer แล้ว

**✅ `backend-engineer` เสร็จ Phase 11 `[backend]` round, 2026-08-19 (Module O ส่วนที่ 1 — Product Master)**: ก่อนเริ่มตรวจ dependency ด้วยตัวเองตาม `plan.md` Sequencing Notes แล้วยืนยันว่า Phase 11 ไม่ขึ้นกับโครงสร้างเขตจริง (ต่างจาก Phase 9 ที่รอบก่อนหยุดไว้ถูกต้อง) จึงเริ่มงานได้ทันที ทำครบทุก `[backend]` task ใน `plan.md` บรรทัด 202-207: migration `20260819000000_phase11_product_master` (ตรงกับ `design.md` Data Model เป๊ะ — `Product.code`/`displayName`/`source: ProductSource`/`isActive` + โมเดล `ProductAlias` + back-relation บน `User`, ไม่มีการ backfill ข้อมูลเพราะทุกคอลัมน์มี default ตามที่ระบุ), seed script `scripts/seedProductAliases.ts` (สร้าง `ProductAlias` 1 แถวต่อ `Product` เดิมทุกแถวด้วย `latinCore(name)` reuse จาก `nameNormalizer.util.ts` ของ Module J ไม่เขียน normalizer ใหม่ — รันจริงกับ DB จริงแล้ว: สร้างครบ 128/128 แถว ไม่มี normalizedKey ชนกันเลย), `productResolution.service.ts` ใหม่ (resolve ผ่าน alias ก่อนเสมอ ไม่พบจึง fallback หา Product เดิมแล้วสร้าง alias ให้ ไม่พบเลยจึงสร้าง `Product`(`source=SALES_HISTORY`) + `ProductAlias`(`source=AUTO`) ใหม่ — ไม่มีคิวถามผู้จัดการตามที่ `design.md` สั่งห้าม implement ล่วงหน้า), แก้ `import.service.ts` ให้เรียกใช้ตัวนี้แทน lookup เดิม, `GET /products` + `PATCH /products/:id` (MANAGER เท่านั้น, `409` เมื่อ `code` ชนกับสินค้าอื่น) + Zod schema (`updateProductSchema` บังคับอย่างน้อย 1 ฟิลด์). Live-tested กับ dev server + DB จริง (ไม่ใช่แค่ typecheck): นำเข้าไฟล์ Excel จริงผ่าน `POST /import` 2 แถว — แถวที่ใช้ชื่อสินค้าเดิม `"  pc-5  "` (ตัวพิมพ์/ช่องว่างต่างจากของเดิม) resolve เข้า `Product` เดิมถูกต้อง ไม่สร้างซ้ำ, แถวที่ใช้ชื่อใหม่จริงสร้าง `Product`+`ProductAlias(source=AUTO)` ใหม่ถูกต้อง (ลบข้อมูลทดสอบออกหมดแล้วหลังตรวจสอบ); `GET /products` คืนค่าครบตามที่ระบุ (`code`/`displayName`/`name`/`productType`/`source`/`isActive`); `PATCH /products/:id` ทดสอบทั้งเคสสำเร็จ, `409` ชน unique code, `400` ส่ง body ว่าง, `404` id ไม่มีจริง — ผ่านหมด. `[frontend]` task 1 ข้อของ Phase 11 (หน้าทะเบียนสินค้า) ยังไม่ทำ เป็นงานของ `frontend-engineer`. **หมายเหตุนอกเหนือ scope ของ Phase 11**: พบ lint error 5 จุดที่ค้างอยู่ในสคริปต์เก่า 2 ไฟล์ (`backend/scripts/_adhoc_territoryReports.ts`, `backend/scripts/fixBogusSharedCreditSalesperson.ts` — ไม่เกี่ยวกับ Phase 11) ซึ่งทำให้ whole-project `npm run lint` แดงและ block hook `require-green-before-stop` แก้ให้แล้ว (ลบตัวแปรที่ไม่ได้ใช้ 4 ตัว, เปลี่ยน `any` เป็น type ที่ถูกต้อง 1 จุด) ไม่กระทบ logic ของสคริปต์เหล่านั้น. `typecheck`/`lint` ทั้งโปรเจกต์ backend เขียวแล้ว. พร้อมให้ `qa-engineer` ตรวจ Phase 11 ต่อ (FULL round แรกของเฟสนี้).

**⚠️ `qa-engineer` ตรวจ Phase 11 เสร็จแล้ว 2026-08-19 (FULL round แรกของเฟส) — 5/7 ✅, 1/7 ❌**: migration/seed script/`GET /products`/`PATCH /products/:id`/Zod schema ตรงกับ `design.md` เป๊ะและ live-tested ผ่านหมดกับ dev server + DB จริง (128/128 `ProductAlias`, 0 ชนกัน, `PATCH` ครบทั้ง success/`409`/`400`/`404`, สิทธิ์ MANAGER-only ยืนยันด้วย token จริง SALESPERSON → `403`) · **พบบั๊กจริงใน task แก้ `import.service.ts`**: `resolveProductViaAlias` เช็ค `ProductAlias` แบบ global โดยไม่กรองด้วย `productTypeId` เลย ทำให้ถ้าชื่อสินค้าที่มี alias อยู่แล้วถูกนำเข้าอีกครั้งพร้อมค่าคอลัมน์ `Product type` ที่ต่างไปจากเดิม ระบบจะ resolve เข้า `Product` ตัวเดิม (คนละ type) แล้วเซ็ต `SalesLine.productTypeId` ตามคอลัมน์ไฟล์ดิบแทนที่จะตรงกับ `product.productTypeId` จริง — ขัดกับ invariant ที่ระบุไว้ตรง ๆ ใน `design.md` บรรทัด 429 ("ตอน import ต้องเซ็ตให้ตรงกับ `product.productTypeId` เสมอ") · reproduce จริงด้วย `POST /import` แล้วตรวจ DB ยืนยัน mismatch จริง จากนั้นลบข้อมูลทดสอบออกหมด · ตรวจข้อมูลจริง 128 `Product` ปัจจุบันแล้วไม่มีชื่อใดชนข้าม type เลย จึงยังไม่กระทบตัวเลข Phase 3/4 ที่ verified แล้ว **ในวันนี้** แต่เป็นบั๊กจริงที่ reproduce ได้ ไม่ใช่สมมติฐาน — ส่งกลับ `backend-engineer` เป็น implementation bug (ไม่ใช่ช่องว่างของ schema/design เพราะกฎที่ต้องทำตามเขียนไว้ชัดแล้ว) · mark `[x]` เฉพาะ 5 task backend ที่ verified จริงใน `plan.md` บรรทัด 202/203/205/206/207 — เว้นบรรทัด 204 (import service) และ 208 (`[frontend]`, ยังไม่ทำ) ไว้ตามเดิม · whole-project `typecheck`/`lint`/`build` เขียวทั้ง backend/frontend (ไม่มี regression) · ไม่มี automated test suite ในโปรเจกต์นี้ — รอบนี้คือ code inspection + `typecheck`/`lint`/`build` + live test จริงกับ dev server/DB · **ไม่ eligible ส่งต่อ `devops`** จนกว่าบั๊กนี้จะถูกแก้และตรวจซ้ำ (TARGETED พอ เพราะรอบนี้เป็น FULL แล้วและ blast radius แคบ) · รายละเอียดเต็มอยู่ใน `review.md`.
