# ระบบประเมินและสนับสนุนพนักงานขาย (Sales Evaluation) — Verification & Review

## Open Issues — all phases

| Issue | Phase | Routes to | Blocking |
|---|---|---|---|
| `skippedRows` on `ImportBatch` is never populated by any code path (blank rows silently `continue`d without incrementing it) — the field's definition vs `errorRows` needs to be clarified in `design.md` | Phase 2 (Module C) → `review/phase-2.md` | `system-analyst` (or the user decides the definition directly) | No — cosmetic/informational, not a data-correctness issue |
| Security review outstanding — Module C accepts an external `.xlsx` upload (20MB limit + in-memory `exceljs` parsing, unresolved-`sharedFormula`-cell handling, `.xlsx`-extension+MIME filtering) | Phase 2 (Module C) → `review/phase-2.md` | `security` | **Yes** — `design.md` explicitly flags Module C `⚠️ Sensitive`; required gate before deploy |
| Security review outstanding — Module B (auth, passwords, JWT, personal data) | Phase 1 (Module B) → `review/phase-1.md` | `security` | **Yes** — `design.md` explicitly flags Module B `⚠️ Sensitive`; required gate before deploy |
| Security review outstanding — Module G (AI Coaching): data leaves the org to Gemini, `GEMINI_API_KEY` handling, `CoachingInsight.kpiSnapshot` payload audit | Phase 6 (Module G) → `review/phase-5-6.md` | `security` | **Yes** — `design.md` explicitly flags Module G `⚠️ Sensitive`; required gate before deploy. Functional QA confirms anonymization and permission enforcement hold up, but that is not a substitute for the security pass `design.md` requires |
| Phase 2 and Phase 3's most recent verification rounds were **TARGETED**, not FULL. Per this file's own gate ("Deploy eligibility requires a FULL round"), `devops` cannot treat either phase as deploy-eligible on a TARGETED round alone, regardless of how clean the targeted fix-checks came back | Phase 2/3 (Modules C/D) → `review/phase-2.md` / `review/phase-3.md` | `qa-engineer` (run one FULL round on each before deploy) | **Yes** — for `devops` eligibility specifically; does not call the phases' underlying correctness into question |
| **Phase 6's most recent verification round is now TARGETED** (this round, 2026-08-16 — see below), superseding its previous FULL round (Phase 5/6, 2026-08-15). Per this file's own gate, `devops` cannot treat Phase 6 as deploy-eligible until one more FULL round runs, even though this TARGETED round confirmed the SALESPERSON→403 fix is correct and live-tested and closed two previously-open items outright | Phase 6 (Module G) → this round, below | `qa-engineer` (run one FULL round before deploy) | **Yes** — for `devops` eligibility specifically; does not call the fix's correctness into question |
| `qa.temp.user@example.com` leftover test account still present in the dev DB (`isActive: false`) | Phase 1 (Module B) → `review/phase-1.md` | — (cleanup only, no specific owner) | No |
| Security review — Module D (Targets) isn't in `design.md`'s explicit `⚠️ Sensitive` list, but its write paths carry financial figures (`revenueTarget`); non-blocking completeness suggestion, not a required gate | Phase 3 (Module D) → `review/phase-3.md` | `security` | No — suggestion only |
| Responsive layout at actual mobile viewport widths has never been visually verified across **any** phase that has UI — Phase 5/6 (dashboard, leaderboard, coaching panel) and Phase 7 (individual report, team-overview report) all show sound mobile-first Tailwind patterns on inspection (`grid-cols-1`/`lg:grid-cols-2`, `flex-wrap`, `p-4 sm:p-6`, `max-w-5xl`/`max-w-3xl`), but no round has had browser/viewport automation tooling available to actually render and check | Phase 5 (Module F), carried through Phase 7 (Module H) → `review/phase-5-6.md`, `review/phase-7.md` | `qa-engineer` (re-run with browser/viewport tooling in a future round) | No — non-blocking; the one `plan.md` checkbox for this (Phase 5's) is left unchecked pending an actual viewport check; Phase 7 has no dedicated checkbox for it but carries the identical gap |
| `CoachingInsight.kpiSnapshot.sentToGemini` is only set `true` when `callGemini()` returns successfully — if a real API key is present and the network request is sent but Gemini responds with a late error (e.g. a non-2xx status *after* receiving the request body), `sentToGemini` would still read `false` even though the payload did leave the process over the network. Worth `security`'s attention specifically since Module G's whole audit point is "data leaving the org" — not confirmed as an actual problem even now that a real `GEMINI_API_KEY` and a real successful call exist (this round's live call returned `200` on the first try, so the late-failure branch still hasn't been exercised) | Phase 6 (Module G) → `review/phase-5-6.md` | `security` | No — flagged for the security pass to look at, not a confirmed defect |
| Phase 6's `isStale` "set to `true` by a fresh import touching that period" path was verified by **code inspection only** in the Phase 5/6 round (`import.service.ts:448-479`, unchanged since Phase 2) — the live end-to-end chain (real `POST /import` → `isStale` flips true → `generate` clears it) could not be driven that round because the file-upload action was blocked by that session's auto-mode classifier. The "cleared by generate" half **was** live-verified that round. Not touched by this round's fix (`import.service.ts` is untouched, outside this round's scope) | Phase 6 (Module G) → `review/phase-5-6.md` | `qa-engineer` (re-attempt the full live chain in a future round once file-upload testing is available) | No — code matches the same logic Phase 2's round already inspected; not a suspected defect |

**Resolved this round (2026-08-16, TARGETED round on Phase 6)**: (1) the **SALESPERSON-can-generate-own-insight permission rule** open item — `business-analyst` closed the underlying business decision (MANAGER-only, salespeople read-only) on 2026-08-16, and this round live-confirmed the corresponding backend/frontend fix (`coachingInsight.controller.ts`, `dashboard/page.tsx`, `reports/individual/page.tsx`, `CoachingInsightPanel.tsx`) enforces it correctly — no longer open. (2) The three leftover `qa-temp-*.ts` scripts in `backend/src/` — confirmed deleted (via `Glob`, zero matches) before this round started; no longer open. (3) The Gemini-success path (real API call, not the fallback) — `GEMINI_API_KEY` is no longer empty in `backend/.env`; this round drove a real `generate` call as MANAGER and got back `status: "SUCCESS"`, `provider: "gemini"`, `model: "gemini-flash-lite-latest"` with real generated Thai coaching text — the previously untestable path is now live-confirmed working, including with the new default model name. No longer open. (4) **"Should the backend refuse to deactivate the last remaining active MANAGER account?"** (previously this file's Phase 1 open item) — `business-analyst` closed this on 2026-08-16 as an Accepted Risk, not a code change: the user explicitly accepts not blocking it (`requirement.md`'s `### ความเสี่ยงที่ผู้ใช้ยอมรับแล้ว (Accepted Risks)` — "ไม่บล็อกการปิดใช้งานบัญชีผู้จัดการคนสุดท้ายที่ยัง active อยู่"). No code was expected or required for this closure; verified only that `requirement.md` actually records it. No longer open.

**Resolved in a prior round** (kept for reference, not re-litigated): Phase 7's SALESPERSON-role live access to `/reports/*` — closed live in the Phase 7 FULL round (`review/phase-7.md`); a leftover orphaned `CoachingInsight` test row (Chidchanok, `YEAR`/2026/0) from a prior interrupted QA attempt on that same phase was found and deleted that round.

Phase 4 introduced no new security-sensitive surface (it computes over already-imported/validated data and gates its one write surface — `PUT/PATCH /settings/*` — behind the existing MANAGER role check; it accepts no untrusted external input and `design.md` doesn't flag Module E `⚠️ Sensitive`), so no new security gate was added for it. Phase 5 (Module F) is the same — read-only, no new write surface, not flagged `⚠️ Sensitive` in `design.md`. **Phase 7 (Module H) is the same again** — read-only reports composed entirely from already-computed/already-validated numbers, no new write surface, `design.md` doesn't flag Module H `⚠️ Sensitive` — none of these three need a `security` pass.

## Verification Summary (current round)

**TARGETED round — Phase 6 (AI Coaching Insights, Module G).** Re-checks one specific, already-implemented fix — `generateInsight` now rejects any non-MANAGER role, closing the item this file previously tracked as "SALESPERSON-can-generate-own-insight was an unwritten judgment call, not a confirmed business decision" — plus its blast radius. This qualifies for TARGETED, not FULL, because: the prior round for this phase (Phase 5/6, 2026-08-15) was FULL and left a file manifest to compare against; the work since then is confined to the named fix (`coachingInsight.controller.ts`'s role check, the matching frontend gate in two pages and one shared component's copy, plus an unrelated one-line Gemini model-name change the brief asked to be spot-checked); and this phase is not about to be handed to `security` or `devops` as a direct result of this round (it already needs a required `security` pass regardless, tracked in Open Issues, and per this file's own gate a TARGETED round cannot make it `devops`-eligible anyway — see the new Open Issues row above).

**What this round does not cover**: it does not re-verify the other 13 of Phase 6's 14 already-✅-Verified tasks from scratch (the anonymization logic, the `aiEnabled`/`aiAnonymize` settings endpoints, the `isStale`-set-by-import path, the Zod validator) beyond confirming the files that implement them are byte-identical to the Phase 5/6 manifest (i.e., untouched) — those stand on the Phase 5/6 FULL round's verification, not this one. It does not cover Phase 5's or Phase 7's own tasks beyond the specific shared-component/shared-code-watchlist check below. Behaviour that changed without changing types, in files this fix didn't touch and the watchlist doesn't cover, is out of scope for this round by definition.

Read `plan.md` (Phase 6 block, Plan Summary, Sequencing Notes), `design.md` (Module G contract, Data Model's `CoachingInsight` entry), `requirement.md` (the 2026-08-16 business decision and its `### ความเสี่ยงที่ผู้ใช้ยอมรับแล้ว` section), `review.md`'s Open Issues (rows 12 and 16 as pointed to by the brief) and the Phase 5/6 archive (`review/phase-5-6.md`) for its file manifest, and `_docs/status.md` before starting.

**The fix itself, read in full**: `coachingInsight.controller.ts#generateInsight` now checks `req.user!.role !== "MANAGER"` and returns `403` with `"เฉพาะผู้จัดการเท่านั้นที่สั่งสร้างสรุป AI ได้"` before calling the service — the previous SALESPERSON-own-linked branch is gone entirely, and `coachingInsight.service.ts#generateInsight` (re-read in full) confirms it never had its own role logic to begin with, so the permission boundary has always lived in exactly one place. `coachingInsight.routes.ts`'s comment was updated to match (no route-level change — the route still applies `authenticate, requirePasswordChanged` only, gating is inside the controller as before). `dashboard/page.tsx` and `reports/individual/page.tsx` both now compute `canGenerateInsight = currentUser?.role === "MANAGER"` (the previous own-salesperson-can-generate branch removed from both), and `CoachingInsightPanel.tsx`'s disabled-state copy now reads "เฉพาะผู้จัดการเท่านั้นที่สั่งสร้างสรุปนี้ได้" — read in full, confirmed this is the only text change in that file (byte diff 6142→6035, 0 line-count change).

**Blast radius, `Grep`-verified before inspecting further**: `generateInsight` (the service function) has exactly one caller in the whole backend (`coachingInsight.controller.ts`) and `CoachingInsightPanel` has exactly two consumers in the whole frontend (`dashboard/page.tsx`, `reports/individual/page.tsx` — `reports/team-overview/page.tsx` does not render it at all). Both consumers were read in full and both correctly derive `canGenerateInsight` the same new way and pass it through unchanged prop-wise (`canGenerate={canGenerateInsight}`). No other route, controller, or component references `generateInsight`, `canGenerateInsight`, or the old copy string.

**Shared-code watchlist, checked regardless of whether the fix touched it**: `backend/src/middleware/authenticate.ts` (1720 bytes/55 lines) and `backend/src/lib/prisma.ts` (90 bytes/3 lines) are unchanged from the Phase 7 manifest's copies. `frontend/lib/api.ts` (14248 bytes/429 lines) is byte-identical to the Phase 7 manifest's copy — no drift in the shared API client. `frontend/app/(protected)/layout.tsx` (the shared protected-route layout, wiring `AuthGuard` + `NavBar`) was read in full — no coaching-insight-specific or role-specific logic duplicated there. All four are clean.

**Full schema/Data Model contract check for Phase 6 (not reduced for TARGETED, per this file's own rule)**: `backend/prisma/schema.prisma`'s `CoachingInsight` model (lines 288-308) was read field-by-field against `design.md`'s Data Model `CoachingInsight` block (`id`, `salespersonId`+relation, `periodType`, `year`, `periodNumber`, `kpiSnapshot: Json`, `contentTh: String?`, `status: InsightStatus @default(PENDING)`, `provider: String?`, `model: String?`, `errorMessage: String?`, `isStale: Boolean @default(false)`, `generatedById: String?`+relation, `generatedAt: DateTime @default(now())`, the `@@unique([salespersonId, periodType, year, periodNumber])` and `@@index([year, periodType])`) — an exact match, and the whole file is byte-identical to the copy already checked in the Phase 5/6 and Phase 7 rounds (8707 bytes/308 lines). This fix is server-side authorization and frontend gating only, as the brief stated — confirmed with zero schema drift.

**Whole-project `typecheck`/`lint`/`build`, not just the changed area**: `backend` — `npm run typecheck`, `npm run lint`, `npm run build` all pass clean (no errors, no warnings surfaced). `frontend` — `npm run typecheck`, `npm run lint` both pass clean; `npm run build` succeeds (`✓ Compiled successfully`, `✓ Generating static pages using 7 workers (20/20)`) and the route list confirms `/dashboard`, `/reports/individual`, `/reports/team-overview`, and every other pre-existing route still compile — no regressions anywhere else in the app. Neither `package.json` defines a `test` script — **this project has no automated tests; this round's live-testing below is code inspection plus real HTTP calls against the real dev DB, not an automated suite.**

**Live permission-boundary test, all three cases the brief asked for, against the running dev server and the real dev DB** (MANAGER JWT signed for `manager1@example.com`; SALESPERSON JWT signed for `sp-test-phase6@example.com`, linked via `Salesperson.userId` to Miss Chidchanok Seethong — the same seeded accounts prior rounds used):
- **MANAGER generates for a salesperson (Chidchanok, Q1 2026)** → `POST /coaching-insights/:salespersonId/generate` → `201`.
- **SALESPERSON — even for their own linked record (Chidchanok is `sp-test-phase6@example.com`'s own salesperson)** → same call → `403`, body `{"error":"เฉพาะผู้จัดการเท่านั้นที่สั่งสร้างสรุป AI ได้"}` — this is the specific case the fix targets (previously this exact combination returned `201`), and it now correctly rejects even the salesperson's *own* record, matching the closed business decision (MANAGER-only, no exceptions).
- **Unauthenticated** (no `Authorization` header) → same call → `401`, `{"error":"Missing or invalid Authorization header"}` — confirms `authenticate` middleware is still active and this isn't a case of the 403 masking a missing 401 check.
- **Read access unaffected** — the same SALESPERSON token against `GET /coaching-insights/:salespersonId` still returns `200` (reading stays open to everyone, only generation is gated), confirming the fix didn't over-restrict.

**Bonus: the Gemini-success path is now live-testable and was live-tested, closing a previously-open item.** `backend/.env` now has a real `GEMINI_API_KEY` (previously empty in every round). The MANAGER-generate call above was driven with `aiEnabled: true` (the seeded default) and returned `status: "SUCCESS"`, `provider: "gemini"`, `model: "gemini-flash-lite-latest"`, `sentToGemini: true`, and a real generated Thai coaching sentence referencing the salesperson's actual numbers (`"...ยอดขายรวมที่ทำได้ 773,540 บาท จากเป้าหมาย 1,800,000 บาท คิดเป็นความสำเร็จร้อยละ 42.97..."`) — this is the first round where the real Gemini call path (not just the fallback) has actually been exercised, and it confirms the `gemini-2.0-flash` → `gemini-flash-lite-latest` model-name change **does not break the Gemini call path** — the opposite, it demonstrably works end-to-end with the new default. Anonymization held during this live call too (`salesperson.name: "พนักงานขาย A"` in the stored `kpiSnapshot.payload`, consistent with the Phase 5/6 round's finding).

**Cleanup**: the one `CoachingInsight` row this round generated for the live test (Chidchanok, `QUARTER`/2026/1) was deleted after use — confirmed by a follow-up query that only the one pre-existing engineer self-test row (`sp-test-phase6@example.com`'s self-test, Chidchanok/`MONTH`/2026/6, already known from prior rounds) remains. The three `qa-temp-*.ts` scripts flagged as an open cleanup item after the Phase 7 round are now confirmed gone (`Glob` for `backend/src/qa-temp-*.ts` returns zero matches) — closed as an Open Issue, see above. This round's own temporary signing/query scripts were written under the OS scratchpad convention (never inside the repo) and deleted at the end of the round. The backend dev server (:4000) was stopped at the end; no frontend dev server was needed since the frontend checks were `typecheck`/`lint`/`build` only.

## Verified File Manifest — Phase 6 (TARGETED round)

Files inspected this TARGETED round, with size and line count. This supplements rather than replaces the Phase 5/6 FULL round's manifest (archived in `review/phase-5-6.md`) — the files below are the fixed files, their blast radius, and the shared-code watchlist; every other Phase 6 file stands on the Phase 5/6 manifest unless listed here as unchanged-and-reconfirmed.

| File | Bytes | Lines | Round |
|---|---:|---:|---|
| `backend/src/controllers/coachingInsight.controller.ts` | 2043 | 51 | Phase 6 TARGETED (fixed — MANAGER-only role check) |
| `backend/src/routes/coachingInsight.routes.ts` | 1167 | 30 | Phase 6 TARGETED (comment-only change, no route logic change) |
| `backend/src/services/coachingInsight.service.ts` | 12423 | 292 | Phase 6 TARGETED (re-read in full; confirmed no role logic present at all — permission boundary lives solely in the controller, unaffected by this fix; unchanged since Phase 7 manifest) |
| `backend/src/services/gemini.service.ts` | 2109 | 57 | Phase 6 TARGETED (fixed — `DEFAULT_GEMINI_MODEL` changed to `gemini-flash-lite-latest`; live-tested working, see Verification Summary) |
| `frontend/app/(protected)/dashboard/page.tsx` | 6339 | 152 | Phase 6 TARGETED (fixed — `canGenerateInsight` simplified to MANAGER-only) |
| `frontend/app/(protected)/reports/individual/page.tsx` | 8985 | 194 | Phase 6/7 blast radius TARGETED (fixed — same `canGenerateInsight` change, Phase 7 component) |
| `frontend/components/coaching/CoachingInsightPanel.tsx` | 6035 | 143 | Phase 6 TARGETED (fixed — disabled-state copy updated; rest of file byte-diff-confirmed unchanged) |
| `backend/src/middleware/authenticate.ts` | 1720 | 55 | Phase 6 TARGETED (watchlist — confirmed unchanged from Phase 7 manifest) |
| `backend/src/lib/prisma.ts` | 90 | 3 | Phase 6 TARGETED (watchlist — confirmed unchanged) |
| `frontend/lib/api.ts` | 14248 | 429 | Phase 6 TARGETED (watchlist — byte-identical to Phase 7 manifest, no drift) |
| `frontend/app/(protected)/layout.tsx` | 374 | 13 | Phase 6 TARGETED (watchlist — shared protected layout, read in full, no duplicated role logic) |
| `backend/prisma/schema.prisma` | 8707 | 308 | Phase 6 TARGETED (full contract check, not reduced — `CoachingInsight` model re-verified field-by-field against `design.md`, byte-identical to Phase 5/6/7 manifests, zero drift) |

## Per-Task Results — Phase 6 (TARGETED round, this round)

Only the fixed task and the tasks in its blast radius are re-verified to full standard this round; the remaining Phase 6 tasks stand on the Phase 5/6 FULL round (`review/phase-5-6.md`) and are not re-listed here.

- [x] Verified — `[backend]` `POST /coaching-insights/:salespersonId/generate` (MANAGER only) — `coachingInsight.controller.ts#generateInsight`. Live-tested all 3 cases the brief asked for: MANAGER → `201`; SALESPERSON, including for their own linked record → `403` with the correct Thai message (this is the specific behavior that changed — previously `201`); unauthenticated → `401`. `GET` (read) confirmed still open to SALESPERSON (`200`), unaffected by the fix.
- [x] Verified — `[backend]` `POST /coaching-insights/:salespersonId/generate` route wiring — `coachingInsight.routes.ts`. Comment-only diff, route still applies `authenticate, requirePasswordChanged` with gating inside the controller, matching the updated comment.
- [x] Verified — `[backend]` Gemini API call path with the new default model — `gemini.service.ts#callGemini`. Live-tested with a real `GEMINI_API_KEY` (now present in `backend/.env`, previously empty in every prior round): real call to `gemini-flash-lite-latest` returned `status: "SUCCESS"`, `provider: "gemini"`, real generated Thai text — confirms the model-name change does not break the call path, and additionally closes the previously-open "Gemini-success path never live-tested" item.
- [x] Verified — `[frontend]` Dashboard coaching panel gating — `dashboard/page.tsx`. `canGenerateInsight` now MANAGER-only, matches the backend exactly.
- [x] Verified — `[frontend]` Individual report coaching panel gating — `reports/individual/page.tsx` (Phase 7 file, in this fix's blast radius). Same `canGenerateInsight` change, consistent with the dashboard.
- [x] Verified — `[frontend]` Coaching panel disabled-state copy — `CoachingInsightPanel.tsx`. Renders "เฉพาะผู้จัดการเท่านั้นที่สั่งสร้างสรุปนี้ได้" when `canGenerate` is `false`; rest of the component's rendering logic (status badge, `isStale` badge, FAILED-status fallback notice, drill-down button) confirmed unchanged by reading the file in full.
- [x] Verified — `[backend]` `qa-temp-*.ts` cleanup — confirmed via `Glob` that `backend/src/qa-temp-inspect.ts`, `qa-temp-sign.ts`, `qa-temp-parse-xlsx.ts` are gone and nothing in the app (`Grep` for `qa-temp` across `backend/src`) references them.

## Design/requirement contract checks — Phase 6 (this round)

- **`requirement.md`'s 2026-08-16 business decision** ("สั่งสร้าง AI coaching insight ได้เฉพาะ MANAGER พนักงานขายอ่านได้อย่างเดียว") — confirmed the implementation matches exactly, live-tested in both directions (MANAGER succeeds, SALESPERSON — including for their own record — is rejected).
- **Data Model**: `CoachingInsight` re-verified field-by-field against `design.md`'s Data Model — exact match, zero drift (see manifest). This fix touches no schema.
- **No new security-sensitive surface introduced by this fix** — the change narrows an existing write surface (`POST .../generate`) rather than adding one; Module G's required `security` gate (data leaving the org to Gemini, API key handling) remains open regardless, unaffected by this round either way.

## Issues Found — Phase 6 (this round)

No implementation bugs found. The fix does exactly what the brief and the closed business decision describe, live-tested and confirmed in all three permission cases plus the read-path regression check. No new issues surfaced in the blast radius or the shared-code watchlist.

## Review Outcome — Phase 6 (TARGETED round)

**Phase 6 (AI Coaching Insights, Module G) — TARGETED round. The named fix (MANAGER-only `generateInsight`) is confirmed correct and live-tested across all three permission cases; its blast radius (both frontend pages, the shared panel component, the unrelated Gemini model-name change) is confirmed correct; the shared-code watchlist and the full schema contract show zero drift; whole-project `typecheck`/`lint`/`build` are clean.**

This closes two previously-open Open Issues outright (the business-decision-confirmation item, and the Gemini-success-path-never-tested item) and confirms the three leftover `qa-temp-*.ts` scripts are gone. It also, per this file's own gate, **resets Phase 6's deploy eligibility** — the phase's most recent round is now TARGETED rather than FULL, so `devops` cannot treat it as deploy-eligible until one more FULL round runs, even though nothing in this round calls the phase's correctness into question. See the new Open Issues row above. Phase 6 still needs its required `security` pass (Module G is `⚠️ Sensitive` per `design.md`) before deploy, independent of this round's mode.

**Pending the user's accept decision.** This was a TARGETED round, not FULL, so the all-✅ autonomous-mode auto-accept exception in this file's own rules does not apply here regardless of session mode — the user should explicitly confirm accept-as-is, request a follow-up FULL round now (which would close the deploy-eligibility gap immediately), or decide something else, before this round is treated as closed.

## Archived rounds

- Phase 1 (Auth & User Management, Module B) — accepted, complete (16/16), historical (predates FULL/TARGETED mode labeling) → `review/phase-1.md`
- Phase 2 (Excel Import & Master Data, Module C) — accepted, complete (19/19), most recent round TARGETED → `review/phase-2.md`
- Phase 3 (Targets, Module D) — accepted, complete (10/10), most recent round TARGETED → `review/phase-3.md`
- Phase 4 (KPI & Scoring Engine, Module E) — accepted, complete (18/18), FULL round, zero defects → `review/phase-4.md`
- Phase 5 (Dashboards & Leaderboard, Module F) — FULL round, 10/11 (1 non-blocking partial, responsive), pending user decision → `review/phase-5-6.md`
- Phase 6 (AI Coaching Insights, Module G) — FULL round (2026-08-15), 14/14, zero defects → `review/phase-5-6.md`; superseded by a TARGETED round (2026-08-16, see current round above) that fixed and confirmed the MANAGER-only generate permission — pending user decision, required `security` pass, and one more FULL round before `devops` eligibility
- Phase 7 (Coaching Reports & Export, Module H) — FULL round, 8/8, zero defects, pending user decision → `review/phase-7.md`

## Change Log

- 2026-08-15 — First verification round for Phase 1 (Module B). 15/16 ✅ Verified. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — Second verification round for Phase 1 (Module B). 16/16 ✅ Verified, Phase 1 complete. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — First verification round for Phase 2 (Module C). 18/19 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-2.md`.
- 2026-08-15 — First verification round for Phase 3 (Module D). 9/10 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-3.md`.
- 2026-08-15 — TARGETED re-verification of two previously-flagged fixes: Phase 2's `ImportBatch` failure-path persistence and Phase 3's missing `GET /product-types` (both fixed, confirmed live). 19/19 Phase 2 and 10/10 Phase 3 tasks now ✅ Verified. Full entry archived to `review/phase-2.md`/`review/phase-3.md`.
- 2026-08-15 — FULL verification round for Phase 4 (Module E, KPI & Scoring Engine). 18/18 tasks ✅ Verified, zero defects, all `plan.md` boxes checked. Full entry archived to `review/phase-4.md`.
- 2026-08-15 — FULL verification round for Phase 5 (Module F, Dashboards & Leaderboard) and Phase 6 (Module G, AI Coaching Insights), run together since both build directly on Phase 4 and were implemented in the same session. Phase 5: 10/11 ✅ Verified, 1 ⚠️ Partial (responsive layout not visually confirmed at mobile viewport — no browser tool available; code pattern is sound). Phase 6: 14/14 ✅ Verified, zero implementation defects. Full entry archived to `review/phase-5-6.md` (superseded by this round).
- 2026-08-15 — FULL verification round for Phase 7 (Module H, Coaching Reports & Export) — the module's last implementation phase. 8/8 tasks ✅ Verified, zero implementation defects, all `plan.md` boxes checked. Checked first whether a previously-interrupted QA attempt on this exact phase had left any write-up or checkboxes (it hadn't — only two leftover artifacts: 3 reusable `qa-temp-*.ts` scripts and one orphaned test `CoachingInsight` row, the latter deleted this round). Independently recomputed the individual report's numbers for Chidchanok Seethong Q1 2026 against Phase 4/6's already-verified figures (exact match, reproduced a third time inside a freshly-generated `CoachingInsight`); verified the team-overview ascending sort against real data, cross-checked independently against `GET /kpi/team` (exact match on all 12 scores); downloaded and programmatically parsed both real `.xlsx` exports (not just checked HTTP status), confirming row-for-row content matching what's on screen; closed the previously-flagged SALESPERSON-role access gap live (200 across all 3 report endpoints for both an own-linked and a fully-unlinked SALESPERSON token, 401 unauthenticated); confirmed zero duplicate KPI/insight calculation logic (report layer only composes Phase 4/6's services); confirmed the previous-period comparison has no off-by-one (Q1 2026 → Q4 2025, via the same unchanged `period.util.ts` function already used elsewhere). Both `backend`/`frontend` `typecheck`/`lint`/`build` clean. Restructured `review.md`: archived Phase 5/6's full round verbatim to `review/phase-5-6.md`, carried forward every still-open item, added a new open item for the 3 leftover QA temp scripts, and added Phase 7 as the new current round. All 7 module phases now have at least one completed verification round — see the full-module summary delivered alongside this round for what "ready for `devops`" still requires.
- 2026-08-16 — TARGETED verification round for Phase 6 (Module G, AI Coaching Insights), re-checking the uncommitted fix that makes `generateInsight` MANAGER-only (closing the previously-open business-decision item, per `business-analyst`'s 2026-08-16 decision) plus its blast radius (both frontend pages, the shared coaching panel component, an unrelated Gemini model-name change). Live-tested all 3 permission cases (MANAGER `201`, SALESPERSON-even-own `403`, unauthenticated `401`) against the real dev DB; confirmed the shared-code watchlist (`authenticate.ts`, `prisma.ts`, `lib/api.ts`, the protected layout) and the full `CoachingInsight` schema contract show zero drift; whole-project `typecheck`/`lint`/`build` clean on both `backend`/`frontend`. Also live-tested the Gemini-success path for the first time in any round (a real `GEMINI_API_KEY` is now present) — real call succeeded with the new `gemini-flash-lite-latest` default model, closing that previously-open item too. Also closed, by inspection of `requirement.md`, a separate previously-open item (blocking deactivation of the last remaining active MANAGER) that `business-analyst` resolved as an Accepted Risk requiring no code. Confirmed the 3 leftover `qa-temp-*.ts` scripts are gone. Archived Phase 7's FULL round verbatim to `review/phase-7.md`; this TARGETED round is now the current round. Per this file's own gate, Phase 6's deploy eligibility resets to "needs one more FULL round" since its most recent round is now TARGETED — new Open Issues row added for this.
