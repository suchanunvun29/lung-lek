# Phase 3 (Targets, Module D) — accepted, complete

Phase 3 (Targets, Module D) — **10 of 10 tasks ✅ Verified. Phase 3 is complete.** Originally verified by full code inspection of every backend/frontend file touched (`target.routes.ts`, `target.controller.ts`, `target.service.ts`, `target.validators.ts`, `TargetsGrid`/`TargetCell`/`CopyTargetsModal`/`ProductGroupTargetsModal`/`targets/page.tsx`/`targets/[targetId]/revisions/page.tsx`), field-for-field comparison of `Target`/`TargetProductGroup`/`TargetRevision`/`TargetChangeType` in `backend/prisma/schema.prisma` against `design.md`'s Data Model (exact match, no drift), clean `typecheck`/`lint`/`build` on both `backend/` and `frontend/`, and live HTTP testing against the real dev DB and both real dev servers (`backend` on :4000, `frontend` on :3000, both started and stopped by this session). The one item left ⚠️ Partial in that round (product-group targets modal's product-type source list) was fixed by `backend-engineer`/`frontend-engineer` and **confirmed resolved live in a later targeted round** — see the note at the end of Per-Task Results and Issues Found #1 below.

Live testing method (original round): same approach as the Phase 2 round — a short-lived MANAGER JWT signed with the app's own `JWT_SECRET` (no DB write) for `manager1@example.com`, plus a SALESPERSON JWT for the pre-existing `qa.targets.salesperson@example.com` test account (left over from a prior session, reused rather than creating a new one). All target/revision rows created by this round's live testing (a full CREATE→UPDATE→UPDATE→product-group-set→product-group-replace→UPDATE chain, plus a copy-created month-6 target) were deleted afterward via a read-only-adjacent cleanup script; `Target`/`TargetRevision` counts were confirmed back at the exact pre-session baseline (2 targets / 4 revisions — themselves pre-existing artifacts of the engineers' own self-testing, left untouched).

What was actually driven live (not just read):
- **Role gating, all 3 write endpoints + both read endpoints, both directions**: SALESPERSON → `PUT /targets/:salespersonId/:year/:month` 403, `PUT /targets/:targetId/product-groups` 403, `POST /targets/copy` 403; SALESPERSON → `GET /targets?year=` 200, `GET /targets/:targetId/revisions` 200 (view-everything/edit-restricted, matching `requirement.md`'s role model). MANAGER → all of the above 200/successful.
- **Multi-edit `TargetRevision` chain, including product-group children**: created a target (CREATE, `before: null`), updated revenue/newCustomer twice (two UPDATE revisions with correct prior-state `before`), set 2 product-group targets (UPDATE revision whose `before.productGroupTargets` is `[]` and `after.productGroupTargets` has both groups), fully replaced the product groups with a different single group (UPDATE revision whose `before` shows the old 2 groups and `after` shows the new 1 group — confirming full-replace semantics are reflected in the snapshot, not a merge), then updated revenue to the `Decimal(14,2)` boundary value `999999999999.99` (UPDATE revision's `before`/`after` both round-tripped this value exactly as `999999999999.99` in the JSON snapshot — verified in Node that this exact string survives a `Number()` round-trip with no precision loss at this field's actual maximum). All 6 revisions fetched via `GET /targets/:targetId/revisions` and manually cross-checked against what was actually written — every `before` matched the prior `after` exactly, no gaps or corrupted snapshots across the chain. A second, independently pre-existing 4-revision chain in the DB (CREATE → UPDATE(revenue) → UPDATE(product-group add) → CREATE(via copy)) was also inspected read-only and shows the same correct pattern, corroborating the engineers' self-test claim.
- **`copyTargets` overwrite semantics, both branches, with product-group carryover**: `overwrite:false` copy from a month with 1 target (1 product group) to an empty destination month → `created:[salespersonId]`, destination correctly got the same revenue/newCustomer/product-group data as source (including the `productType` join). Re-running the identical copy with `overwrite:false` → `skipped:[salespersonId]`, destination left untouched (confirmed via `GET /targets` — no new revision, no data change), demonstrating the requirement's "idempotent skip-on-recopy" claim is real, not asserted. Then modified the source's revenue and swapped its product groups entirely, re-ran the copy with `overwrite:true` → `updated:[salespersonId]`, destination correctly fully replaced (old single product group gone, new two product groups present, revenue/newCustomer matching the new source values) — confirmed via both `GET /targets` and the destination's own `TargetRevision` (an UPDATE revision whose `before` is the *old* destination state and `after` is the *new* copied-in state, with `note: "คัดลอกจากเป้าเดือน 5/2026"`). Also live-tested the empty-source edge case (`fromYear/fromMonth` with zero targets) → `{"sourceCount":0,"created":[],"updated":[],"skipped":[]}`, no crash.
- **No team-wide target entity**: grepped `backend/prisma/schema.prisma` for `team` (case-insensitive) — zero matches. Confirmed by direct schema read that `Target` is scoped to `salespersonId` + `year` + `month` only (`@@unique([salespersonId, year, month])`), with no separate team/aggregate model, and no controller/service code anywhere computes or persists a team-level target — `requirement.md`'s "ไม่มีเป้าระดับทีม — ยอดทีมคือผลรวมของรายคนเท่านั้น" holds exactly as implemented.
- **Zod validation, live, negative cases**: `revenueTarget: -100` → 400; `newCustomerTarget: 1.5` (non-integer) → 400; `month: 13` → 400; nonexistent `salespersonId` → 404 ("Salesperson not found", a controller-level check beyond Zod); `copyTargetsSchema`'s `.refine` rejecting `fromYear/fromMonth === toYear/toMonth` → 400 with the expected Thai message; duplicate `productTypeId` within one `product-groups` PUT body → 400 (controller-level check); unknown `productTypeId` → 400; `product-groups` PUT on a nonexistent `targetId` → 404.
- **Decimal-to-JSON conversion in revision snapshots**: `toTargetSnapshot()`'s `Number(target.revenueTarget)`/`Number(pg.revenueTarget)` conversion was stress-tested at the field's actual maximum (`Decimal(14,2)` → `999999999999.99`, 12 integer digits + 2 decimal, the true ceiling of this column) and at a near-zero edge (`0.01`) — both round-tripped through the JSON snapshot with no precision loss, no `NaN`, no `null`. Since `revenueTarget` is `@default(0)` (never nullable) and Zod's `targetUpsertBodySchema`/`productGroupTargetsBodySchema` both enforce `z.number().nonnegative()` before the value ever reaches Prisma, there is no code path that could hand `toTargetSnapshot()` a `null`/`undefined` Decimal to convert — the conversion is safe for every value this schema can actually contain, not just the values tested.

Both `backend/` and `frontend/` pass `npm run typecheck`, `npm run lint`, and `npm run build` clean (frontend build confirms `/targets` and `/targets/[targetId]/revisions` compile, the latter correctly using Next 16's async `params` convention, same pattern already confirmed correct in Phase 2's `import-batches/[id]`).

## Per-Task Results — Phase 3

- [x] Verified — `[backend]` `GET /targets` — `target.controller.ts:13-26`, `target.routes.ts:27`. No role gate (open to all authenticated+password-changed users, per `requirement.md`'s view-everything model), filters by `year` query param, includes `salesperson` summary and `productGroupTargets` (with joined `productType`). Live-tested for both roles.
- [x] Verified — `[backend]` `PUT /targets/:salespersonId/:year/:month` (MANAGER only) — `target.controller.ts:28-48`, `target.service.ts:33-86` (`upsertMonthlyTarget`). Upserts by the schema's `@@unique([salespersonId, year, month])`, writes a `TargetRevision` on every call (`CREATE` with `before: Prisma.JsonNull` on first write, `UPDATE` with a real prior-state `before` on every subsequent write) inside the same `$transaction` as the `Target` write itself. Live-tested a 3-step revenue/newCustomer edit chain plus role gating (403 for SALESPERSON) and a 404 guard for a nonexistent `salespersonId`.
- [x] Verified — `[backend]` `PUT /targets/:targetId/product-groups` (MANAGER only) — `target.controller.ts:50-72`, `target.service.ts:93-135` (`setProductGroupTargets`). True full-replace: `deleteMany` then `createMany` inside one transaction, not a diff/merge — live-confirmed by replacing a 2-group set with a different single group and seeing the old groups fully gone. Writes an `UPDATE` `TargetRevision` capturing the parent `Target`'s full snapshot (including the now-changed `productGroupTargets`) even though the parent's own `revenueTarget`/`newCustomerTarget` didn't change. Controller-level guards live-tested: duplicate `productTypeId` in one request → 400, unknown `productTypeId` → 400, nonexistent `targetId` → 404, role gating → 403 for SALESPERSON.
- [x] Verified — `[backend]` `POST /targets/copy` (MANAGER only) — `target.controller.ts:74-79`, `target.service.ts:146-233` (`copyTargets`). Iterates all source-period `Target` rows in one transaction; `overwrite:false` skips any destination that already exists (live-confirmed idempotent — re-running the same copy twice produces `created` then `skipped`, no duplicate revision, no data change on the second run); `overwrite:true` fully replaces the destination's `revenueTarget`/`newCustomerTarget`/`note`/`productGroupTargets` (live-confirmed the old product groups are gone, not merged, after an overwrite copy) and writes an `UPDATE` `TargetRevision` with a `note` recording the source period. New destinations get a `CREATE` revision with the copied product groups already present in `after`. Empty-source edge case (`sourceCount: 0`) live-tested, no crash. Role gating live-confirmed (403 for SALESPERSON).
- [x] Verified — `[backend]` `GET /targets/:targetId/revisions` — `target.controller.ts:81-96`. 404s on a nonexistent `targetId`, otherwise returns all revisions ordered `changedAt: desc` with `changedBy` (id/displayName/email) joined in. No role gate (view-everything), live-confirmed 200 for SALESPERSON.
- [x] Verified — `[backend]` Zod schemas — `target.validators.ts`. `targetUpsertBodySchema` (`revenueTarget` nonnegative number, `newCustomerTarget` nonnegative integer), `targetUpsertParamsSchema` (`month` 1-12), `productGroupTargetsBodySchema`, `copyTargetsSchema` (with a `.refine` blocking same-period copy). All live-negative-tested (see Verification Summary) with correct 400s and field-level Thai error messages where applicable.
- [x] Verified — `[frontend]` Team-wide targets grid — `app/(protected)/targets/page.tsx`, `components/targets/TargetsGrid.tsx`, `components/targets/TargetCell.tsx`. Salespeople × 12 months grid, sticky first column, inline click-to-edit cell (revenue + newCustomer) wired to `PUT /targets/:salespersonId/:year/:month` via `lib/api.ts`'s `upsertTarget`, `canEdit` correctly derived from `currentUser?.role === "MANAGER"` and gates both the click handler and a visible "view-only" notice for SALESPERSON. Empty cells show "คลิกเพื่อตั้งเป้า" (MANAGER) vs "ยังไม่ได้ตั้งเป้า" (SALESPERSON) — correctly differentiated. Year selector present. Confirmed compiling and route-loading cleanly (`npm run build`, live `curl` to `/targets` → 200, no server error).
- [x] Verified — `[frontend]` "คัดลอกเป้าเดือนก่อน" copy action — `components/targets/CopyTargetsModal.tsx`, only rendered/reachable when `canEdit` (MANAGER) in `targets/page.tsx`. From/to year+month inputs, `overwrite` checkbox, calls `POST /targets/copy` via `lib/api.ts`'s `copyTargets`, renders the real `created`/`updated`/`skipped` arrays from the response (resolved to salesperson display names) rather than a generic success message — matches what the backend actually returns, confirmed by comparing the component's rendering logic against the live API responses captured this round.
- [x] Verified (upgraded from ⚠️ Partial) — `[frontend]` Product-group targets modal — `components/targets/ProductGroupTargetsModal.tsx`. The core mechanism (add/edit/remove rows, full-replace save via `PUT /targets/:targetId/product-groups`, read-only rendering for SALESPERSON) was already live-verified end-to-end against the real backend. **The previously-flagged gap is now fixed and re-confirmed live**: `backend/src/routes/productType.routes.ts` + `masterData.controller.ts#listProductTypes` now expose `GET /product-types` (`prisma.productType.findMany()`, open to any authenticated+password-changed role, no MANAGER gate — matching the view-everything model), and `frontend/lib/deriveProductTypes.ts`'s `fetchKnownProductTypes` now calls this endpoint directly instead of sampling `GET /sales-lines`. Confirmed live in a later targeted round: `GET /product-types` returned the real 3 rows (`GI`, `C-cae`, `U-share`) for both a MANAGER and a SALESPERSON token (200 both). Confirmed by code grep that both `sales-lines/page.tsx` and `targets/page.tsx` (which feeds `ProductGroupTargetsModal`) import and call `fetchKnownProductTypes`, and no remaining code path samples `/sales-lines` for product types. The structural gap (a `ProductType` absent from the most recent 500 invoice-dated sales lines being unselectable) no longer exists.
- [x] Verified — `[frontend]` Target revision-history page — `app/(protected)/targets/[targetId]/revisions/page.tsx`. Fetches `GET /targets/:targetId/revisions` + salespeople + product types for name resolution, renders each revision's `changeType` badge, `changedBy.displayName` + `changedAt` (localized), and full before/after (`revenueTarget`, `newCustomerTarget`, `productGroupTargets` resolved to product-type names) side by side. Correctly handles `before: null` ("— ยังไม่มี —") for the initial CREATE revision. Live-rendering logic cross-checked against the real 6-revision chain and the pre-existing 4-revision chain captured this round — every field the page reads exists in the actual API response shape.

## Design/requirement contract checks — Phase 3

- **Data Model**: `Target`, `TargetProductGroup`, `TargetRevision`, `TargetChangeType` in `backend/prisma/schema.prisma` match `design.md`'s Data Model block field-for-field (names, types, `@default`/`@db.Decimal(14,2)`/`@unique`/`@@index`, relations, cascade behavior) — checked every line side by side, no drift.
- **"No team-level target" business rule**: confirmed absent as an entity (see Verification Summary) — the team total is purely a client-side sum-of-individuals concept if/when it's ever displayed (not built yet, correctly out of Phase 3's scope), never a persisted row.
- **Revision history completeness** (`requirement.md`: "เก็บประวัติการแก้ไขไว้...ใครแก้ เมื่อไหร่ จากเท่าไหร่เป็นเท่าไหร่"): confirmed on every one of the three write paths (monthly upsert, product-group replace, copy) — none of them mutate a `Target`/`TargetProductGroup` row without also writing a `TargetRevision` in the same transaction, live-verified across a 6-step edit chain with no gaps.
- **"Copy เป้าเดือนก่อนมาใช้ซ้ำ" with overwrite control**: confirmed both branches work as specified, including that `overwrite:false` is genuinely idempotent (a real requirement.md ask — "ต้องกรอกได้สะดวก... คัดลอกเป้าเดือนก่อนมาใช้ซ้ำ" implies re-running the copy shouldn't be destructive by default).
- **View-everything / edit-restricted role model**: confirmed live in both directions across all 5 target endpoints, consistent with the same pattern already verified in Phase 1/2.
- **`ProductType` as a first-class read surface**: `design.md`'s Data Model already had `ProductType` as its own model; the addition of `GET /product-types` doesn't touch the schema, it's purely an endpoint that was missing before — no drift introduced.

## Issues Found — Phase 3

1. ~~**No dedicated `GET /product-types` endpoint**~~ — **Resolved and re-confirmed live** (2026-08-15, targeted round). `backend-engineer` added `GET /product-types` (`productType.routes.ts` + `masterData.controller.ts#listProductTypes`), and `frontend-engineer` repointed `deriveProductTypes.ts`'s `fetchKnownProductTypes` (used by both the sales-lines filter and `ProductGroupTargetsModal`) to call it instead of sampling `/sales-lines`. Live-confirmed 200 for both roles, correct 3-row response.
2. **Security review still outstanding for this module's write paths**, consistent with the project's own flag pattern — `design.md`'s Sequencing Notes only explicitly calls out Modules B/C/G as `⚠️ Sensitive`, and Module D isn't separately flagged there. Noting anyway since Phase 3 introduces MANAGER-only mutation endpoints with financial figures (`revenueTarget`) — not a defect found by this functional pass, just a completeness note for whoever runs `security` next; no specific concern beyond what's already covered by the shared JWT/role-middleware pattern already verified correct here. **Still open, non-blocking** — carried to main `review.md` Open Issues.

## Review Outcome — Phase 3

**Phase 3 (Targets, Module D) — accepted, 10/10 tasks verified. Phase 3 is complete.**

Issue #1 (missing `GET /product-types`) resolved and reconfirmed live in a targeted round. Issue #2 (security pass) remains an open, non-blocking completeness note — Module D isn't in `design.md`'s explicit `⚠️ Sensitive` list, unlike Modules B/C/G.

## Change Log (Phase 3 rounds)

- 2026-08-15 — First verification round for Phase 3 (Module D, Targets). 9/10 tasks ✅ Verified, 1/10 ⚠️ Partial, boxes checked in `plan.md` for the 9. Verified via full code inspection (`target.service.ts`/`controller`/`routes`/`validators`, all Phase 3 frontend pages/components), field-for-field schema comparison against `design.md` (no drift), clean `typecheck`/`lint`/`build` on both apps, and live HTTP testing with both real dev servers running (backend `npm run dev` on :4000, frontend `npm run dev` on :3000, both stopped at session end): a live 6-step `TargetRevision` chain (create → 2 revenue updates including a `Decimal(14,2)` boundary value `999999999999.99` → product-group set → product-group full-replace) confirmed accurate before/after snapshots including product-group children at every step, with exact Decimal-to-Number precision (no `NaN`/precision loss at the field's actual max); `copyTargets` live-tested for both `overwrite:false` (idempotent skip on recopy, confirmed via a second identical call producing `skipped` not `created`) and `overwrite:true` (full replace of revenue/newCustomer/product-groups, confirmed old groups fully gone not merged) plus an empty-source edge case; role gating live-confirmed in both directions across all 3 write endpoints and both read endpoints; confirmed by schema grep that no team-aggregate `Target` entity exists anywhere. Found one implementation gap: the product-group targets modal's selectable product-type list is derived from a 500-row `/sales-lines` sample rather than a real `GET /product-types` endpoint (routed to `backend-engineer`) — not currently observable as broken with this dataset's 3 product types, but structurally real. All QA-created `Target`/`TargetRevision` rows deleted at the end of the session; DB confirmed back at the exact pre-session baseline (2 targets / 4 revisions, pre-existing from the engineers' own self-testing, left untouched).
- 2026-08-15 — Targeted re-verification of the single remaining item (missing `GET /product-types`), run in the same session as Phase 4's FULL round (see main `review.md`). Confirmed live: `GET /product-types` exists, is open to any authenticated role (200 for both MANAGER and SALESPERSON tokens, matching the view-everything model), and both `sales-lines/page.tsx` and `targets/page.tsx` (feeding `ProductGroupTargetsModal`) now call it via `deriveProductTypes.ts`'s `fetchKnownProductTypes` instead of sampling `/sales-lines`. 10/10 tasks now ✅ Verified, Phase 3 complete, `plan.md`'s previously-unchecked box for `หน้า/modal ตั้งเป้ากลุ่มสินค้าต่อคนต่อเดือน` (line 64) now checked `[x]`, since the gap that had kept it unchecked in the first round is resolved and re-verified. Module D's security-pass note (Issue #2) remains open, carried to main `review.md` Open Issues.
