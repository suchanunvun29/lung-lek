# Phase 4 (KPI & Scoring Engine, Module E) — FULL round (session also ran TARGETED re-checks of Phase 2/3 fixes)

This archive preserves, verbatim, the "Verification Summary (current round)" and Phase 4-specific sections that were live in `review.md` as of the 2026-08-15 FULL round, before Phase 5/6's round superseded them as the current round.

## Verification Summary (as recorded that round)

This session ran **two separate rounds**, at the coordinating agent's direction:

**1. TARGETED re-verification** of two previously-flagged fixes, one in Phase 2 and one in Phase 3 (Phase 2's most recent round had been FULL; Phase 3's most recent round had been FULL; both fixes were narrow and well-specified, making a targeted re-check the right scope per this file's own mode rules). Both fixes were confirmed live against the real dev DB with both dev servers running (`backend` on :4000, `frontend` on :3000):

- **Phase 2** — `import.service.ts`'s failure-path `catch` block now persists `totalRows`/`insertedRows`/`updatedRows`/`errorRows`/`skippedRows`/`sheetsFound`/`periodsTouched` on a `FAILED` `ImportBatch`, instead of leaving them at `0`/`null`. **Live-reproduced**: fired two real concurrent `POST /import` requests against the actual sample file (`รายละเอียดขาย มกราคม - มีนาคม 2569.xlsx`) 1 second apart. The second request's transaction correctly lost the `pg_try_advisory_xact_lock` race and failed in ~1.3s; its resulting `ImportBatch` record showed `status: "FAILED"`, `totalRows: 860`, `errorRows: 14`, `sheetsFound: ["Sheet1","Sheet2"]`, `insertedRows: 0`, `updatedRows: 0`, `periodsTouched: []` — correctly populated and consistent with its own 14 `ImportIssue` rows, closing the previously-reproduced bug where these fields all showed `0`/`null` despite the issues existing. The first (slower) request committed normally in parallel (`status: "PARTIAL"`, matching the pre-existing baseline). Full detail archived in `review/phase-2.md`; 19/19 Phase 2 tasks now ✅ Verified, `plan.md` box checked.
- **Phase 3** — `GET /product-types` now exists (`backend/src/routes/productType.routes.ts` + `masterData.controller.ts#listProductTypes`), open to any authenticated+password-changed role (no MANAGER gate, matching the view-everything model), and the frontend's `lib/deriveProductTypes.ts` (`fetchKnownProductTypes`) — used by both `sales-lines/page.tsx`'s filter and `targets/page.tsx`'s `ProductGroupTargetsModal` — now calls this endpoint instead of sampling `GET /sales-lines`. **Live-confirmed**: `GET /product-types` returned the real 3 rows (`GI`, `C-cae`, `U-share`) for both a MANAGER token and a SALESPERSON token (200 both). Confirmed by code grep that no remaining code path samples `/sales-lines` for product types. Full detail archived in `review/phase-3.md`; 10/10 Phase 3 tasks now ✅ Verified, `plan.md` box for `หน้า/modal ตั้งเป้ากลุ่มสินค้าต่อคนต่อเดือน` checked (the `GET /product-types` endpoint itself was never a separate `plan.md` line item — it's a dependency the modal task needed, now resolved).

Live testing method for this targeted round: a short-lived MANAGER JWT (`manager2@example.com`) and a SALESPERSON JWT (pre-existing `qa.targets.salesperson@example.com`) were signed directly with the app's own `JWT_SECRET` (read from `backend/.env`, no DB write) via a temporary compiled script placed in `backend/src/`, run once, then deleted along with its compiled output before this round ended — no trace left in the codebase.

**2. FULL verification** of Phase 4 (KPI & Scoring Engine, Module E) — every task in the phase, from scratch, per `plan.md`'s Phase 4 block and `design.md`'s "KPI & Scoring Rules" section (design.md's own words: "the heart of the system and the easiest place to get wrong"). This phase is about to become eligible for deploy consideration, and per this file's own gate, a phase's first FULL round is required before it's deploy-eligible regardless of how clean the result looks — this satisfies that.

Verified by full code inspection of every backend/frontend file touched (`kpi.service.ts`, `kpi.controller.ts`, `kpi.routes.ts`, `kpi.validators.ts`, `period.util.ts`, `settings.service.ts`, `settings.controller.ts`, `settings.routes.ts`, `settings.validators.ts`, all Phase 4 frontend pages/components), field-for-field comparison of `ScoringWeight`/`ScoringWeightRevision`/`EvaluationSetting`/`KpiMetric` in `backend/prisma/schema.prisma` against `design.md`'s Data Model (exact match, no drift), clean `typecheck`/`lint`/`build` on both `backend/` and `frontend/` (frontend build also confirms `/kpi`, `/settings/scoring-weights`, `/settings/evaluation` all compile as routes), and — going well beyond "endpoints return 200" per this round's explicit brief — **independent hand-computation of every scored metric's formula against raw `SalesLine`/`Target`/`Hospital` data**, not just re-reading the service's own output.

What was actually driven live and hand-verified (not just read or taken on the engineer's self-report):

- **`REVENUE_VS_TARGET`** — for a real salesperson (Miss Chidchanok Seethong) and period (Q1 2026), independently queried `SalesLine.total` (aggregate sum) and `Target.revenueTarget` (summed across the 3 months) directly via Prisma outside the service. Hand result: actual `773,540` ÷ target `1,800,000` = `42.97%` — matches the API's `achievementPercent: 42.974444...` exactly, and matches the engineer's self-reported number.
- **`NEW_CUSTOMERS`** — independently re-implemented the "first-ever sale system-wide, `isPreExistingCustomer=false`, credited to whoever made that first sale" logic directly against raw `SalesLine`/`Hospital` data (not by calling the service's own helper). Hand result: 12 hospitals credited to this salesperson in Q1 2026, with the exact same 12 hospital IDs the API returned, against a target of 15 → `80%` — matches exactly.
- **`PRODUCT_GROUP` per-group capping — specifically scrutinized per this round's brief.** No existing data had a salesperson over-performing one product group while under-performing another, so one was constructed live through the app's own real endpoints (not raw SQL): `PUT /targets/:salespersonId/:year/:month` + `PUT /targets/:targetId/product-groups` for a real salesperson (Mr. Sathit Ratanakuakul, who genuinely sells in 2 product types) with deliberately asymmetric targets — `U-share` target `100` (actual `9,500`, a ~95x overperformance) and `C-cae` target `300,000` (actual `158,150`, a genuine underperformance). The API returned `score: 52.73%`, matching the hand-computed capped formula exactly: `(min(9500,100) + min(158150,300000)) / (100+300000) × 100 = 158250/300100×100 = 52.73%`. Had the implementation summed raw actuals instead of capping per group first (the exact bug `design.md` warns against — "ตัดยอดเกินรายกลุ่มออกก่อน เพื่อไม่ให้กลุ่มหนึ่งไปกลบอีกกลุ่ม"), the score would have come out `55.86%` instead — confirming the massive `U-share` overperformance is genuinely prevented from masking the real `C-cae` shortfall. The test target and its 2 `TargetRevision` rows were deleted afterward via a direct cleanup script; confirmed back to 0 targets for that salesperson.
- **Composite renormalization — specifically scrutinized per this round's brief.** Confirmed live across three distinct cases: (a) a salesperson with 3-of-5 computable metrics (Chidchanok, Q1 2026) → `composite: 60.61`, `computedFromLabel: "คิดจาก 3 จาก 5 เกณฑ์"`, matching `Σ(weight×score)/Σ(weight)` computed by hand over exactly the 3 computable metrics' weights (50+15+15=80) and scores; (b) a salesperson with 0-of-5 computable metrics (no targets set at all) → `composite: null` (not `0`), `message: "ยังไม่มีคะแนนรวม เนื่องจากยังไม่มีเกณฑ์ใดคำนวณได้ในงวดนี้"`; (c) **a deliberately constructed edge case** — temporarily set `ScoringWeight` to `REVENUE_VS_TARGET=0, NEW_CUSTOMERS=0, PRODUCT_GROUP=0, RETENTION=50, CONSISTENCY=50` (so all 3 of Chidchanok's computable metrics have weight `0`) via a real `PUT /settings/scoring-weights` call → `composite: null` again, but with the *distinct* message `"ยังไม่มีคะแนนรวม เนื่องจากน้ำหนักรวมของเกณฑ์ที่คำนวณได้เป็น 0"`, correctly distinguishing "no computable metrics" from "computable metrics all have zero weight" rather than dividing by zero or silently showing `0`. Weights restored to `50/15/15/10/10` immediately after, confirmed via `GET /settings/scoring-weights`; the two QA-only `ScoringWeightRevision` rows this created were deleted afterward via a direct cleanup script.
- **`RETENTION` and `CONSISTENCY` `dataCoverageMonths` gating — specifically scrutinized per this round's brief.** Confirmed the real dev DB currently has exactly 3 months of `SalesLine` data (`{2026-01, 2026-02, 2026-03}`, queried directly), and confirmed `EvaluationSetting.minMonthsForChurn`/`minMonthsForConsistency` are both `6` (**the engineer's claimed restoration from the temporary test value of 3 was independently confirmed** via a direct `GET /settings/evaluation` before touching anything). With the real settings (6/6), both metrics correctly return `computable: false` with `reason: "ข้อมูลยังไม่เพียงพอ ต้องการ 6 เดือน ปัจจุบันมี 3 เดือน"` across every period tested. To verify the *formulas themselves* rather than just the gate, temporarily lowered both thresholds to `3` via a real `PATCH /settings/evaluation` call (mirroring what the engineer's own self-test had done) and independently hand-computed both metrics directly against raw `SalesLine` data for the same salesperson+period the engineer had reported: `RETENTION` — Feb 2026 had 6 distinct hospitals, March 2026 retained 1 of them → `16.6667%`, matching the API and the engineer's self-report exactly. `CONSISTENCY` — monthly sums `[164140, 368500, 240900]` for Jan/Feb/Mar, hand-computed population mean `257846.67`, stddev `84285.80`, `CV = 0.32688`, score `max(0, 1-CV)×100 = 67.3117%` — matching the API and the engineer's self-report exactly. Thresholds were restored to `6`/`6` immediately after via `PATCH /settings/evaluation`, confirmed via a follow-up `GET`, and confirmed the gating correctly reverted to "insufficient data" for the same salesperson/period.
- **Drill-down accuracy — specifically scrutinized per this round's brief.** For `REVENUE_VS_TARGET` (Chidchanok, Q1 2026): drill-down returned 45 `SalesLine` rows summing to exactly `773,540`, matching the parent metric's `actual` figure. For `NEW_CUSTOMERS`: drill-down returned exactly 12 rows, with hospital IDs identical to the metric's own `hospitalIds` list. For `PRODUCT_GROUP`: drill-down returned 45 rows summing to `773,540` — the genuine uncapped source total, correctly *not* the capped `200,000` used in scoring (the drill-down is meant to show real underlying transactions, not the capped scoring input). For `RETENTION` (with thresholds temporarily lowered): drill-down returned exactly 2 rows — the one retained hospital's lines in both the previous period (Feb) and current period (March) — matching `retainedHospitalIds`. All 5 supplementary drill-down metrics (`ACTIVE_CUSTOMERS`, `CHURNED_CUSTOMERS`, `PRODUCT_PENETRATION`, `REVENUE_BY_HOSPITAL`, `MONTHLY_TREND`) smoke-tested live, all 200.
- **Role gating on both settings endpoints — specifically scrutinized per this round's brief.** Live-confirmed both directions: `GET /settings/scoring-weights` and `GET /settings/evaluation` → 200 for a SALESPERSON token; `PUT /settings/scoring-weights` and `PATCH /settings/evaluation` → `403 {"error":"Forbidden: insufficient role"}` for the same SALESPERSON token; both writes → 200 for a MANAGER token. Zod's weight-sum validation live-tested: a `weights` array summing to 95 (not 100) → `400`, correct field-level message.
- **`GET /kpi/:salespersonId`, `GET /kpi/team`, `GET /kpi/:salespersonId/drill-down/:metric`** all live-tested across multiple salespeople/periods, including the `GET /kpi/team` role-open check (200 for SALESPERSON).

All QA-created test state was reverted before this round ended: the constructed `Target`/`TargetProductGroup`/`TargetRevision` rows for the capping test (deleted), the 3 `ScoringWeightRevision` rows created during the weight-edge-case and restore-defaults tests (deleted), `ScoringWeight` confirmed back at `50/15/15/10/10`, `EvaluationSetting` confirmed back at `churnMonths=6, minMonthsForChurn=6, minMonthsForConsistency=6`. The 3 new `ImportBatch` rows created by this session's live import testing (Job 1, Phase 2) were left in place as the expected artifacts of exercising `POST /import` for real, consistent with how prior rounds have handled this. All temporary scripts written to `backend/src/` for JWT signing and DB inspection were deleted before this round ended, along with their compiled output; both dev servers (backend :4000, frontend :3000) were stopped.

**Zero implementation defects were found in Phase 4.** This is an unusually clean phase — every formula, gating rule, edge case, and role check specifically called out in this round's brief was independently reproduced from raw data and matched the API's output exactly.

## Verified File Manifest — Phase 4

Files inspected in this FULL round, with size and line count, so a future TARGETED round can tell what moved.

| File | Bytes | Lines | Round |
|---|---:|---:|---|
| `backend/src/services/kpi.service.ts` | 23574 | 654 | Phase 4 FULL |
| `backend/src/controllers/kpi.controller.ts` | 2495 | 68 | Phase 4 FULL |
| `backend/src/routes/kpi.routes.ts` | 1168 | 35 | Phase 4 FULL |
| `backend/src/validators/kpi.validators.ts` | 2407 | 65 | Phase 4 FULL |
| `backend/src/services/period.util.ts` | 1851 | 65 | Phase 4 FULL |
| `backend/src/services/settings.service.ts` | 927 | 30 | Phase 4 FULL |
| `backend/src/controllers/settings.controller.ts` | 1629 | 45 | Phase 4 FULL |
| `backend/src/routes/settings.routes.ts` | 1026 | 33 | Phase 4 FULL |
| `backend/src/validators/settings.validators.ts` | 1547 | 37 | Phase 4 FULL |
| `backend/prisma/schema.prisma` | 8707 | 308 | Phase 4 FULL |
| `frontend/app/(protected)/kpi/page.tsx` | 4552 | 117 | Phase 4 FULL |
| `frontend/app/(protected)/settings/scoring-weights/page.tsx` | 3119 | 75 | Phase 4 FULL |
| `frontend/app/(protected)/settings/evaluation/page.tsx` | 2825 | 66 | Phase 4 FULL |
| `frontend/components/kpi/CompositeScoreBadge.tsx` | 2013 | 51 | Phase 4 FULL |
| `frontend/components/kpi/ScoreCard.tsx` | 2082 | 52 | Phase 4 FULL |
| `frontend/components/kpi/SupplementaryKpisPanel.tsx` | 5113 | 107 | Phase 4 FULL |
| `frontend/components/kpi/PeriodSelector.tsx` | 2807 | 89 | Phase 4 FULL |
| `frontend/components/kpi/KpiDrillDownModal.tsx` | 4690 | 107 | Phase 4 FULL |
| `frontend/components/settings/ScoringWeightsForm.tsx` | 3809 | 101 | Phase 4 FULL |
| `frontend/components/settings/ScoringWeightsReadOnly.tsx` | 878 | 23 | Phase 4 FULL |
| `frontend/components/settings/ScoringWeightRevisionsList.tsx` | 1860 | 49 | Phase 4 FULL |
| `frontend/components/settings/EvaluationSettingForm.tsx` | 4478 | 104 | Phase 4 FULL |
| `frontend/components/settings/EvaluationSettingReadOnly.tsx` | 2007 | 34 | Phase 4 FULL |
| `frontend/lib/kpiLabels.ts` | 1887 | 48 | Phase 4 FULL |
| `frontend/lib/api.ts` | 11189 | 350 | Phase 4 FULL |
| `frontend/lib/types.ts` | 9951 | 376 | Phase 4 FULL |

## Per-Task Results — Phase 4 (this round)

- [x] Verified — `[backend]` Service คำนวณ `REVENUE_VS_TARGET` — `kpi.service.ts:47-81` (`computeRevenueVsTarget`). `SUM(total)` of the period ÷ summed monthly `revenueTarget` × 100, capped at 100, `computable: false` with reason "ยังไม่ได้ตั้งเป้า หรือเป้าเป็น 0" when target sum is 0. Hand-verified against raw `SalesLine`/`Target` data for a real salesperson+period — exact match (see Verification Summary).
- [x] Verified — `[backend]` Service คำนวณ `NEW_CUSTOMERS` — `kpi.service.ts:83-147` (`computeNewCustomers`, `getFirstSalePerHospital`, `getNewCustomerCreditedLines`). Correctly computes the *system-wide* first sale per non-pre-existing hospital (not per-salesperson-first-sale), credits whoever made that first transaction, filters to hospitals whose first sale falls in the requested period. Hand-re-implemented independently against raw data — exact match on both the count and the specific hospital IDs credited.
- [x] Verified — `[backend]` Service คำนวณ `PRODUCT_GROUP` — `kpi.service.ts:149-208` (`computeProductGroup`). Per-group `min(actual, target)` capping applied *before* summing (`cappedSum`), divided by `Σ target`. **This round specifically constructed a live over/under-performance scenario across 2 real product groups and confirmed the capping prevents overperformance from masking underperformance** — see Verification Summary for the exact numbers.
- [x] Verified — `[backend]` Service คำนวณ `RETENTION` — `kpi.service.ts:210-267` (`computeRetention`). Gates on `dataCoverageMonths < settings.minMonthsForChurn` before computing, also handles the "no customers in previous period" edge case. Hand-verified against raw `SalesLine` hospital sets for two adjacent real months — exact match, both with the gate correctly blocking computation at the real dev DB's current 3-month coverage and correctly computing when temporarily unblocked.
- [x] Verified — `[backend]` Service คำนวณ `CONSISTENCY` — `kpi.service.ts:269-318` (`computeConsistency`). Coefficient of variation over `trailingMonths(..., settings.minMonthsForConsistency)`, gated the same way as `RETENTION`, handles `mean === 0`. Hand-verified — population mean/stddev/CV computed independently from raw `SalesLine` sums, exact match to the API including the `CV=0.32688` figure.
- [x] Verified — `[backend]` Service คำนวณ `dataCoverageMonths` — `kpi.service.ts:22-28` (`getDataCoverageMonths`). `prisma.salesLine.findMany({distinct: ["year","month"]})`. Confirmed returns `3` against the real dev DB, matching a direct independent query of the same table.
- [x] Verified — `[backend]` Service renormalize คะแนนรวม — `kpi.service.ts:330-373` (`computeCompositeScore`). Filters to `computable` metrics only before summing weight×score and dividing by the summed weight of exactly those metrics (true renormalization, not a fixed-denominator average); returns `computedFromLabel` ("คิดจาก X จาก 5 เกณฑ์"); returns `composite: null` (never `0`) with an explanatory `message` both when zero metrics are computable and — a distinct, correctly-separated case — when the computable metrics' combined weight is `0`. **All three states live-confirmed this round**, including the second edge case which required deliberately constructing it via a real settings change (see Verification Summary).
- [x] Verified — `[backend]` Service คำนวณ KPI ประกอบ (ไม่คิดคะแนน) — `kpi.service.ts:391-534` (`computeSupplementaryKpis`, `computeChurnedCustomers`). All 5 supplementary metrics present (active/churned customers, product penetration, revenue share by hospital, monthly trend), each returning hospital/product-type ids usable for drill-down. Live-tested for a real salesperson+period; churn logic correctly excludes hospitals active in the current period and applies `churnMonths` to the rest.
- [x] Verified — `[backend]` Endpoint `GET /kpi/:salespersonId` — `kpi.controller.ts:7-28`, `kpi.routes.ts:21-26`. 404s on unknown `salespersonId`, returns composite + supplementary together, period validated via `periodQuerySchema`. Live-tested across multiple salespeople/periods.
- [x] Verified — `[backend]` Endpoint `GET /kpi/team` — `kpi.controller.ts:30-46`, `kpi.routes.ts:19`. Returns composite scores for every active salesperson for one period, no role gate (view-everything model). Live-confirmed 200 for a SALESPERSON token.
- [x] Verified — `[backend]` Endpoint `GET /kpi/:salespersonId/drill-down/:metric` — `kpi.controller.ts:48-68`, `kpi.routes.ts:28-33`. Correctly routes scored metrics (`getScoredMetricDrillDown`) vs supplementary metrics (`getSupplementaryDrillDown`) via `SCORED_METRIC_SET`. Drill-down row accuracy independently hand-verified for `REVENUE_VS_TARGET`, `NEW_CUSTOMERS`, `PRODUCT_GROUP`, `RETENTION` this round (see Verification Summary); all 5 supplementary metrics smoke-tested live.
- [x] Verified — `[backend]` Endpoint `GET/PUT /settings/scoring-weights` — `settings.controller.ts:8-26`, `settings.routes.ts:17-23`, `settings.service.ts` (`updateScoringWeights`). `GET` open to all roles; `PUT` requires MANAGER (live-confirmed 403 for SALESPERSON), writes a `ScoringWeightRevision` with full before/after snapshot inside the same transaction as the weight update, on every call. Zod enforces exactly 5 unique metrics summing to 100 (live-tested a sum-≠-100 body → 400).
- [x] Verified — `[backend]` Endpoint `GET/PATCH /settings/evaluation` — `settings.controller.ts:28-45`, `settings.routes.ts:25-31`. `GET` open to all roles; `PATCH` requires MANAGER (live-confirmed 403 for SALESPERSON), partial-update Zod schema (`.refine` requires ≥1 field). Live round-tripped `minMonthsForChurn`/`minMonthsForConsistency` between `6` and `3` and back, confirming both persistence and the restoration the engineer claimed.
- [x] Verified — `[backend]` Zod schemas — `kpi.validators.ts`, `settings.validators.ts`. `periodQuerySchema`/`drillDownQuerySchema` validate `periodType` enum + `periodNumber` range per type (1-12 for MONTH, 1-4 for QUARTER) and normalize `YEAR`'s `periodNumber` to `0`; `scoringWeightsUpdateSchema` enforces exactly 5 unique metrics summing to 100; `evaluationSettingUpdateSchema` requires at least one field. All live-negative-tested.
- [x] Verified — `[frontend]` หน้าตั้งค่าน้ำหนักคะแนน (MANAGER เท่านั้น) — `app/(protected)/settings/scoring-weights/page.tsx`, `ScoringWeightsForm.tsx`, `ScoringWeightsReadOnly.tsx`, `ScoringWeightRevisionsList.tsx`. `canEdit` correctly gated on `role === "MANAGER"`; form validates the sum-to-100 rule client-side (mirroring the server's Zod rule) before allowing submit; revision history renders every `before`/`after` pair with `changedBy`/`changedAt`. Route compiles clean (`npm run build`), live `curl` → 200.
- [x] Verified — `[frontend]` หน้าตั้งค่าคงที่ของการประเมิน (MANAGER เท่านั้น) — `app/(protected)/settings/evaluation/page.tsx`, `EvaluationSettingForm.tsx`, `EvaluationSettingReadOnly.tsx`. All 5 fields present (`churnMonths`, `minMonthsForChurn` — a superset of `plan.md`'s line-item wording, which only names `minMonthsForConsistency` explicitly, but the field exists in the confirmed schema and is correctly exposed anyway — not a gap), `minMonthsForConsistency`, `aiEnabled`, `aiAnonymize`), MANAGER-only edit with read-only fallback. Route compiles clean, live `curl` → 200.
- [x] Verified — `[frontend]` Component แสดงคะแนนรวม + badge "คิดจาก X จาก 5 เกณฑ์" — `CompositeScoreBadge.tsx`, `ScoreCard.tsx`. Badge shows the composite (or `-` when `null`, never `0`), the "คิดจาก X จาก 5 เกณฑ์" label, and an expandable list of excluded criteria with their reasons; `ScoreCard` lists all 5 metrics with per-metric drill-down buttons. Written as a standalone, prop-driven component with no phase-4-specific coupling, so it's structurally reusable on dashboard/leaderboard/report as `design.md` asks — actual reuse there is Phase 5/7 scope and correctly not yet built.
- [x] Verified — `[frontend]` Component drill-down (modal/expandable) — `KpiDrillDownModal.tsx`. Fetches `GET /kpi/:salespersonId/drill-down/:metric`, renders the source `SalesLine` rows in a table (date/invoice/hospital/product/product-type/total) plus metric-specific context (retained hospital count, churn threshold, trailing months). Row-sum accuracy independently hand-verified this round for multiple metrics (see Verification Summary).

## Design/requirement contract checks — Phase 4

- **Data Model**: `ScoringWeight`, `ScoringWeightRevision`, `EvaluationSetting`, `KpiMetric` in `backend/prisma/schema.prisma` match `design.md`'s Data Model block field-for-field (names, types, `@unique`/`@default("singleton")`/relations) — checked every line side by side, no drift.
- **KPI & Scoring Rules contract** (`design.md` lines ~436-457): all 5 metric formulas, the `dataCoverageMonths` definition, the renormalization rule (including the "never show 0" rule and the distinct "no computable metrics" vs "computable metrics all weight 0" messages), and the supplementary-KPI drill-down requirement all confirmed to match `design.md` verbatim — by code reading *and* by independent hand-computation against raw data, not just by re-reading the service's own claims.
- **`achievementCapPercent` / scoring-above-100% is correctly NOT implemented** — `design.md`'s Unresolved Open Questions explicitly defers this ("ยังไม่ทำใน MVP นี้"), and `computeRevenueVsTarget`/`computeNewCustomers` both correctly cap at `min(..., 100)`. Matches scope.
- **View-everything / edit-restricted role model**: confirmed live in both directions across both settings endpoint pairs, consistent with the pattern already verified in Phases 1-3.
- **Weight seed values (50/15/15/10/10)**: confirmed present and correct in the live `ScoringWeight` table, matching `design.md`'s locked initial weights.

## Issues Found — Phase 4

None. Zero implementation defects found in this FULL round — every formula, gating rule, edge case (per-group capping, both composite-null branches, threshold restoration, drill-down row accuracy, role gating) specifically called out in this round's brief was independently reproduced from raw `SalesLine`/`Target`/`Hospital`/`EvaluationSetting` data and matched the API's output exactly, not just re-confirmed against the service's own claims.

The one non-blocking observation, not a defect: `PUT /settings/scoring-weights` writes a new `ScoringWeightRevision` on every call regardless of whether the submitted weights actually differ from the current ones (before===after is possible). `design.md` says "บันทึก ScoringWeightRevision ทุกครั้งที่แก้" (record a revision every time it's edited), which is arguably satisfied literally by "every PUT call" — this mirrors the same accepted behavior already noted for `TargetRevision` in Phase 3. Not routed anywhere; noted for completeness only.

## Review Outcome — Phase 4

**Phase 4 (KPI & Scoring Engine, Module E) — FULL round, 18/18 tasks ✅ Verified, zero defects found, all boxes checked in `plan.md`.**

Pending the user's decision (see below). If accepted, Phase 4 is eligible for the `security` agent to skip (no sensitive surface introduced) and eligible for `devops` consideration once the user also resolves the pre-existing deploy blockers already open on Phases 1-3 (the two outstanding `security` gates on Modules B and C).

Also from this session: Phase 2 and Phase 3's previously-flagged fixes are both confirmed resolved (TARGETED round) — see the Verification Summary above and their respective archives. **Because Phase 2's and Phase 3's most recent rounds were TARGETED, not FULL, neither is currently deploy-eligible on its own strength** — each phase's original FULL round is still the last FULL round on record for it, and per this file's own gate a phase needs a FULL round specifically before the `security`→`devops` handoff, not just an accepted TARGETED fix-check. Practically: Phase 2 and Phase 3's implementations are complete and their known issues resolved, but a future FULL re-pass (even a quick one, since nothing else has changed) should run before `devops` ships them, purely to satisfy this rule rather than because anything is suspected wrong.

## Change Log (Phase 4 round)

- 2026-08-15 — First verification round for Phase 1 (Module B). 15/16 ✅ Verified. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — Second verification round for Phase 1 (Module B). 16/16 ✅ Verified, Phase 1 complete. Full entry archived to `review/phase-1.md`.
- 2026-08-15 — First verification round for Phase 2 (Module C). 18/19 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-2.md`.
- 2026-08-15 — First verification round for Phase 3 (Module D). 9/10 ✅ Verified, 1 ⚠️ Partial. Full entry archived to `review/phase-3.md`.
- 2026-08-15 — TARGETED re-verification of two previously-flagged fixes: Phase 2's `ImportBatch` failure-path persistence (live-reproduced via a real concurrent-import collision, now correct) and Phase 3's missing `GET /product-types` (now exists, frontend repointed, live-confirmed for both roles). 19/19 Phase 2 tasks and 10/10 Phase 3 tasks now ✅ Verified; both phases' `plan.md` boxes checked. Phase 2 and Phase 3 full entries archived to `review/phase-2.md` and `review/phase-3.md` respectively (superseded by this round and by Phase 4 becoming the current round).
- 2026-08-15 — FULL verification round for Phase 4 (Module E, KPI & Scoring Engine), run in the same session as the targeted re-checks above. 18/18 tasks ✅ Verified, zero defects, all `plan.md` boxes checked. Verified by full code inspection, field-for-field schema comparison against `design.md` (no drift), clean `typecheck`/`lint`/`build` on both apps, and extensive independent hand-computation of every scored metric's formula against raw `SalesLine`/`Target`/`Hospital` data for real salespeople/periods — not just re-confirming the service's or the implementing engineer's own self-reported numbers. Specifically scrutinized and confirmed correct: `PRODUCT_GROUP`'s per-group capping (constructed a live asymmetric over/under-performance scenario across 2 real product groups via real target-setting endpoints, confirmed the capped formula — not a naive sum — drives the score); the composite renormalization formula including both its `composite: null` branches (zero computable metrics, and computable metrics with zero combined weight — the second deliberately constructed via a real settings change); `RETENTION`/`CONSISTENCY`'s `dataCoverageMonths` gating (confirmed the real dev DB's 3-month coverage against the restored `minMonthsForChurn`/`minMonthsForConsistency = 6` defaults, and independently re-derived both formulas by hand after temporarily lowering the thresholds, mirroring the engineer's own claimed self-test); drill-down row-sum accuracy for 4 scored metrics; and role gating on both settings endpoints in both directions. All QA-constructed test data (a target + 2 revisions for the capping test, 3 scoring-weight revisions for the composite edge-case test) was deleted afterward; `ScoringWeight`/`EvaluationSetting` confirmed back at their correct baseline values. Restructured `review.md` to match the current Open-Issues/current-round/manifest format for the first time — archived Phase 1, 2, and 3's full historical detail verbatim to `review/phase-1.md`, `review/phase-2.md`, `review/phase-3.md`, carrying every still-open item up into the new `## Open Issues — all phases` table at the top of this file.
