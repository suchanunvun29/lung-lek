# Phase 7 (Coaching Reports & Export, Module H) — FULL round

This archive preserves, verbatim, the "Verification Summary (current round)", "Verified File Manifest — Phase 7", "Per-Task Results — Phase 7", "Design/requirement contract checks — Phase 7", "Issues Found — Phase 7", and "Review Outcome — Phase 7" sections that were live in `review.md` as of the 2026-08-15 FULL round, before this 2026-08-16 TARGETED round on Phase 6 superseded them as the current round. The corresponding one-line `## Change Log` entry for this round stays in `review.md` (cumulative log, not duplicated here).

## Verification Summary (as recorded that round)

**FULL round — Phase 7 (Coaching Reports & Export, Module H).** This is Phase 7's first verification round (no prior round exists — `plan.md`'s Phase 7 checkboxes were still all `[ ]` and `review.md` had no Phase 7 section before this round started), and it is also, per the brief, likely the last implementation-phase QA round for this module — both conditions independently require FULL under this file's own mode gate.

**On the previously-interrupted attempt**: before starting, `plan.md` and `review.md` were checked for any partial write-up. None had landed — every Phase 7 checkbox was still `[ ]` and `review.md`'s current round was still Phase 5/6's. The only traces of the interrupted attempt were (a) three leftover temp scripts in `backend/src/` (`qa-temp-inspect.ts`, `qa-temp-sign.ts`, `qa-temp-parse-xlsx.ts` — a user-lister, a JWT-signer, and an `.xlsx` dumper), which this round **reused** rather than recreating, and (b) one orphaned `CoachingInsight` row (Chidchanok Seethong, `periodType: YEAR`, `year: 2026`, `periodNumber: 0`, `generatedById` = the same seeded `manager1@example.com` account, `generatedAt` timestamped earlier the same day) that the interrupted session had created mid-test and never cleaned up. Both were confirmed as leftovers rather than real data (the temp scripts are prefixed `qa-temp-` and not imported by any route; the orphaned insight matched the exact salesperson named in the brief and was generated by the seeded test manager account within the same session's timeframe) — the insight row was deleted this round (see Cleanup below); the temp scripts could not be deleted under this round's tooling and are now recorded as an open cleanup item.

Read `plan.md` (Phase 7 block, Plan Summary, Sequencing Notes, Unresolved Open Questions), `design.md` (Data Model, Module H, KPI & Scoring Rules, Risks), `requirement.md` (Core Feature 8, Scope, Open Question 6 on export format), `review.md` and all four `review/phase-*.md` archives, and `_docs/status.md` before starting.

**Both `backend/` and `frontend/` pass `npm run typecheck`, `npm run lint`, and `npm run build` clean** — frontend build confirms `/reports/individual` and `/reports/team-overview` compile as routes alongside all pre-existing routes, no regressions.

**Both dev servers were started** (`backend` :4000, `frontend` :3000) and driven live against the real dev DB. Reused the leftover `qa-temp-sign.ts`/`qa-temp-inspect.ts` scripts to sign a MANAGER JWT, a SALESPERSON JWT linked to Chidchanok Seethong (`sp-test-phase6@example.com`), and a SALESPERSON JWT with no linked `Salesperson` record — same seeded accounts and same `JWT_SECRET`-signing approach the Phase 4 and Phase 5/6 rounds already used successfully, no DB writes for auth itself.

**Independently recomputed the individual report's numbers against Phase 4/6's already-verified figures for Chidchanok Seethong, Q1 2026 (`QUARTER`/2026/1), live, as MANAGER**: `GET /reports/individual/:salespersonId` returned `composite.composite: 60.61` (`คิดจาก 3 จาก 5 เกณฑ์`), `REVENUE_VS_TARGET` `detail.actual: 773540` / `detail.target: 1800000` / `achievementPercent: 42.974444444444444`, `NEW_CUSTOMERS` `detail.actual: 12` / `detail.target: 15` (`score: 80`), `PRODUCT_GROUP` `score: 100` — an **exact match** to the figures Phase 4's FULL round independently hand-verified against raw `SalesLine`/`Target` data and Phase 5/6's round reproduced inside the AI payload. Generated a real `CoachingInsight` for the same salesperson/period afterward and confirmed the identical numbers appear a third time inside `kpiSnapshot.payload` (`revenue.actual: 773540`, `composite.score: 60.61`, `newCustomers.actual: 12`).

**Previous-period comparison checked for an off-by-one**: the same call returned `previousPeriod: {"periodType":"QUARTER","year":2025,"periodNumber":4}` for `period: {"periodType":"QUARTER","year":2026,"periodNumber":1}` — Q1 2026's previous period is correctly Q4 2025, not Q4 2026 or Q1 2025. This comes from `period.util.ts`'s `previousPeriod()`, unchanged since Phase 4/6 (byte-identical to the Phase 5/6 manifest's copy) and already used elsewhere for the same purpose (`coachingInsight.service.ts`'s `vsPreviousPeriod` clause) — `report.service.ts` calls the same function rather than reimplementing the adjacency rule, so there is exactly one place this logic could be wrong and it was already correct before this round. `previousComposite.composite` correctly came back `null` (`message: "ยังไม่มีคะแนนรวม..."`) since there's no `SalesLine`/`Target` data before 2026 in this dataset — not a bug, a genuine "no prior data" case, and the frontend's `formatScoreDelta()` correctly renders `"เทียบไม่ได้ (บางงวดยังไม่มีคะแนน)"` rather than a misleading `0`/blank delta for this case (confirmed by reading `reports/individual/page.tsx`'s delta-formatting logic against this exact response shape).

**Team-overview sort order verified against real data, independently cross-checked against a second endpoint**: `GET /reports/team-overview` for Q1 2026 returned all 12 salespeople, with the 3 who have any computable composite score correctly ascending first (`Chidchanok 60.61` → `Chutchataporn 95.38` → `Tasanee 100`) and the remaining 9 (all `composite: null`) appended after in the underlying `displayName`-alphabetical order the service starts from. Independently re-derived the same 12 composite values via a direct call to `GET /kpi/team` (Phase 4's endpoint) for the same period — **every score matched exactly**, confirming the report's sort isn't just internally consistent but numerically correct against a separately-computed source. This also matches Phase 5/6's already-verified `rank:null`-never-`0` pattern from the leaderboard (`team-overview`'s null-composite entries render `"-"` for rank on the frontend, never `0`).

**Both Excel exports downloaded and parsed programmatically, not just checked for HTTP 200**: `GET /reports/individual/:salespersonId/export` and `GET /reports/team-overview/export` (both as MANAGER, Q1 2026) returned `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` and correctly-encoded `Content-Disposition: attachment; filename="coaching-report-Miss%20Chidchanok%20Seethong-QUARTER-2026-1.xlsx"` / `filename="team-overview-QUARTER-2026-1.xlsx"` — matched the frontend's `CONTENT_DISPOSITION_FILENAME_PATTERN` regex in `lib/api.ts`'s `downloadFile()` exactly (verified by hand-tracing the regex against both header strings). Both files identified as real `Microsoft Excel 2007+` documents by the `file` command (not corrupt/empty streams), then opened programmatically with `exceljs` (reusing the leftover `qa-temp-parse-xlsx.ts` script) and the row contents read in full:
- Individual report sheet (`รายงานรายบุคคล`): header/period rows, `คะแนนรวม` row (`60.61`, `คิดจาก 3 จาก 5 เกณฑ์`), previous-period composite row (`N/A`, `คิดจาก 0 จาก 5 เกณฑ์` — correctly not `0`), a full 5-row KPI table with score/actual/target/หมายเหตุ per metric (values match the JSON response exactly, including `N/A` + the real "ข้อมูลยังไม่เพียงพอ..." reason text for `RETENTION`/`CONSISTENCY`), supplementary KPI rows (12 active, 0 churned, 1 avg product type), and the coaching insight's real `contentTh` text verbatim as the final row.
- Team-overview sheet (`ภาพรวมทีม`): header/sort-order note rows, then all 12 salespeople in the exact order confirmed above, ranked entries showing `1`/`2`/`3` + score + `computedFromLabel`, unranked entries showing `"-"` + `"N/A"` + the real Thai "ยังไม่มีคะแนนรวม..." message — an exact row-for-row match to the on-screen JSON.

**SALESPERSON-role live access to `/reports/*` — closed live this round** (previously flagged as code-inspected-only, not re-tested for Phase 7 specifically):
- SALESPERSON linked to Chidchanok, requesting Chidchanok's own individual report → `200`.
- SALESPERSON with **no linked `Salesperson` record at all**, requesting Chidchanok's individual report (someone else's) → `200` — confirms the view-everything model holds for reports too, not just for the salesperson's own data.
- Same unlinked SALESPERSON, requesting `/reports/team-overview` → `200`.
- No `Authorization` header at all → `401` (confirms `authenticate` middleware is actually active on this router, not just permissive by omission).

This matches the exact pattern already live-verified for `/kpi`, `/leaderboard`, and `/targets` in prior rounds — `report.routes.ts` uses the identical `router.use(authenticate, requirePasswordChanged)` with no `requireRole` call.

**Confirmed no duplicate KPI/insight calculation logic was introduced** — `report.service.ts` was read in full: its only calls into computation are `kpiService.computeCompositeScore`, `kpiService.computeSupplementaryKpis`, and (via `coachingInsight.service.ts`'s already-verified `METRIC_LABEL_TH` export, imported for labels only) no scoring math. `getIndividualReport`/`getTeamOverviewReport` do nothing but `await` these calls, shape the result object, and (for team-overview) filter+sort on the already-computed `composite.composite` value. `buildIndividualReportWorkbook`/`buildTeamOverviewWorkbook` only read already-computed fields onto Excel rows — no recomputation, no reimplementation of the renormalization or any of the 5 metric formulas. `coachingInsight.service.ts` itself was re-read in full this round (it changed by 7 bytes / 0 lines since the Phase 5/6 manifest — traced to no functional difference; every function body is identical) and confirmed the individual report **fetches** the existing `CoachingInsight` via a plain `findUnique` — it never calls `generateInsight()` — matching the plan's explicit intent that generation stays a deliberate user action, not something a report view triggers as a side effect. Live-confirmed this by requesting the individual report for a salesperson/period with no existing insight first (`coachingInsight: null` in the response, no new row created) before generating one and re-requesting (now returns the real insight, still no second row created — confirmed by DB query, only one row for that `salespersonId+periodType+year+periodNumber` throughout).

**`exceljs` dependency confirmed reused, not newly added** — `backend/package.json` has exactly one `"exceljs"` entry (`^4.4.0`), the same one Phase 2's import feature already depends on; no duplicate or second package.

**Schema/Data Model contract check**: `backend/prisma/schema.prisma` is byte-for-byte and line-for-line identical to the copy the Phase 5/6 FULL round already checked field-by-field against `design.md`'s Data Model (8707 bytes / 308 lines, unchanged) — confirming Module H genuinely introduced **zero** new models/fields/relations, matching `design.md`'s own "Models: อ่านอย่างเดียว" (read-only) entry for Module H exactly. No drift to report.

**Cleanup**: the orphaned `CoachingInsight` row from the interrupted prior attempt (Chidchanok, `YEAR`/2026/0) and the one `CoachingInsight` row this round generated for testing (Chidchanok, `QUARTER`/2026/1) were both deleted after use — confirmed by a follow-up query that only the one pre-existing engineer self-test row (`sp-test-phase6@example.com`'s self-test, Chidchanok/`MONTH`/2026/6, already known from the Phase 5/6 round) remains. The three `qa-temp-*.ts` scripts in `backend/src/` were **not** deleted (see Open Issues — this round's tooling didn't permit a file delete; they are reused artifacts, not new ones this round created). Both dev servers (backend :4000, frontend :3000) were stopped at the end.

**What this round does not cover** (same limitation as every prior round): actual rendered-viewport mobile-responsive behavior for the two new report pages — no browser automation tool was available. Code inspection shows the same `p-4 sm:p-6`/`flex-wrap`/`max-w-5xl` mobile-first pattern already accepted elsewhere, but that is not the same as a rendered check. `plan.md`'s Phase 7 task list has no dedicated checkbox for this (unlike Phase 5's), so nothing is left unchecked for it, but the underlying gap is real and is carried in Open Issues.

## Verified File Manifest — Phase 7

Files inspected in this FULL round, with size and line count, so a future round can tell what moved. Files already covered by the Phase 5/6 manifest are re-listed here only where they were re-read or their size/line count changed this round.

| File | Bytes | Lines | Round |
|---|---:|---:|---|
| `backend/src/services/report.service.ts` | 7978 | 204 | Phase 7 FULL (new) |
| `backend/src/controllers/report.controller.ts` | 2587 | 60 | Phase 7 FULL (new) |
| `backend/src/routes/report.routes.ts` | 1381 | 42 | Phase 7 FULL (new) |
| `backend/src/services/period.util.ts` | 1851 | 65 | Phase 7 FULL (re-read, unchanged since Phase 5/6) |
| `backend/src/services/coachingInsight.service.ts` | 12423 | 292 | Phase 7 FULL (re-read in full; 7-byte diff from Phase 5/6 manifest traced to no functional change — every function body identical) |
| `backend/src/utils/jwt.ts` | 675 | 22 | Phase 7 FULL (re-read) |
| `backend/src/middleware/authenticate.ts` | 1720 | 55 | Phase 7 FULL (re-read) |
| `backend/prisma/schema.prisma` | 8707 | 308 | Phase 7 FULL (re-confirmed byte-identical to Phase 5/6 manifest — zero schema drift) |
| `backend/package.json` | 1220 | 45 | Phase 7 FULL (confirmed single `exceljs` entry, reused not duplicated) |
| `frontend/app/(protected)/reports/individual/page.tsx` | 8991 | 194 | Phase 7 FULL (new) |
| `frontend/app/(protected)/reports/team-overview/page.tsx` | 4625 | 110 | Phase 7 FULL (new) |
| `frontend/lib/api.ts` | 14248 | 429 | Phase 7 FULL (grew from Phase 5/6's 12131/376 — `getIndividualReport`/`getTeamOverviewReport`/`downloadFile`/`exportIndividualReport`/`exportTeamOverviewReport` added) |
| `frontend/lib/types.ts` | 12926 | 462 | Phase 7 FULL (grew from Phase 5/6's 11929/435 — `IndividualReportData`/`TeamOverviewEntry`/`TeamOverviewData` added, checked field-for-field against the real backend response shapes) |
| `frontend/lib/kpiLabels.ts` | 4007 | 104 | Phase 7 FULL (grew from Phase 5/6's 3658/97 — `SCORED_METRIC_ORDER`/`periodLabelTh` added, additive only, all Phase 5/6 exports unchanged) |
| `frontend/components/NavBar.tsx` | 3003 | 76 | Phase 7 FULL (grew from Phase 5/6's 2835/74 — 2 report links added) |
| `frontend/components/kpi/PeriodSelector.tsx` | 2807 | 89 | Phase 7 FULL (re-read in full, unchanged; confirms MONTH/QUARTER/YEAR all selectable) |
| `frontend/components/kpi/ScoreCard.tsx` | 2573 | 59 | Phase 7 FULL (byte-identical to Phase 5/6 manifest — confirmed genuine reuse, not a fork) |
| `frontend/components/kpi/CompositeScoreBadge.tsx` | 2013 | 51 | Phase 7 FULL (confirmed reused via import, not previously in a manifest — Phase 4 artifact) |
| `frontend/components/coaching/CoachingInsightPanel.tsx` | 6142 | 143 | Phase 7 FULL (byte-identical to Phase 5/6 manifest — confirmed genuine reuse) |
| `frontend/components/dashboard/RevenueTargetProgress.tsx` | 2336 | 54 | Phase 7 FULL (byte-identical to Phase 5/6 manifest — confirmed genuine reuse) |
| `frontend/components/dashboard/SalespersonSwitcher.tsx` | 774 | 28 | Phase 7 FULL (byte-identical to Phase 5/6 manifest — confirmed genuine reuse) |
| `frontend/components/kpi/SupplementaryKpisPanel.tsx` | 5113 | 107 | Phase 7 FULL (confirmed reused via import, not previously in a manifest — Phase 4 artifact) |
| `frontend/components/kpi/KpiDrillDownModal.tsx` | 4690 | 107 | Phase 7 FULL (confirmed reused via import, not previously in a manifest — Phase 4 artifact) |

## Per-Task Results — Phase 7 (this round)

- [x] Verified — `[backend]` `GET /reports/individual/:salespersonId` — `report.service.ts#getIndividualReport`, `report.controller.ts`. Live-tested for Chidchanok Seethong Q1 2026: target vs actual for every metric, composite (`60.61`), previous-period composite (correctly `null`/Q4 2025), and the `CoachingInsight` fetched (not generated) — see Verification Summary for full figures, all matching Phase 4/6's already-verified numbers exactly. 404s on an unknown `salespersonId` (`findSalespersonOrNull` check in the controller).
- [x] Verified — `[backend]` `GET /reports/team-overview` — `report.service.ts#getTeamOverviewReport`. Live-tested: ascending sort by composite for all computable salespeople, non-computable appended after — cross-checked independently against `GET /kpi/team` for the same period, exact match on all 12 scores.
- [x] Verified — `[backend]` `exceljs` dependency — `backend/package.json`, single entry, reused from Phase 2 (`^4.4.0`), no duplicate.
- [x] Verified — `[backend]` `GET /reports/individual/:salespersonId/export` — `report.service.ts#buildIndividualReportWorkbook`, `report.controller.ts#exportIndividualReport`. Downloaded and parsed programmatically; real `.xlsx`, correct `Content-Type`/`Content-Disposition`, sheet `รายงานรายบุคคล` with row-for-row content matching the JSON report exactly.
- [x] Verified — `[backend]` `GET /reports/team-overview/export` — `report.service.ts#buildTeamOverviewWorkbook`, `report.controller.ts#exportTeamOverviewReport`. Downloaded and parsed programmatically; real `.xlsx`, correct headers, sheet `ภาพรวมทีม` with row-for-row content matching the on-screen sort order exactly.
- [x] Verified — `[frontend]` หน้ารายงานรายบุคคล — `app/(protected)/reports/individual/page.tsx`. Target vs actual (`RevenueTargetProgress`, reused), all KPI (`ScoreCard`, reused), composite (`CompositeScoreBadge`, reused), strengths/weaknesses (`CoachingInsightPanel`, reused, correctly passed `canGenerate` matching the same MANAGER-any/SALESPERSON-own-only rule already server-enforced), previous-period comparison with correctly-handled null-previous-composite case, period selectable via `PeriodSelector` (reused, MONTH/QUARTER/YEAR all present) — confirmed via full code read and by independently exercising the exact API calls the page makes.
- [x] Verified — `[frontend]` หน้าภาพรวมทีม — `app/(protected)/reports/team-overview/page.tsx`. Renders the ascending-sorted list with rank `"-"` (never `0`) for non-computable entries and the real reason message, period-switchable, matches `requirement.md`'s "ดูว่าใครควรได้รับการช่วยเหลือก่อน" intent. Not MANAGER-gated in code — consistent with the app's established view-everything model (confirmed live: SALESPERSON gets `200`), not a mismatch against `requirement.md`/`design.md`, which never specify a MANAGER-only restriction for this page and instead establish "ทุก role เห็นข้อมูลธุรกิจเท่ากันหมด" as the general rule.
- [x] Verified — `[frontend]` ปุ่ม Export Excel (both pages) — `lib/api.ts#downloadFile`/`exportIndividualReport`/`exportTeamOverviewReport`. Correctly bypasses the JSON `request()` helper, reads `Content-Disposition` via a regex hand-traced against both real header strings this round, triggers a real `<a download>` click, revokes the object URL after. Loading/error states (`exporting`/`exportError`) present on both pages.

## Design/requirement contract checks — Phase 7

- **Data Model**: zero new models/fields — `schema.prisma` confirmed byte-identical to the copy already checked field-by-field against `design.md`'s Data Model in the Phase 5/6 round. Matches `design.md`'s own "Models: อ่านอย่างเดียว" entry for Module H.
- **`requirement.md` Core Feature 8** ("สรุปรายบุคคล 1 หน้าต่อ 1 คนต่อ 1 รอบ: เป้า vs ผลจริง, KPI ทุกตัว, คะแนนรวม, จุดแข็ง/จุดที่ควรพัฒนา, แนวโน้มเทียบรอบก่อน") — confirmed live, all five elements present and correct in both the JSON response and the exported `.xlsx`.
- **`requirement.md` Core Feature 8** ("ภาพรวมทั้งทีมในหน้าเดียว เพื่อดูว่าใครควรได้รับการช่วยเหลือก่อน") — confirmed: ascending-by-composite sort puts the lowest scorer first, matching "ควรได้รับการช่วยเหลือก่อน" (needs help first) as the ordering intent, not a raw alphabetical or arbitrary list.
- **`requirement.md` Core Feature 8 / Open Question 6** ("Export ออกเป็นไฟล์ได้...PDF หรือ Excel หรือทั้งสองอย่าง") — `design.md` locked this to **Excel only**, explicitly to avoid Thai-font/headless-browser PDF problems on the deploy target. Confirmed: only `.xlsx` export exists anywhere in the code, no PDF generation attempted — implements the locked decision, not the original open-ended question.
- **View-everything / edit-restricted role model**: confirmed live in both directions across all 4 `/reports/*` endpoints (fully open to both roles, no write surface on this module at all) — consistent with the pattern already verified in Phases 1-6.
- **`design.md`'s KPI & Scoring Rules "AI coaching" clause**, re-applied to Module H: the individual report must show the coaching text **and** fall back gracefully when none exists yet — confirmed live (`coachingInsight: null` case renders "ยังไม่ได้สร้างสรุปจุดแข็ง/จุดที่ควรพัฒนาสำหรับงวดนี้" in the export, and the frontend's `CoachingInsightPanel` handles the same null case independently, already verified in Phase 5/6).
- **No duplicate KPI/insight calculation logic** — confirmed by a full read of `report.service.ts` and a re-read of `coachingInsight.service.ts`; every number in both report endpoints and both exports traces back to a single call into `kpi.service.ts`'s already-verified functions.
- **Previous-period adjacency** — confirmed correct (Q1 2026 → Q4 2025) via `period.util.ts#previousPeriod`, unchanged and reused rather than reimplemented.

## Issues Found — Phase 7

No implementation bugs were found. Every item the brief specifically asked to scrutinize (recomputed numbers vs Phase 4/6, team-overview sort order against real data, real downloaded/parsed Excel content, the SALESPERSON-role access gap, absence of duplicate KPI/insight logic, previous-period adjacency) held up under live testing. Two carried-forward, non-blocking items surfaced or were newly found this round, neither an implementation bug:

1. **Mobile-responsive layout for the two new report pages still not visually verified** — same tooling gap as every prior round (no browser automation available). Code inspection shows the same accepted mobile-first Tailwind pattern used elsewhere. See Open Issues.
2. **Three leftover `qa-temp-*.ts` scripts in `backend/src/`** from the previously-interrupted attempt — reused rather than recreated this round, could not be deleted under this round's tooling. Not application code, not imported by any route, zero functional impact, but should be removed by whoever next has file-delete permission. See Open Issues.

## Review Outcome — Phase 7

**Phase 7 (Coaching Reports & Export, Module H) — FULL round, 8/8 tasks ✅ Verified, zero implementation defects. All `plan.md` boxes checked.**

Module H needs no `security` pass of its own — it is read-only, composes only already-computed/already-validated numbers from Phases 4 and 6, introduces no new write surface, and `design.md` does not flag it `⚠️ Sensitive`. Pending the user's decision (see below).

**This is the last implementation phase in the module's plan.** All 7 phases now have at least one completed verification round. What "ready for `devops`" actually requires beyond this is covered in the full-module summary delivered alongside this round — Phase 7 being clean does not, by itself, clear the module for deploy.

*(Note added by the 2026-08-16 TARGETED round on Phase 6: the item-2 leftover `qa-temp-*.ts` scripts above were deleted by the engineer before that round started — confirmed by `Glob`, no trace remains. That Open Issues row was closed as part of the 2026-08-16 round; see `review.md`.)*
