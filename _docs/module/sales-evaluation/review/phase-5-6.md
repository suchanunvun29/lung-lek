# Phase 5 (Dashboards & Leaderboard, Module F) & Phase 6 (AI Coaching Insights, Module G) — FULL round

This archive preserves, verbatim, the "Verification Summary (current round)", "Verified File Manifest — Phase 5 & 6", "Per-Task Results — Phase 5", "Per-Task Results — Phase 6", "Design/requirement contract checks — Phase 5 & 6", "Issues Found — Phase 5 & 6", and "Review Outcome — Phase 5 & 6" sections that were live in `review.md` as of the 2026-08-15 FULL round, before Phase 7's round superseded them as the current round.

## Verification Summary (as recorded that round)

**FULL round** — Phase 5 (Dashboards & Leaderboard, Module F) and Phase 6 (AI Coaching Insights, Module G), both verified from scratch for the first time. This is the right mode per this file's own gate: neither phase has been verified before, and Phase 6 is `⚠️ Sensitive` per `design.md` and will need a `security` pass before deploy — a FULL round is required before that handoff regardless of how clean the result looks.

Read `plan.md` (Phase 5/6 blocks, Plan Summary, Sequencing Notes, Unresolved Open Questions), `design.md` (Data Model, KPI & Scoring Rules, Modules F/G, Risks, Open Questions), `requirement.md` (Core Features 5-7, Scope, Constraints, Open Questions 1-2), and `_docs/status.md` before starting, per the standard procedure.

Both `backend/` and `frontend/` pass `npm run typecheck`, `npm run lint`, and `npm run build` clean (frontend build confirms `/dashboard` and `/leaderboard` compile as routes, alongside every other existing route — no regressions from Phase 5/6 elsewhere in the app).

Both dev servers were started (`backend` :4000, `frontend` :3000) and driven live against the real dev DB. Since no working manager/salesperson password was available at the start of this round (same recurring blocker as prior rounds), a short-lived MANAGER JWT and two SALESPERSON JWTs were signed directly with the app's own `JWT_SECRET` (read from `backend/.env`, no DB write) via a temporary script placed in `backend/src/` and a one-off `node -e` invocation, both deleted before this round ended.

**Confirmed no duplicate KPI calculation logic was introduced in either phase** — this was specifically scrutinized per the brief, given how extensively Phase 4's formulas were hand-verified. `leaderboard.controller.ts` imports and calls `kpi.service.ts`'s `computeCompositeScore`/`computeRevenueVsTarget`/`getNewCustomerActualCount` directly; `coachingInsight.service.ts` imports and calls `computeCompositeScore`/`computeSupplementaryKpis`/`computeRevenueVsTarget` directly. Neither reimplements any of the 5 metric formulas or the composite renormalization. The only "new" math in either phase is trivial aggregation over already-computed scores (a team-average of `composite.composite` values in `coachingInsight.service.ts`, and a per-metric team-average in the frontend's `computeTeamAverageScores` for `ScoreCard`'s `teamAverages` prop) — neither touches the underlying KPI formulas themselves.

**Cross-checked `GET /kpi/:salespersonId` numbers against Phase 4's already-QA-verified numbers, live, this round**: generating a coaching insight for Miss Chidchanok Seethong (Q1 2026) reproduced `revenue.actual: 773540`, `achievementPercent: 42.97444444444444`, `composite.score: 60.61`, `newCustomers.actual: 12` inside the AI payload — an exact match to the figures Phase 4's FULL round independently hand-verified against raw `SalesLine`/`Target` data. The `PERCENT_TARGET` leaderboard criteria for the same salesperson/period independently returned the identical `42.974444444444444`.

**Leaderboard — live-tested all 4 criteria** (`COMPOSITE`/`PERCENT_TARGET`/`REVENUE`/`NEW_CUSTOMERS`) for Q1 2026 against the real 12-salesperson dataset, as both a MANAGER and a SALESPERSON token:
- `COMPOSITE` and `PERCENT_TARGET` correctly rank only the salespeople with a computable value (2 of 12 have any `Target` set for Q1 2026 at all) and give everyone else `rank: null` (never `0`) with the correct Thai reason text (`"ยังไม่มีคะแนนรวม เนื่องจากยังไม่มีเกณฑ์ใดคำนวณได้ในงวดนี้"` / `"ยังไม่ได้ตั้งเป้า หรือเป้าเป็น 0"`) — **the rank:null-never-0 rule holds for real zero-computable-metric salespeople**, not just a constructed edge case.
- `PERCENT_TARGET` showed Mrs. Tasanee Angsawothai at `350.7115%` — genuinely uncapped, distinct from the same salesperson's `COMPOSITE` score of `100` (capped, since `REVENUE_VS_TARGET`'s *score* is `min(achievement,100)` before it's fed into the composite) — confirming the claimed distinction is real, not just present in the code.
- `REVENUE` and `NEW_CUSTOMERS` correctly rank all 12 salespeople (these are raw actuals, not target-relative, so they're always computable regardless of whether a target exists) — descending order confirmed correct for all 12 in both criteria.
- Zod validation live-tested: `criteria=BOGUS` → `400` with the exact enum-mismatch field error.
- SALESPERSON token → `200` on `/leaderboard` (no role gate, matching the view-everything model), confirmed for `COMPOSITE`.

**AI Coaching (Module G) — anonymization scrutinized specifically per the brief, live, not just read.** Generated a real insight for Chidchanok (Q1 2026, `aiAnonymize: true` — the seeded default): the stored `kpiSnapshot.payload` showed `salesperson.name: "พนักงานขาย A"` and `supplementary.topHospitalsByRevenue[].hospitalName` replaced with `"โรงพยาบาล 1"`..`"โรงพยาบาล 5"` (5 real hospitals, all replaced, none left as their real name). Read the full `KpiSummaryPayload` interface and `buildKpiSummaryPayload`/`anonymizePayload` in `coachingInsight.service.ts` line by line: the only two salesperson/hospital-name-carrying fields in the entire payload are `salesperson.name` and `supplementary.topHospitalsByRevenue[].hospitalName`, both anonymized; every other field is either a metric enum, a score, a count, or a Thai reason string — no raw `SalesLine`, `invoiceNo`, or `Product`/`ProductType` name anywhere in the payload. Also live-confirmed the payload correctly has **no crash and a sensible fallback** for a salesperson with zero computable metrics (Miss Chutchataporn Sanamthong, no `Target` set at all): `contentTh: "งวดนี้ยังไม่ได้ตั้งเป้ายอดขาย จึงยังสรุปเทียบเป้าไม่ได้"`, still anonymized (`salesperson.name: "พนักงานขาย A"`).

**Permission model — MANAGER-can-generate-for-anyone / SALESPERSON-own-only — live-tested on the backend directly (not just the frontend gate), all 4 combinations**:
- MANAGER generating for any salesperson → `201`, succeeds (tested against 2 different salespeople).
- SALESPERSON (linked via `Salesperson.userId` to Chidchanok) generating their **own** insight → `201`, succeeds.
- The same SALESPERSON generating **someone else's** insight → `403 {"error":"พนักงานขายสร้างสรุปได้เฉพาะของตัวเองเท่านั้น"}`.
- A different SALESPERSON with **no linked `Salesperson` record at all** generating for anyone → `403`, same message (the `salesperson.userId !== req.user.id` check correctly never matches when `userId` is `null`).
- `GET /coaching-insights/:salespersonId` open to all roles for any salesperson (view-everything model) — confirmed `200` for a SALESPERSON reading a different salesperson's insight.

This is exactly the enforcement the brief asked to scrutinize, and it holds consistently across all 4 cases — on the server, not just hidden behind a disabled frontend button (the frontend's `canGenerateInsight` logic in `dashboard/page.tsx` independently mirrors the same rule, but the backend was tested directly with raw `curl` calls bypassing the UI entirely).

**`aiEnabled` switch — live-tested.** Toggled `EvaluationSetting.aiEnabled` to `false` via a real `PATCH /settings/evaluation` (MANAGER token), generated an insight → `status: "SUCCESS"`, `provider: "rule-based"`, `kpiSnapshot.sentToGemini: false` — confirmed Gemini was never called (immediate response, no 15s timeout wait). Restored `aiEnabled: true` immediately after, confirmed via `GET /settings/evaluation`.

**Fallback path (the only live-testable AI path, since `GEMINI_API_KEY` is empty in `backend/.env`) — live-tested.** With `aiEnabled: true` and an empty key, `callGemini()` throws before making any network request (`"GEMINI_API_KEY ไม่ได้ตั้งค่าไว้"`); `generateInsight()` catches this, sets `status: "FAILED"`, `errorMessage` populated, and `contentTh` falls back to `buildRuleBasedSummary()`'s real Thai sentence built from the same numeric payload — never blank. Confirmed the frontend's `CoachingInsightPanel.tsx` renders this correctly: an amber "เรียก AI ไม่สำเร็จ...แสดงสรุปที่คำนวณจากกฎแทน" notice plus the actual fallback sentence, and confirmed the rest of the dashboard (`RevenueTargetProgress`, `MonthlyTrendChart`, `BreakdownPieChart`, `ScoreCard`) renders unconditionally regardless of the coaching panel's status — a broken/failed AI call cannot blank the rest of the page. **The Gemini-success path itself is structurally implemented but genuinely untestable this round** (no real key available) — this is an inherent limitation of this round, not a defect: `gemini.service.ts`'s `callGemini()` was read in full and correctly implements a plain `fetch` POST to the Gemini `generateContent` endpoint with a 15s `AbortController` timeout, correct request/response shape handling, and error messages that don't leak the API key.

**`isStale` — partially live-tested.** The "cleared by generate" half was live-verified: manually set `isStale: true` on an existing `CoachingInsight` row via a scoped, single-row Prisma write, confirmed `true` via `GET`, then called the real `POST .../generate` endpoint and confirmed `isStale: false` in the response — matching `coachingInsight.service.ts`'s `upsert` (`isStale: false` in both `create` and `update` branches). The "set to `true` by a fresh import touching that period" half (`import.service.ts:448-479`'s `markInsightsStaleForTouchedPeriods`, unchanged since Phase 2) was **verified by code inspection only** this round — a real end-to-end test (upload the sample `.xlsx` file via `POST /import`, confirm the flag flips) was attempted twice (once via `curl -F`, once via a Node `fetch`/`FormData` script) but both attempts were blocked by this session's auto-mode permission classifier before reaching the server. This is a genuine live-coverage gap for this round, not a suspected code defect — the code itself matches the exact logic Phase 2's FULL round already read and reasoned about (period-key computation for `MONTH`/`QUARTER`/`YEAR`, `updateMany` on `CoachingInsight.isStale`), and the fields it touches haven't changed since. See Open Issues above.

Cleanup: all 3 QA-created `CoachingInsight` rows (Chidchanok Q1-2026/M2-2026, Chutchataporn Q1-2026) were deleted after testing, confirmed via a follow-up query that only the one pre-existing engineer self-test row (`sp-test-phase6@example.com`'s self-test, Chidchanok/MONTH/2026/6) remains. `EvaluationSetting.aiEnabled` confirmed restored to `true`. The temporary JWT-signing script (`backend/src/qa-temp-inspect.ts`) was deleted before this round ended, along with the scratch Node script used for the blocked import-upload attempt (never reached the server, no server-side trace). Both dev servers (backend :4000, frontend :3000) were stopped at the end.

## Verified File Manifest — Phase 5 & 6

Files inspected in this FULL round, with size and line count, so a future TARGETED round can tell what moved.

| File | Bytes | Lines | Round |
|---|---:|---:|---|
| `backend/src/routes/leaderboard.routes.ts` | 706 | 16 | Phase 5/6 FULL |
| `backend/src/controllers/leaderboard.controller.ts` | 2353 | 61 | Phase 5/6 FULL |
| `backend/src/validators/leaderboard.validators.ts` | 817 | 25 | Phase 5/6 FULL |
| `backend/src/routes/kpi.routes.ts` | 1168 | 35 | Phase 5/6 FULL |
| `backend/src/controllers/kpi.controller.ts` | 2495 | 68 | Phase 5/6 FULL |
| `backend/src/services/kpi.service.ts` | 24149 | 663 | Phase 5/6 FULL |
| `backend/src/services/coachingInsight.service.ts` | 12416 | 292 | Phase 5/6 FULL |
| `backend/src/services/gemini.service.ts` | 2101 | 57 | Phase 5/6 FULL |
| `backend/src/controllers/coachingInsight.controller.ts` | 2094 | 51 | Phase 5/6 FULL |
| `backend/src/routes/coachingInsight.routes.ts` | 1190 | 30 | Phase 5/6 FULL |
| `backend/src/validators/coachingInsight.validators.ts` | 454 | 14 | Phase 5/6 FULL |
| `backend/src/services/import.service.ts` | 23617 | 699 | Phase 5/6 FULL (re-read for `isStale` marking) |
| `backend/src/services/period.util.ts` | 1851 | 65 | Phase 5/6 FULL |
| `backend/prisma/schema.prisma` | 8707 | 308 | Phase 5/6 FULL |
| `frontend/app/(protected)/dashboard/page.tsx` | 6554 | 155 | Phase 5/6 FULL |
| `frontend/app/(protected)/leaderboard/page.tsx` | 4032 | 99 | Phase 5/6 FULL |
| `frontend/components/dashboard/SalespersonSwitcher.tsx` | 774 | 28 | Phase 5/6 FULL |
| `frontend/components/dashboard/RevenueTargetProgress.tsx` | 2336 | 54 | Phase 5/6 FULL |
| `frontend/components/dashboard/MonthlyTrendChart.tsx` | 1741 | 41 | Phase 5/6 FULL |
| `frontend/components/dashboard/BreakdownPieChart.tsx` | 2108 | 56 | Phase 5/6 FULL |
| `frontend/components/kpi/ScoreCard.tsx` | 2573 | 59 | Phase 5/6 FULL (re-read, `teamAverages` prop added) |
| `frontend/components/coaching/CoachingInsightPanel.tsx` | 6142 | 143 | Phase 5/6 FULL |
| `frontend/components/NavBar.tsx` | 2835 | 74 | Phase 5/6 FULL |
| `frontend/lib/api.ts` | 12131 | 376 | Phase 5/6 FULL |
| `frontend/lib/types.ts` | 11929 | 435 | Phase 5/6 FULL |
| `frontend/lib/kpiLabels.ts` | 3658 | 97 | Phase 5/6 FULL |
| `frontend/lib/coachingLabels.ts` | 891 | 26 | Phase 5/6 FULL |
| `frontend/components/settings/EvaluationSettingForm.tsx` | 4478 | 104 | Phase 5/6 FULL (re-confirmed reuse, unchanged since Phase 4) |
| `frontend/package.json` | 616 | 29 | Phase 5/6 FULL |

## Per-Task Results — Phase 5 (this round)

- [x] Verified — `[backend]` `GET /kpi/:salespersonId/trend` "if not already covered by Phase 4" — correctly resulted in **no new endpoint**: `kpi.routes.ts`/`kpi.controller.ts` have no `/trend` route, and `computeSupplementaryKpis`'s `monthlyRevenueTrend` field (`kpi.service.ts:488-494`) already returns 12 trailing months of revenue, consumed directly by `MonthlyTrendChart` via `kpi.supplementary.monthlyRevenueTrend`. The condition in the task ("ถ้ายังไม่ครอบคลุม") was false, so no code was the correct outcome, not a gap.
- [x] Verified — `[backend]` `GET /leaderboard` — `leaderboard.routes.ts`, `leaderboard.controller.ts`. Live-tested all 4 criteria for both roles, correct descending sort, `rank: null` (never `0`) for non-computable entries — see Verification Summary for full detail including the real uncapped-vs-capped `PERCENT_TARGET`/`COMPOSITE` distinction.
- [x] Verified — `[backend]` Zod schema for `/leaderboard` query — `leaderboard.validators.ts`. Reuses `kpi.validators.ts`'s `basePeriodFields`/`periodNumberIsValid`/`normalizePeriodNumber` rather than duplicating period validation; adds only the `criteria` enum. Live-tested `criteria=BOGUS` → `400`.
- [x] Verified — `[frontend]` `recharts` dependency — `frontend/package.json` (`"recharts": "^3.10.1"`).
- [x] Verified — `[frontend]` Personal dashboard — `app/(protected)/dashboard/page.tsx`, `RevenueTargetProgress.tsx`. Shows actual/target/`"เหลืออีก X บาท ถึงเป้า"` (or `"เกินเป้า"` when over), progress bar capped visually at 100%, correct `"คำนวณไม่ได้"` fallback when `REVENUE_VS_TARGET` isn't computable.
- [x] Verified — `[frontend]` Monthly trend chart (`recharts`) — `MonthlyTrendChart.tsx`. `ResponsiveContainer` + `LineChart`, Thai-locale-formatted axis/tooltip, empty-state handled.
- [x] Verified — `[frontend]` Product-group / hospital breakdown charts — `BreakdownPieChart.tsx` (used twice on the dashboard). Correctly groups long tails into `"อื่น ๆ"` past 7 slices, filters zero-value slices, Thai-locale tooltip.
- [x] Verified — `[frontend]` KPI-vs-team-average component (reuse `ScoreCard` from Phase 4) — `ScoreCard.tsx`'s new `teamAverages?: Partial<Record<ScoredKpiMetric, number>>` prop, populated by `kpiLabels.ts`'s `computeTeamAverageScores` from a real `GET /kpi/team` call. Genuinely reused (same component, extended with a prop), not duplicated. `computeTeamAverageScores` correctly averages only over salespeople where that specific metric is computable, not treating non-computable as `0`.
- [x] Verified — `[frontend]` Salesperson switcher (any role can view anyone) — `SalespersonSwitcher.tsx`. Plain `<select>` listing every salesperson, no role restriction on switching (matches `requirement.md`'s "สลับดูมุมมองของพนักงานขายคนอื่นได้ (ไม่มีการปิดบัง)").
- [x] Verified — `[frontend]` Leaderboard page (4 criteria + period switch) — `app/(protected)/leaderboard/page.tsx`. All 4 criteria buttons, `PeriodSelector` reused from Phase 4, medal/rank display correctly shows `"-"` (never `0`) for `rank: null` entries with the reason text shown instead. Live-tested end-to-end against the real backend for all 4 criteria.
- [ ] Partial — `[frontend]` Responsive layout for Dashboard/Leaderboard on mobile — code shows correct mobile-first Tailwind patterns throughout (`grid-cols-1` default + `lg:grid-cols-2` on the dashboard's breakdown charts, `flex-wrap` on every button row/header, `p-4 sm:p-6` container padding, `max-w-5xl`/`max-w-3xl` centered containers), consistent with the same pattern already accepted on Phases 1-4's pages. **Not visually confirmed at an actual rendered mobile viewport** — no browser automation tool was available this round. Left unchecked in `plan.md` and routed to a future `qa-engineer` round with viewport tooling, per Open Issues above.

## Per-Task Results — Phase 6 (this round)

- [x] Verified — `[backend]` Service ประกอบ payload สรุป KPI — `coachingInsight.service.ts`'s `buildKpiSummaryPayload`. Composes exclusively from `kpi.service.ts` outputs (`computeCompositeScore`, `computeSupplementaryKpis`, `computeRevenueVsTarget`) — no raw `SalesLine` row anywhere in the `KpiSummaryPayload` interface or its construction. Confirmed live: the stored `kpiSnapshot.payload` for a real generation contains only scores/counts/labels, never an invoice number, product name, or lot.
- [x] Verified — `[backend]` Anonymization service — `anonymizePayload`. Replaces `salesperson.name` with the constant `"พนักงานขาย A"` and each distinct hospital name in `topHospitalsByRevenue` with a per-request-sequential `"โรงพยาบาล N"` label, unconditionally (not just a happy path — also confirmed correct for the zero-computable-metrics case, where the payload still anonymizes correctly before returning the "not enough data" fallback). Read every field of `KpiSummaryPayload` to confirm these are the *only* two name-carrying fields in the whole structure — no leak found anywhere else in the payload.
- [x] Verified — `[backend]` Gemini API call with timeout + `kpiSnapshot` audit trail — `gemini.service.ts`'s `callGemini` (15s `AbortController` timeout, correctly aborts and throws a clear Thai timeout message rather than hanging), `coachingInsight.service.ts` stores `{ payload: outboundPayload, sentToGemini }` into `kpiSnapshot` on every generation, success or failure. Minor observation (not a defect, flagged for `security` in Open Issues): `sentToGemini` is only `true` after a successful response, so a real-key-but-late-network-failure case would still read `false` even though the request body may have left the process.
- [x] Verified — `[backend]` Rule-based fallback on Gemini failure — `buildRuleBasedSummary`. Live-tested with the real empty `GEMINI_API_KEY`: `status: "FAILED"`, `errorMessage` populated with a real (non-leaking) message, `contentTh` is a genuine multi-clause Thai sentence built from the same numeric payload, never blank. Also live-tested the zero-computable-metrics case (no crash, sensible "ยังไม่ได้ตั้งเป้ายอดขาย" fallback) and the composite-vs-team-average / best-vs-worst-metric / trend-vs-previous-period clauses all fire correctly when their preconditions are met (confirmed via the Chidchanok Q1-2026 generation, which had all of these preconditions true).
- [x] Verified — `[backend]` `POST /coaching-insights/:salespersonId/generate` — `coachingInsight.controller.ts`. Permission enforcement (MANAGER any / SALESPERSON own-only) **live-tested on the server directly across all 4 combinations** — see Verification Summary. Respects the `salespersonId+periodType+year+periodNumber` unique constraint via `upsert` (regenerating the same period updates the same row rather than creating a duplicate — confirmed by re-generating Chidchanok's Q1-2026 insight multiple times during testing and confirming the `id` stayed the same each time). Clears `isStale` — live-confirmed (see Verification Summary; the import-side "sets `isStale`" half was code-inspection-only this round, see Open Issues).
- [x] Verified — `[backend]` `GET /coaching-insights/:salespersonId` — `coachingInsight.controller.ts`. Open to all roles (live-confirmed 200 for a SALESPERSON reading someone else's insight), 404s on unknown `salespersonId`, returns `null` `insight` (not an error) when none exists yet for the period.
- [x] Verified — `[backend]` `aiEnabled` switch — live-tested: toggling it off via a real `PATCH /settings/evaluation` and generating produced `status: "SUCCESS"`, `provider: "rule-based"`, `sentToGemini: false`, with no call attempt to Gemini (immediate response). Restored after.
- [x] Verified — `[backend]` `GEMINI_API_KEY` in `.env`, never to client — `gemini.service.ts` reads it via `process.env.GEMINI_API_KEY` only; confirmed by grepping the whole repo that the key is referenced nowhere else, and by inspecting every coaching-insight response body live (no `apiKey`/`GEMINI_API_KEY` field present anywhere in the JSON returned to the client).
- [x] Verified — `[backend]` Zod schema for generate body — `coachingInsight.validators.ts`'s `generateInsightBodySchema`, reuses `kpi.validators.ts`'s period fields rather than duplicating validation logic.
- [x] Verified — `[frontend]` Coaching panel (`contentTh`/status/`isStale` badge) — `CoachingInsightPanel.tsx`. Shows `contentTh`, a status badge (`INSIGHT_STATUS_LABEL_TH`/`INSIGHT_STATUS_BADGE_CLASS` from `coachingLabels.ts`), and the `isStale` badge with the exact Thai text from `design.md`'s intent ("ข้อมูลอัปเดตแล้ว — กด \"สร้างใหม่\" เพื่อรีเฟรชสรุป").
- [x] Verified — `[frontend]` "สร้างใหม่" (regenerate) button — calls `generateCoachingInsight` → `POST /coaching-insights/:salespersonId/generate`, live-confirmed round-trips correctly and updates the panel's state from the response (not a page reload).
- [x] Verified — `[frontend]` Loading/error state, `FAILED` fallback — live-confirmed: `status === "FAILED"` renders an amber notice **plus** the real `contentTh` fallback content underneath it (never blank), consistent with `design.md`'s "หน้าจอต้องยังแสดง KPI ปกติได้...ห้ามให้หน้าพังหรือว่างเปล่า". The rest of the dashboard renders independently of this component's state.
- [x] Verified — `[frontend]` Drill-down from AI summary to source KPI numbers (reuse Phase 4) — `CoachingInsightPanel`'s "ดูที่มาของตัวเลข" button calls the same `onDrillDown` callback the dashboard already wires to `KpiDrillDownModal` (Phase 4's component) — genuinely reused, not duplicated, defaults to `REVENUE_VS_TARGET`.
- [x] Verified — `[frontend]` System-wide AI on/off switch (MANAGER only, `EvaluationSetting.aiEnabled`) — confirmed this is **correctly Phase 4's existing `EvaluationSettingForm.tsx`**, unchanged, already exposing `aiEnabled`/`aiAnonymize` behind the same `canEdit === MANAGER` gate verified in Phase 4's FULL round. Live re-confirmed the toggle actually changes generation behavior this round (see `aiEnabled` switch entry above) — Phase 4 verified the UI wiring, this round verified the switch's actual effect on Phase 6 behavior.

## Design/requirement contract checks — Phase 5 & 6

- **Data Model**: No new models were required for Phase 5 (read-only). `CoachingInsight` (Phase 6) in `backend/prisma/schema.prisma` matches `design.md`'s Data Model block field-for-field — `id`, `salespersonId`/`salesperson` relation, `periodType`/`year`/`periodNumber`, `kpiSnapshot: Json`, `contentTh: String?`, `status: InsightStatus @default(PENDING)`, `provider`/`model`/`errorMessage: String?`, `isStale: Boolean @default(false)`, `generatedById`/`generatedBy`, `generatedAt`, the `@@unique([salespersonId, periodType, year, periodNumber])` constraint, and the `@@index([year, periodType])` — checked every line side by side against `design.md`, no drift. `PeriodType`/`InsightStatus` enums also match exactly.
- **Leaderboard contract** (`design.md`/`requirement.md` Core Feature 6): all 4 criteria (คะแนนรวม/% ทำได้ตามเป้า/ยอดขาย/ลูกค้าใหม่) implemented, real names shown, month/quarter/year period switching — confirmed live.
- **AI coaching contract** (`design.md`'s "AI coaching" paragraph, KPI & Scoring Rules section): "คำนวณ KPI และการเปรียบเทียบ...ให้เสร็จในระบบก่อน แล้วส่งเฉพาะผลสรุปเชิงตัวเลขที่ปิดบังชื่อแล้ว" — confirmed, the payload is built entirely from already-computed `kpi.service.ts` output and anonymized before it's used to build the Gemini prompt or stored as the "actually sent" snapshot. "ถ้าเรียกไม่สำเร็จ → status = FAILED และหน้าจอต้องแสดงตัวเลข KPI และการเปรียบเทียบตามปกติ พร้อมข้อความสรุปแบบ rule-based สำรอง" — confirmed live exactly as specified.
- **`requirement.md`'s "ข้อเสนอแนะ (ไม่บังคับ)" on anonymization** ("พิจารณาปิดบังชื่อเต็มของโรงพยาบาลและชื่อพนักงานขาย...หากไม่กระทบคุณภาพของคำแนะนำ") — `design.md` locked this to **mandatory** behind the `aiAnonymize` setting (default `true`, confirmed seeded/set that way in the live dev DB), which is a stricter, confirmed-with-the-user resolution of the original "suggested, not required" language — correctly implemented as the locked, not the suggested, version.
- **Permission decision on `POST /generate` — `plan.md`'s own Unresolved Open Question #4**: `design.md` never resolved this; `plan.md` explicitly flagged it and told the engineer to ask the user directly if still unclear at Phase 6 start. The engineer instead made a documented judgment call (MANAGER any / SALESPERSON own-only) rather than asking. The call itself is reasonable and matches the general "view-everything, edit-restricted" pattern used everywhere else in the app, and it's **implemented consistently** (verified live, all 4 cases above) — but it is still an unconfirmed business decision, not a settled one, and is carried to Open Issues routed to `business-analyst` rather than treated as resolved by this QA pass.
- **View-everything / edit-restricted role model**: confirmed live in both directions across `/leaderboard` (fully open) and `/coaching-insights/:salespersonId` (`GET` open, `POST .../generate` restricted per the rule above) — consistent with the pattern already verified in Phases 1-4.

## Issues Found — Phase 5 & 6

No implementation bugs were found in either phase. Everything specifically called out in this round's brief to scrutinize (anonymization completeness, permission enforcement on both `GET`/generate, the `rank:null`-never-`0` rule with real zero-computable-metric salespeople, `isStale` set/clear, absence of duplicate KPI logic, responsive layout) held up under live testing, with one exception and two carried-forward gaps, none of which are implementation bugs:

1. **Responsive layout not visually verified** (Phase 5) — a verification-method gap, not a code defect; code inspection shows sound mobile-first Tailwind patterns. Routed to a future `qa-engineer` round with viewport tooling. See Open Issues.
2. **`isStale`-set-by-import not live-driven this round** (Phase 6) — blocked by this session's tooling classifier on the file-upload action itself, not a code concern; the underlying code is unchanged since Phase 2's own inspection of it. Routed to a future `qa-engineer` round. See Open Issues.
3. **`sentToGemini` flag semantics** (Phase 6) — a minor, unconfirmed-as-actual-problem observation worth `security`'s attention given Module G's whole point is auditing what data leaves the org. See Open Issues.
4. **The SALESPERSON-own-only generate permission is an unconfirmed business decision**, correctly and consistently implemented but never actually signed off by the user. Routed to `business-analyst`. See Open Issues.

None of the four block accepting Phase 5/6's functional correctness; they are process/coverage notes and one design-decision confirmation, not defects to send back to an engineer.

## Review Outcome — Phase 5 & 6

**Phase 5 (Dashboards & Leaderboard, Module F) — FULL round, 10/11 tasks ✅ Verified, 1/11 ⚠️ Partial (responsive layout — verification-method gap only, not a functional defect). `plan.md` boxes checked for the 10.**

**Phase 6 (AI Coaching Insights, Module G) — FULL round, 14/14 tasks ✅ Verified. All `plan.md` boxes checked.** Zero implementation defects. The Gemini-success path is structurally implemented and code-reviewed but genuinely untestable this round (`GEMINI_API_KEY` empty) — an inherent limitation of this round, not a defect, and should be re-tested live once a real key is available (ideally before or during `devops`'s deploy prep, not left for production to discover).

Both phases pending the user's decision (see below). If accepted:
- Phase 5 needs no `security` pass (read-only, not flagged `⚠️ Sensitive`) and is eligible for `devops` consideration on its own merits, **once** the pre-existing deploy blockers on Phases 1-3 (outstanding `security` gates on Modules B/C, and Phase 2/3's need for a FULL re-pass before `devops` per this file's own gate) are also resolved — Phase 5 doesn't remove those blockers, it just doesn't add new ones.
- Phase 6 is **not** eligible for `devops` until `security` runs its required Module G pass (`design.md`'s own explicit flag) — this functional QA pass is not a substitute for that, and this round's own gate rule (FULL round required before the `security`→`devops` handoff) is satisfied by this being a FULL round, but the security pass itself still has to happen.
