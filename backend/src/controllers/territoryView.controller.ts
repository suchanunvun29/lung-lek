import ExcelJS from "exceljs"; import { Request, Response } from "express"; import { getTerritoryView } from "../services/territoryView.service"; import { TerritoryViewQuery } from "../validators/territoryView.validators"; import { canViewSalesperson } from "../services/viewerScope.service";
export async function getMyTerritoryView(req: Request, res: Response) { if (!(await canViewSalesperson(req.user!, req.params.salespersonId))) return res.status(403).json({ error: "Forbidden" }); const result = await getTerritoryView(req.params.salespersonId, req.query as unknown as TerritoryViewQuery); if (!result) return res.status(404).json({ error: "Salesperson not found" }); res.json({ period: req.query, ...result }); }
export async function exportMyTerritoryView(req: Request, res: Response) { if (!(await canViewSalesperson(req.user!, req.params.salespersonId))) return res.status(403).json({ error: "Forbidden" }); const result = await getTerritoryView(req.params.salespersonId, req.query as unknown as TerritoryViewQuery); if (!result) return res.status(404).json({ error: "Salesperson not found" }); const workbook = new ExcelJS.Workbook(); const sheet = workbook.addWorksheet("Territory view"); sheet.columns = [{ header: "รายการ", key: "type", width: 28 }, { header: "โรงพยาบาล", key: "hospital", width: 40 }, { header: "ยอดขาย", key: "revenue", width: 18 }]; result.soldHospitals.forEach((row) => sheet.addRow({ type: "ขายได้แล้ว", hospital: row.hospital?.displayName, revenue: row.revenue })); result.soldBeforeButNotInPeriod.forEach((hospital) => sheet.addRow({ type: "เคยขายได้ แต่ไม่มีในงวดนี้", hospital: hospital.displayName })); res.setHeader("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"); res.setHeader("Content-Disposition", "attachment; filename=my-territory-view.xlsx"); await workbook.xlsx.write(res); res.end(); }
