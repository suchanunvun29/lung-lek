import { Router } from "express"; import { authenticate, requirePasswordChanged, requireRole } from "../middleware/authenticate"; import { validate } from "../middleware/validate"; import { asyncHandler } from "../utils/asyncHandler"; import * as c from "../controllers/territory.controller"; import * as v from "../validators/territory.validators";
const router = Router(); router.use(authenticate, requirePasswordChanged); router.get("/", validate(v.assignmentQuerySchema, "query"), asyncHandler(c.listAssignments)); router.put("/", requireRole("MANAGER"), validate(v.assignmentBodySchema, "body"), asyncHandler(c.putAssignment)); export default router;
