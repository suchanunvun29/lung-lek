import { Router } from "express"; import { authenticate, requirePasswordChanged } from "../middleware/authenticate"; import { validate } from "../middleware/validate"; import { asyncHandler } from "../utils/asyncHandler"; import * as c from "../controllers/territoryProductRanking.controller"; import { territoryProductParamsSchema, territoryProductQuerySchema } from "../validators/territoryProductRanking.validators";
const router=Router();router.use(authenticate,requirePasswordChanged);router.get("/:territoryId/export",validate(territoryProductParamsSchema,"params"),validate(territoryProductQuerySchema,"query"),asyncHandler(c.exportRanking));router.get("/:territoryId",validate(territoryProductParamsSchema,"params"),validate(territoryProductQuerySchema,"query"),asyncHandler(c.getRanking));export default router;
