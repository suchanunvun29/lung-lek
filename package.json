{
  "name": "sales-evaluation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "dotnet watch --project src/SalesEvaluation.Api & npm run dev --prefix frontend",
    "build": "dotnet build && npm run build --prefix frontend",
    "start": "dotnet run --project src/SalesEvaluation.Api --configuration Release & npm run start --prefix frontend",
    "test": "dotnet test",
    "typecheck": "npm run typecheck --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
